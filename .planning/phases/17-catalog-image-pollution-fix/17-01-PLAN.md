---
phase: 17-catalog-image-pollution-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sheets/drive.ts
  - tests/sheets/drive.test.ts
autonomous: true
requirements: [R17-01]

must_haves:
  truths:
    - "Every call to `downloadFromDrive`, `getDriveFileMetadata`, `trashDriveFile`, and `uploadToDrive` aborts within ~30s when the underlying gaxios request hangs (no more 2.5h indefinite stalls like 2026-05-13)."
    - "On timeout the wrapper retries up to 2 more times (3 attempts total) with exponential backoff; on the 3rd timeout the original error is rethrown."
    - "Non-timeout errors (404, 403, 5xx) propagate immediately without retry — operator/caller can distinguish a hard 404 from a network stall."
    - "An audit `--all` run that previously hung at pid 1376842 (435/449) now completes the full 449-pid scan in bounded wall-clock time (the stuck Drive fetch surfaces as a per-pid retry+throw rather than a process-wide deadlock)."
  artifacts:
    - path: "src/sheets/drive.ts"
      provides: "Phase 17-01 timeout-wrapped Drive helpers (B-1 fix)"
      exports: ["downloadFromDrive", "getDriveFileMetadata", "trashDriveFile", "uploadToDrive"]
      contains: "DRIVE_TIMEOUT_MS"
    - path: "tests/sheets/drive.test.ts"
      provides: "Unit coverage for timeout + retry budget on all 4 helpers"
      contains: "isTimeoutError"
  key_links:
    - from: "src/sheets/drive.ts"
      to: "gaxios (transitive via googleapis)"
      via: "second-arg request options carry `timeout: DRIVE_TIMEOUT_MS`"
      pattern: "timeout: DRIVE_TIMEOUT_MS"
    - from: "src/sheets/drive.ts"
      to: "src/lib/logger.ts"
      via: "warn-log every retry attempt with fileId + attempt count (no creds in message)"
      pattern: "drive\\] .* timeout \\(attempt"
---

<objective>
Patch `src/sheets/drive.ts` to add a 30s gaxios `timeout` option + retry-up-to-2 wrapper around every Drive HTTP call, addressing B-1 from `PRODUCTION-AUDIT-FINDINGS.md`. This is the blocking prerequisite for ALL Phase 17 follow-up plans — Plan 17-02/17-03/17-04 each re-trigger full-catalog Drive scans that would re-hit the 2026-05-13 hang without this fix.

Purpose: convert the unbounded hang observed at pid 1376842 (435/449 pids into the audit run, after 2.5 hours of normal operation) into a bounded per-call failure. The audit script's per-pid try/catch boundary already handles thrown errors — what it can't handle is a Promise that never resolves. After 17-01, the worst-case extra latency per stuck call is `30s × 3 retries = 90s`, but normal-case overhead is zero (timeout doesn't fire when the call returns promptly).

Output:
- Patched `src/sheets/drive.ts` (no new exports, no changed signatures — purely internal behavior).
- New `tests/sheets/drive.test.ts` with timeout + retry coverage on all 4 helpers (Wave 0 test scaffolding per RESEARCH §Validation Architecture).

Scope note on B-2/B-3/B-4 (per user prompt "You decide whether B-2/B-3/B-4 are separate PLAN.md files or tasks inside 17-01"):
- **B-2** (manual CLI `--dry-run` trash gate) ships as `17-06-PLAN.md` — different file (`fix-image-pollution-manual.ts`), different concern (operator-CLI safety gate), worth its own commit + test.
- **B-3** (cleanup script `MANUAL/` blind spot) ships as `17-07-PLAN.md` — different file (`cleanup-checkpoint-test-data.ts`), one-shot script. Worth its own commit so its own test file is created at the same time.
- **B-4** (Tier 2 OpenAI billing-hard-limit short-circuit) ships as `17-08-PLAN.md` — different file (`scripts/fix-image-pollution.ts`), different concern (error short-circuit), needs to touch trail-row operation enum. Worth its own commit + test.

Wave 1 runs all four (17-01 + 17-06 + 17-07 + 17-08) in parallel since they touch disjoint files (`drive.ts`, `fix-image-pollution-manual.ts`, `cleanup-checkpoint-test-data.ts`, `fix-image-pollution.ts`).
</objective>

<execution_context>
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-catalog-image-pollution-fix/17-CONTEXT.md
@.planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md
@.planning/research/phase17-prep/PRODUCTION-AUDIT-FINDINGS.md
@src/sheets/drive.ts
@src/shopify/image-standardizer.ts
@src/shopify/clone-pusher.ts
@tests/sheets/reader.test.ts

<interfaces>
<!-- Existing src/sheets/drive.ts exports — keep signatures stable. -->
import { drive_v3 } from 'googleapis';
import { logger } from '../lib/logger.js';

export function createDriveClient(): drive_v3.Drive;
export async function uploadToDrive(
  drive: drive_v3.Drive,
  buffer: Buffer,
  filename: string,
  supplierCode: string,
  styleId: string,
): Promise<string>;
export async function downloadFromDrive(drive: drive_v3.Drive, fileId: string): Promise<Buffer>;
export async function trashDriveFile(drive: drive_v3.Drive, fileId: string): Promise<void>;
export async function getDriveFileMetadata(
  drive: drive_v3.Drive,
  fileId: string,
): Promise<DriveFileMetadata>;
export function extractFileId(url: string): string | null;
export interface DriveFileMetadata { mimeType: string; size: string; name: string; }

<!-- gaxios timeout option (verified — gaxios is the transitive HTTP layer of googleapis).
     Pass as the second positional arg to drive.files.get/.update/.list/.create:

       await drive.files.get(
         { fileId, alt: 'media', supportsAllDrives: true },     // method params (1st arg)
         { responseType: 'arraybuffer', timeout: 30_000 },      // request options (2nd arg)
       );

     For methods that already pass a 2nd-arg options object (downloadFromDrive uses
     `{ responseType: 'arraybuffer' }`), MERGE the timeout into that object. For
     methods that don't currently pass a 2nd arg (e.g. trashDriveFile, files.list
     inside findOrCreateFolder), add a new options object `{ timeout: 30_000 }`. -->

<!-- Proven timeout pattern (raw fetch — different shape, but same intent — see
     src/shopify/image-standardizer.ts lines 244-259 and src/shopify/clone-pusher.ts
     lines 35-51 for AbortController + setTimeout + retry budget). The gaxios path
     is simpler (option flag, not AbortController), but the retry/backoff shape is
     identical. -->

<!-- Error classification: timeouts vs hard errors. gaxios surfaces timeouts
     with one of several signals depending on Node/SDK version:
       - err.code === 'ECONNABORTED'   (axios-style legacy)
       - err.code === 'ETIMEDOUT'      (Node socket timeout)
       - err.code === 'ERR_CANCELED'   (AbortController under the hood)
       - err.message.includes('timeout') or 'aborted' or 'canceled'
     Treat ANY of those as a timeout; everything else as a hard error (rethrow). -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drive API → Buffer | Drive may stall mid-stream or refuse to terminate the TLS session — produces unbounded Promise. |
| Logger → stdout | Retry-log lines include fileId; MUST NOT include service-account creds or auth tokens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | D (DoS) — stuck Drive fetch hangs entire script | All 4 Drive helpers | mitigate | Set `timeout: DRIVE_TIMEOUT_MS` (30_000) on every gaxios request via the 2nd-arg options object. Retry-once-then-throw budget limits worst-case extra latency to ~90s per stuck call. |
| T-17-02 | I (Information Disclosure) — credentials leak via timeout error message | Retry log line + thrown error | mitigate | `logger.warn` retry messages format only `fileId + attempt count`. Never log the gaxios `err.config` (it contains auth headers). Verify in unit test: assert `mock.calls[0][0]` does NOT contain 'Bearer', 'Authorization', 'private_key', or 'GOOGLE_PRIVATE_KEY'. |
| T-17-03 | R (Repudiation) — silent timeout retry masks an underlying outage | Retry path | accept | Each retry warns to logger; operator sees `attempt 1/3 ... attempt 2/3 ... attempt 3/3 timeout` in stderr. Trail TSV is untouched by this plan — the audit-pollution trail still captures the per-pid failure if the 3rd attempt throws. |
| T-17-04 | T (Tampering) — partial upload on `uploadToDrive` retry causes Drive duplication | `uploadToDrive` retry | mitigate | `uploadToDrive`'s update-in-place semantics (find existing → update; else create + grant permissions) make duplicate creates impossible on retry: a 2nd attempt re-runs the same `files.list` lookup, finds the (possibly-partially-uploaded) prior file, and updates it. Test: mock `files.create` to throw timeout once; assert 2nd attempt calls `files.list` again before retrying. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add timeout + retry wrapper to all 4 Drive helpers</name>
  <files>
    src/sheets/drive.ts,
    tests/sheets/drive.test.ts
  </files>
  <read_first>
    - src/sheets/drive.ts (entire file — all 4 target functions plus the internal `findOrCreateFolder` helper at lines 54-83, since findOrCreateFolder calls `drive.files.list` and `drive.files.create` that ALSO need the timeout patch)
    - src/shopify/image-standardizer.ts lines 244-259 (verified AbortController + setTimeout + retry pattern — proves the repo already accepts this kind of wrapper for non-gaxios fetch sites; we are NOT copying it, but the retry-with-backoff cadence is the model)
    - src/shopify/clone-pusher.ts lines 35-51 (same pattern with retry budget)
    - tests/sheets/reader.test.ts (existing test in this directory — DI seam pattern for mocking the `drive_v3.Drive` shape; copy the mock setup)
    - .planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md "Pattern A: Drive timeout helper (17-01)" lines 717-768
    - .planning/research/phase17-prep/PRODUCTION-AUDIT-FINDINGS.md "B-1" row (line 59) — operator-facing impact statement (the audit hang at pid 1376842)
  </read_first>
  <behavior>
    Unit tests (mock `drive_v3.Drive` via `vi.fn` — no real Drive calls):

    - Test 1 (downloadFromDrive happy path unchanged): mocked `drive.files.get` resolves immediately with `{ data: <ArrayBuffer> }`. Assert: `downloadFromDrive` returns the expected Buffer; `drive.files.get` called exactly once; no retry warning logged.
    - Test 2 (downloadFromDrive timeout + retry): mocked `drive.files.get` throws `{ code: 'ECONNABORTED', message: 'timeout of 30000ms exceeded' }` on the first 2 calls, then resolves on the 3rd. Assert: `drive.files.get` called 3 times; `logger.warn` called 2 times (one per retry) with messages containing `'attempt 1/3'` and `'attempt 2/3'` and the fileId; final result is the expected Buffer.
    - Test 3 (downloadFromDrive all 3 attempts time out → throw): mocked throws timeout on all 3 attempts. Assert: `drive.files.get` called exactly 3 times; final error is rethrown (operator sees the actual timeout, not a generic wrapper); `logger.warn` called 3 times.
    - Test 4 (downloadFromDrive non-timeout error rethrown immediately): mocked `drive.files.get` throws `{ code: 404, message: 'File not found' }`. Assert: `drive.files.get` called exactly once (NO retry on hard errors); the 404 error rethrown; `logger.warn` NOT called.
    - Test 5 (getDriveFileMetadata timeout retry): same shape as Test 2, but for `drive.files.get` invoked with `fields: 'mimeType,size,name'`. Assert retry behavior + final return shape `{ mimeType, size, name }`.
    - Test 6 (trashDriveFile timeout retry): mocked `drive.files.update` throws `{ code: 'ETIMEDOUT' }` twice then resolves. Assert: 3 calls; 2 warns; resolves to `void`.
    - Test 7 (uploadToDrive timeout retry on `files.list` lookup): the internal `findOrCreateFolder` helper calls `drive.files.list` — mock it to throw `{ code: 'ERR_CANCELED' }` once then resolve. Assert: 2 calls; 1 warn; upload completes.
    - Test 8 (uploadToDrive timeout retry on `files.create`): mock `drive.files.create` to throw `{ message: 'request aborted' }` once then resolve. Assert: 2 calls to `files.create`; 1 warn; final returned URL is `https://drive.google.com/uc?id=<id>`.
    - Test 9 (T-17-02 credential leak guard): inspect EVERY `logger.warn` call recorded across Tests 2/3/5/6/7/8. Assert NO message contains the strings `'Bearer'`, `'Authorization'`, `'private_key'`, `'GOOGLE_PRIVATE_KEY'`, or `'auth'` (case-insensitive). Retry log lines should contain only `fileId` + `attempt N/3` + a verb like `'timeout'` or `'aborted'`.
    - Test 10 (T-17-04 update-in-place on retry): mocked `files.list` returns a single existing file `{ id: 'EXISTING' }`. Mocked `files.update` throws timeout once then resolves. Assert: `files.list` called either once (lookup result cached within one call to `uploadToDrive`) or twice (re-lookup on retry — either is acceptable); `files.create` NEVER called; final URL contains `'EXISTING'` (we updated, not duplicated).
    - Test 11 (timeout constant exported or const-checked): `grep -nE "^const DRIVE_TIMEOUT_MS = 30_000" src/sheets/drive.ts` matches one line. Magic numbers belong in named constants per global CLAUDE.md "code quality" rules.
  </behavior>
  <action>
    Modify `src/sheets/drive.ts` in place. Do NOT change any export signature. Do NOT add new exports.

    1. **Add constants at the top of the file** (after the existing `ROOT_FOLDER_ID` line, before `findOrCreateFolder`):

       ```
       const DRIVE_TIMEOUT_MS = 30_000;
       const DRIVE_RETRY_MAX = 2;  // 3 attempts total (initial + 2 retries)
       ```

    2. **Add `isTimeoutError` helper (file-local, not exported)** below the constants:

       ```
       function isTimeoutError(err: unknown): boolean {
         const code = (err as { code?: string | number })?.code;
         const message = (err as Error)?.message ?? '';
         return (
           code === 'ECONNABORTED' ||
           code === 'ETIMEDOUT' ||
           code === 'ERR_CANCELED' ||
           message.includes('aborted') ||
           message.includes('timeout') ||
           message.includes('canceled')
         );
       }
       ```

    3. **Wrap every Drive HTTP call body in a retry loop**. Apply the pattern to:
       - `downloadFromDrive` (current body: single `drive.files.get` call with `{ responseType: 'arraybuffer' }` 2nd arg)
       - `getDriveFileMetadata` (current body: single `drive.files.get` call with `fields`)
       - `trashDriveFile` (current body: single `drive.files.update` call with `requestBody: { trashed: true }`)
       - `uploadToDrive` (3+ separate Drive calls: the body of `findOrCreateFolder` runs twice; then `files.list` for existence check; then either `files.update` or `files.create` + `permissions.create`). Wrap each of these calls individually.
       - The internal `findOrCreateFolder` helper (lines 54-83) — wrap its `files.list` and `files.create` calls.

       Pattern (use this exact loop shape — adapt the method call line):

       ```
       let lastErr: unknown;
       for (let attempt = 0; attempt <= DRIVE_RETRY_MAX; attempt++) {
         try {
           const response = await drive.files.get(
             { fileId, alt: 'media', supportsAllDrives: true },
             { responseType: 'arraybuffer', timeout: DRIVE_TIMEOUT_MS },
           );
           return Buffer.from(response.data as ArrayBuffer);
         } catch (err) {
           lastErr = err;
           if (!isTimeoutError(err)) throw err;
           logger.warn(
             `[drive] downloadFromDrive timeout (attempt ${attempt + 1}/${DRIVE_RETRY_MAX + 1}) on ${fileId}`,
           );
         }
       }
       throw lastErr ?? new Error('downloadFromDrive failed after retries');
       ```

       Notes per helper:
       - `downloadFromDrive`: the existing 2nd arg `{ responseType: 'arraybuffer' }` MUST be augmented to `{ responseType: 'arraybuffer', timeout: DRIVE_TIMEOUT_MS }`. Do not introduce a separate 3rd arg — gaxios reads `timeout` from the same options object.
       - `getDriveFileMetadata`: currently `drive.files.get({ fileId, fields: 'mimeType,size,name', supportsAllDrives: true })` has NO 2nd arg. Add `{ timeout: DRIVE_TIMEOUT_MS }` as a new 2nd positional arg.
       - `trashDriveFile`: currently `drive.files.update({ fileId, requestBody: {...}, supportsAllDrives: true })` has NO 2nd arg. Add `{ timeout: DRIVE_TIMEOUT_MS }` as a 2nd arg.
       - `uploadToDrive`: this function calls THREE different Drive endpoints in sequence (`findOrCreateFolder` twice → `files.list` for existence → `files.update` OR `files.create` + `permissions.create`). Each individual call gets the timeout+retry wrapper. To keep readability, factor the retry loop into a small private helper:

         ```
         async function withDriveRetry<T>(
           op: () => Promise<T>,
           label: string,
         ): Promise<T> {
           let lastErr: unknown;
           for (let attempt = 0; attempt <= DRIVE_RETRY_MAX; attempt++) {
             try {
               return await op();
             } catch (err) {
               lastErr = err;
               if (!isTimeoutError(err)) throw err;
               logger.warn(
                 `[drive] ${label} timeout (attempt ${attempt + 1}/${DRIVE_RETRY_MAX + 1})`,
               );
             }
           }
           throw lastErr ?? new Error(`${label} failed after retries`);
         }
         ```

         Then rewrite each helper as a single call to `withDriveRetry(() => drive.files.X(...), 'helperName on fileId=...')`. This is mandatory — DO NOT inline the loop body 4+ times. Inline duplication has been a source of drift in this codebase.

       - Make sure every gaxios call inside the helpers carries `timeout: DRIVE_TIMEOUT_MS` in its 2nd-arg options object. The `withDriveRetry` wrapper enforces the retry budget; the gaxios `timeout` option enforces the per-attempt abort. Both layers are required — `timeout` without `withDriveRetry` would throw after 30s with no retry; `withDriveRetry` without `timeout` would never know to stop the first attempt.

    4. **Logger hygiene** (T-17-02): the retry log message format must be exactly `[drive] <label> timeout (attempt N/M) [on <fileId>]`. The fileId is OK to log (it's a public ID); the auth header is NOT. Never log the raw `err.config` or `err.response`. If a future contributor needs to debug, they can add a separate `logger.debug` line gated on `process.env.DEBUG_DRIVE`.

    5. **Create `tests/sheets/drive.test.ts`** (new file). Mock `drive_v3.Drive` via `vi.fn()` for every method. Cover Tests 1-11 above. Mock the `logger` module (`vi.mock('../../src/lib/logger.js', () => ({ logger: { warn: vi.fn(), info: vi.fn(), error: vi.fn() } }))`). Use `beforeEach(() => vi.clearAllMocks())` to keep call-count assertions isolated per test.

       Critical setup detail for Test 1 (happy path): the test must verify ZERO retry warnings logged. After import, mock the logger module BEFORE importing `src/sheets/drive.ts` (vitest hoists `vi.mock`). The test scaffold pattern is identical to `tests/sheets/reader.test.ts`.

    6. **DO NOT** modify the `extractFileId` function (string parser — no Drive calls).

    7. **DO NOT** introduce new dependencies (no `p-retry`, no `async-retry`, no `axios-retry`). The wrapper is ~20 LOC + gaxios's built-in `timeout`. Per RESEARCH §"Don't Hand-Roll" row: "HTTP fetch timeout — Don't Build: Custom timer + manual abort. Use Instead: Existing pattern in src/shopify/image-standardizer.ts (AbortController + setTimeout) for raw fetch; gaxios `timeout` option for googleapis. — Why: Both already proven in repo."
  </action>
  <verify>
    <automated>npx vitest run tests/sheets/drive.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^const DRIVE_TIMEOUT_MS = 30_000" src/sheets/drive.ts` matches exactly one line.
    - `grep -nE "^const DRIVE_RETRY_MAX = 2" src/sheets/drive.ts` matches exactly one line.
    - `grep -nE "function isTimeoutError\\(" src/sheets/drive.ts` matches exactly one line.
    - `grep -nE "function withDriveRetry" src/sheets/drive.ts` matches exactly one line.
    - `grep -cE "timeout: DRIVE_TIMEOUT_MS" src/sheets/drive.ts` returns at least 4 (one per Drive HTTP call; uploadToDrive has multiple, so likely 6+).
    - `grep -nE "withDriveRetry\\(\\(\\) =>" src/sheets/drive.ts` matches at least 4 lines (one wrapped call per public helper, plus the findOrCreateFolder internals).
    - `grep -nE "\\[drive\\] .* timeout \\(attempt" src/sheets/drive.ts` matches at least 1 line (retry warn format).
    - `grep -ciE "(Bearer|Authorization|private_key|GOOGLE_PRIVATE_KEY)" src/sheets/drive.ts | grep -v '^#'` — the file must not introduce any of those tokens (the existing `GOOGLE_PRIVATE_KEY` env var read at line 30 already exists and is unchanged; this grep is a sanity check that the retry log lines do not embed creds). Run: `grep -niE "logger\\.(warn|info|error).*(?:Bearer|Authorization|private_key)" src/sheets/drive.ts` returns ZERO matches.
    - `npx vitest run tests/sheets/drive.test.ts` reports 11 tests passing.
    - `npx vitest run` (full suite) reports no regressions in Phase 16 tests (`tests/scripts/audit-image-pollution.test.ts`, `tests/scripts/fix-image-pollution.test.ts`, `tests/scripts/fix-image-pollution-manual.test.ts`).
  </acceptance_criteria>
  <done>
    All 4 public Drive helpers (`downloadFromDrive`, `getDriveFileMetadata`, `trashDriveFile`, `uploadToDrive`) and the internal `findOrCreateFolder` wrap every gaxios call in `withDriveRetry` + carry `timeout: DRIVE_TIMEOUT_MS` in the gaxios options. 11/11 tests green. No credential leak in log output. Public export signatures unchanged.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/sheets/drive.test.ts` reports 11/11 tests passing.
- `npx vitest run` (full suite) shows no Phase 15 or Phase 16 regressions.
- Smoke test (manual, operator only — optional): with the Drive client wired, force a fake stuck call by mocking `drive.files.get` at the call site OR by pointing it at a never-responding HTTP endpoint, confirm the helper throws within ~90s (30s × 3 attempts) instead of hanging indefinitely.
</verification>

<success_criteria>
- All Drive helper calls have bounded latency. The 2026-05-13 hang scenario (Drive fetch stuck for 2.5 hours) is no longer possible — worst-case extra latency per stuck call is `30s × 3 attempts = 90s`.
- Retry budget is observable (logger.warn emits one line per retry; final throw surfaces the gaxios error verbatim).
- No credential leakage in log output (T-17-02 mitigation tested).
- `uploadToDrive`'s update-in-place semantics survive retry (T-17-04 mitigation tested).
- Public API surface unchanged — no caller (audit script, fix orchestrator, manual CLI) needs to be modified.
</success_criteria>

<output>
After completion, create `.planning/phases/17-catalog-image-pollution-fix/17-01-SUMMARY.md` recording: total lines added to `src/sheets/drive.ts`, the 11 test cases (one bullet each with what it asserts), any deviation from the planned pattern (e.g., if you found a 5th Drive call site inside `findOrCreateFolder` worth wrapping), and a note on whether the operator should manually re-run the audit `--all` to confirm the original hang is gone (recommend: yes, on the same 449-pid corpus as 2026-05-13).
</output>
