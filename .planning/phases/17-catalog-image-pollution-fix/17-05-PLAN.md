---
phase: 17-catalog-image-pollution-fix
plan: 05
type: execute
wave: 4
depends_on: ["17-01", "17-02", "17-03", "17-04", "17-06"]
files_modified: []
autonomous: false
requirements: [R17-05]

must_haves:
  truths:
    - "After 17-01 + 17-02 + 17-03 + 17-04 + 17-06 ship, operator re-runs the full audit + fix orchestrator and obtains a fresh manual queue TSV at `tmp/image-pollution-manual-queue-{date}.tsv`."
    - "Manual queue size ≤ 20 after re-run (R6 hard cap from Phase 16 — should be comfortably under thanks to 17-02 + 17-04 unlocking ~50-100 Tier 1 fixes and 17-03 closing 84% of Model* pollution)."
    - "Operator walks every remaining manual-queue pid via `scripts/fix-image-pollution-manual.ts` interactively."
    - "After manual triage, re-running the audit on the original 213-pid corpus shows ≤ 5 remaining polluted pids (truly unresolvable — likely New Era proprietary NE220 / NE7xx codes with no supplier model and rejected by Phase 16's AI Tier 2)."
    - "Phase 17 closes per Phase 16's R10 OR-path: either re-audit reports 0 polluted OR every original-polluted pid has a BR_WRITE row in the trail TSV."
  artifacts:
    - path: "tmp/image-pollution-audit-{YYYY-MM-DD}.tsv"
      provides: "Post-17-04 audit output (fresh run)"
      contains: "pid"
    - path: "tmp/image-pollution-manual-queue-{YYYY-MM-DD}.tsv"
      provides: "Post-orchestrator manual queue (Tier 3 input for the operator walkthrough)"
      contains: "pid"
    - path: ".planning/phases/17-catalog-image-pollution-fix/17-05-SUMMARY.md"
      provides: "Operator-recorded triage outcomes per pid"
      contains: "manual queue size"
  key_links:
    - from: "operator runbook"
      to: "scripts/fix-image-pollution-manual.ts (existing — Phase 16 shipped)"
      via: "interactive readline walkthrough with [r]/[s]/[a]/[d]/[v]/[q] menu"
      pattern: "fix-image-pollution-manual"
---

<objective>
Operator-only triage of the residue. NO ENGINEERING. After Plans 17-01..17-04 + 17-06 (B-2 dry-run gate) all ship, the operator runs the full Phase 16 audit + fix flow end-to-end against the current BR catalog, then walks every remaining manual-queue pid through the existing `scripts/fix-image-pollution-manual.ts` interactive CLI.

Per the locked plan execution order in 17-CONTEXT.md, this is the LAST plan to execute in Phase 17 (it depends on every other plan shipping so the residue is as small as possible).

Purpose: close out Phase 17 by handling the truly-unresolvable-automatically pids — primarily expected to be New Era proprietary NE220/NE7xx codes (no S&S coverage per RESEARCH Finding 3) and a handful of anvil 9xx + M786 + QTB6000 pids that no scraper can reach.

Tooling: 100% pre-existing. Phase 16 shipped `scripts/fix-image-pollution-manual.ts` (1040 lines, 21 unit tests, operator-checkpoint-approved 2026-05-13). 17-06 patched the `--dry-run` Drive-trash gap. Operator is the entire workforce for this plan.

Output:
- A fresh audit TSV from the re-run.
- A fresh manual queue TSV with ≤ 20 pids.
- Operator's per-pid decisions logged to the shared trail TSV (via the manual CLI's normal flow).
- 17-05-SUMMARY.md documenting which pids were handled, which were skipped/accepted-as-is, and which (if any) genuinely cannot be resolved.
</objective>

<execution_context>
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-catalog-image-pollution-fix/17-CONTEXT.md
@.planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md
@.planning/research/phase17-prep/PRODUCTION-AUDIT-FINDINGS.md
@.planning/phases/17-catalog-image-pollution-fix/17-01-PLAN.md
@.planning/phases/17-catalog-image-pollution-fix/17-02-PLAN.md
@.planning/phases/17-catalog-image-pollution-fix/17-03-PLAN.md
@.planning/phases/17-catalog-image-pollution-fix/17-04-PLAN.md
@.planning/phases/17-catalog-image-pollution-fix/17-06-PLAN.md
@.planning/phases/16-catalog-image-pollution-audit-fix/16-04-PLAN.md
@scripts/audit-image-pollution.ts
@scripts/fix-image-pollution.ts
@scripts/fix-image-pollution-manual.ts

<interfaces>
<!-- The manual CLI menu (Phase 16) -->
//   [r]eplace      — operator-supplied URL/fileId; verifier-after-fix gates commit
//   [s]kip         — MANUAL_SKIP trail row; pid stays unresolved
//   [a]ccept       — MANUAL_ACCEPT trail row; current image is fine as-is
//   [d]elete       — literal 'DELETE' required; blanks BR cell, trashes Drive file
//   [v]iew         — opens the current image in default viewer (operator inspects)
//   [q]uit         — exits cleanly; progress fsync'd per row

<!-- Phase 16 R10 OR-path: phase closes when re-audit reports 0 OR every
     original-polluted pid has a BR_WRITE row in the trail. -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator stdin → Drive/BR | Operator decisions drive irreversible writes. The Phase 16 manual CLI already mitigates with literal DELETE/FORCE confirmations + verifier-after-fix. |
| Verifier output → BR mutation | gpt-4o-mini Vision is the gatekeeper for `[r]eplace`. Operator can `[f]orce` if confident. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-19 | T — operator deletes wrong cell | manual CLI [d]elete path | mitigate | Phase 16's literal 'DELETE' confirmation still in place. 17-06 (B-2) also gates `--dry-run` against the trash call (closed footgun). Operator can preview with `[v]iew` before `[d]elete`. |
| T-17-20 | R — operator can't tell which manual-queue rows are leftover from older runs | trail TSV resume | mitigate | Phase 16's `loadProcessedPids` already filters terminal-op pids (BR_WRITE / MANUAL_SKIP / MANUAL_ACCEPT). Re-running the manual CLI silently skips already-handled pids — operator only sees fresh items. |
</threat_model>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator re-runs audit + fix orchestrator on current BR catalog</name>
  <what-built>
    All preceding plans (17-01, 17-02, 17-03, 17-04, 17-06) have shipped. The Drive timeout is in place, per-color canonical works, Model* has been rebuilt, and the prefix dispatcher routes Bella/Gildan/adidas through S&S. Time to re-run the full audit + fix orchestrator end-to-end against the LIVE BR catalog and produce a fresh manual queue.
  </what-built>
  <how-to-verify>
    1. **Pre-flight check** — verify the prerequisite commits:
       ```
       git log --oneline -20 | grep -E "17-0[12346]"
       ```
       Expect to see commits for 17-01 (Drive timeout), 17-02 (per-color canonical), 17-03 (model rebuild — both Task 1 and Task 3 SUMMARYs), 17-04 (prefix routing), 17-06 (B-2 dry-run gate). If any are missing, STOP — re-run /gsd-execute-phase on the missing plan first.

    2. **Run the full audit** (Phase 16's existing CLI):
       ```
       NODE_OPTIONS=--use-system-ca npx tsx scripts/audit-image-pollution.ts --all
       ```
       Expected: completes in BOUNDED wall-clock time (the 2026-05-13 hang at pid 1376842 is impossible after 17-01). Produces `tmp/image-pollution-audit-{today}.tsv`.

       If the run hangs again, that's a NEW bug — 17-01 is supposed to prevent this. Stop, file an issue, and replan.

    3. **Run the fix orchestrator** (Phase 16's existing CLI; 17-02 changes are now in place):
       ```
       NODE_OPTIONS=--use-system-ca npx tsx scripts/fix-image-pollution.ts \
         --audit-file tmp/image-pollution-audit-{today}.tsv \
         --max-cost 5
       ```
       Expected:
       - Tier 1 fixes ≥ 30 (researcher's disciplined estimate after 17-02 + 17-04: 50-100 fixes).
       - Tier 2 fixes ≤ 20 (mostly BackImage / DirectSideImage shape-drift).
       - Manual queue size ≤ 20 (R6 hard cap). If > 20: status `BLOCKED-QUEUE-OVERFLOW` → script exits 2 → STOP and replan. (This shouldn't happen given the prior plans, but the R6 gate is the safety net.)

    4. **Run the Model rebuild** (Phase 17 17-03 tool; already executed as part of Task 3 in that plan, but if not yet run, do it now):
       ```
       NODE_OPTIONS=--use-system-ca npx tsx scripts/fix-model-images.ts \
         --audit-file tmp/image-pollution-audit-{today}.tsv \
         --skip-sample-gate \
         --max-cost 10 \
         --re-audit
       ```
       Expected: `model_fail_after_count < 50` per the 17-03 acceptance gate.

    5. **Inspect the fresh manual queue**:
       ```
       wc -l tmp/image-pollution-manual-queue-{today}.tsv
       head -5 tmp/image-pollution-manual-queue-{today}.tsv
       ```
       Expected: ≤ 21 lines (header + ≤ 20 data rows). Each row is a pid the orchestrator couldn't fix automatically.

    6. **Categorize the manual queue** to scope the triage:
       ```
       awk -F'\t' 'NR>1 {print $2}' tmp/image-pollution-manual-queue-{today}.tsv | sort | uniq -c
       ```
       Expected distribution per RESEARCH §"Open Question 7":
       - `shared_url`: cross-brand cluster pids (Adidas-hoodie cluster, etc.) — operator picks replacement URLs.
       - `content_mismatch`: pids where supplier returned wrong product — operator picks correct URL from supplier or generates manually.
       - `model_pollution`: residue from 17-03 (verifier rejected the AI model and S&S had no canonical model image).
       - `shape_drift`: post-Tier-2-AI-fail shape-drift; operator deletes the cell (blank → next push regenerates).

    7. **Confirm scope is workable**:
       - If manual queue size > 20: STOP. The R6 cap was exceeded — replan (likely 17-04 or 17-03 underdelivered). Do NOT proceed to Task 2.
       - If manual queue size 11-20: continue to Task 2. Expect ~30-60 min of operator time.
       - If manual queue size ≤ 10: proceed to Task 2 — quick walkthrough.
       - If manual queue size 0: type `phase-closed` — Phase 17 closes immediately without Task 2.
  </how-to-verify>
  <resume-signal>
    Type the manual queue size (e.g., `manual-queue-size=8`) to proceed to Task 2.
    Or type `phase-closed` if the manual queue is empty (R10 satisfied via OR-path).
    Or type `replan` with the reason (e.g., `replan reason=manual-queue-overflow-25-pids`) to abort and replan.
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator walks every remaining manual-queue pid via the interactive CLI</name>
  <what-built>
    Task 1 produced a fresh `tmp/image-pollution-manual-queue-{date}.tsv` with ≤ 20 pids (operator confirmed in Task 1's resume signal). Phase 16's manual CLI `scripts/fix-image-pollution-manual.ts` already handles the interactive flow. Operator just RUNS it.
  </what-built>
  <how-to-verify>
    1. **Run the manual CLI**:
       ```
       NODE_OPTIONS=--use-system-ca npx tsx scripts/fix-image-pollution-manual.ts
       ```
       The CLI auto-detects `tmp/image-pollution-manual-queue-{today}.tsv` (per Phase 16 Plan 04).

    2. **Per-pid menu**: for each pid the CLI displays the polluted columns, current Drive URLs, suggested action, and prompts `[r/s/a/d/v/q]`. Operator chooses:
       - **`[r]eplace`** — when operator can find a correct URL (supplier website, store, manual fileId). Verifier-after-fix gates the commit. On verifier fail, secondary prompt allows `[f]orce` (literal 'FORCE') only when operator is confident the verifier is wrong.
       - **`[s]kip`** — when operator wants to come back later. Trail logs MANUAL_SKIP. The pid stays in the queue for the next run.
       - **`[a]ccept`** — when the current image is acceptable despite the audit flag (false positive). Trail logs MANUAL_ACCEPT. Pid is removed from future audits (until the next re-audit re-detects).
       - **`[d]elete`** — when no good replacement exists and a blank cell is better than a polluted one. Literal 'DELETE' confirmation required. Trail logs DRIVE_DELETE + BR_WRITE (empty cell). Next push will regenerate from supplier.
       - **`[v]iew`** — opens current and supplier-canonical images side-by-side in default viewer. Use before deciding `[r]` / `[d]`.
       - **`[q]uit`** — exits cleanly; progress fsync'd per-row. Re-run picks up where you left off.

    3. **Pid-level decision guide** (per RESEARCH §"Open Question 7"):
       - **Adidas-hoodie shared-URL cluster** (CE520L, NE220, 8882, 5200, 3911, 6405, 18000B, 4651, 4610 if any survive 17-04): for each, the CORRECT fix is `[r]eplace` with the per-pid S&S URL the orchestrator should have found. If the orchestrator missed it, manually search S&S Activewear for the styleName + color and paste the URL. Verifier-after-fix will confirm match.
       - **Cap pids with hoodie models** (9302, A702, 1010, 6110, 168 — if any survive 17-03's AI rebuild): for each, the model image is structurally wrong (showing a hoodie on a cap pid). Options: `[d]elete` to clear the cell (push regenerates from S&S model if available); or `[r]eplace` with the correct S&S `colorOnModel*` URL.
       - **Wrong-class garment images** (e.g., "tote bag" model on a garment pid — 5+ pids): `[d]elete`; the next push pipeline will regenerate.
       - **New Era NE220 / NE7xx**: per RESEARCH Finding 3, no automated supplier. Operator either finds the URL on `neweracap.com` and pastes it, or `[d]elete`s.

    4. **Final state check**: after walking every pid:
       ```
       npx tsx scripts/fix-image-pollution-manual.ts --re-audit
       ```
       This re-runs the audit on the original 213-pid corpus and prints the R10 status:
       - `RE-AUDIT: 0 pids still polluted` → R10 satisfied via re-audit branch → Phase 17 closes.
       - `RE-AUDIT: N pids still polluted (or N/M = X% BR_WRITE coverage)` → check the BR_WRITE coverage path. If every original-polluted pid has a BR_WRITE row in the trail, R10 is satisfied via the OR-path (operator made a decision on every pid).

       Acceptance: residue ≤ 5 truly unresolvable pids (truly unsupported brands with no replacement URL findable in 30s of operator effort). Document these in 17-05-SUMMARY.md as "deferred / requires escalation".

    5. **Trail audit**: the manual CLI writes MANUAL_SKIP / MANUAL_ACCEPT / BR_WRITE rows for every pid touched. Verify:
       ```
       awk -F'\t' '$3 ~ /MANUAL_(SKIP|ACCEPT)/ || $3 == "BR_WRITE" && $7 == "3"' \
         tmp/image-pollution-fix-trail-{date}.tsv | wc -l
       ```
       Expected: at least (manual queue size from Task 1) lines.
  </how-to-verify>
  <resume-signal>
    Type `phase-closed manual-queue=0 remaining-residue=N` once the manual CLI reports an empty queue.
    Or type `phase-closed manual-queue=N remaining-residue=M deferred=[pid1,pid2,...]` when ≤ 5 residue pids are documented as deferred.
    Or type `pause` if operator needs to break (work is fsync'd per row; safe to resume later via `/gsd-resume-work`).
  </resume-signal>
</task>

</tasks>

<verification>
- `tmp/image-pollution-audit-{date}.tsv` exists from Task 1's audit re-run.
- `tmp/image-pollution-manual-queue-{date}.tsv` exists with ≤ 20 rows (R6 cap).
- After Task 2: trail TSV has MANUAL_SKIP / MANUAL_ACCEPT / BR_WRITE rows for every queue pid.
- `--re-audit` confirms 0 polluted OR every original-polluted pid has a BR_WRITE row.
- 17-05-SUMMARY.md documents per-pid decisions for the residue.
</verification>

<success_criteria>
- Manual queue size ≤ 20 (R6 honored).
- Operator handles every manual-queue pid via the existing CLI.
- Residue ≤ 5 truly-unresolvable pids (documented as deferred in SUMMARY).
- Phase 17 closes per Phase 16's R10 OR-path.
- No new engineering — 100% reuse of Phase 16's manual CLI plus 17-06's `--dry-run` Drive-trash gate fix.
</success_criteria>

<output>
After completion, create `.planning/phases/17-catalog-image-pollution-fix/17-05-SUMMARY.md` recording:
- Manual queue size from Task 1 (real number vs ≤ 20 target).
- Per-pid decisions table:
  | pid | original_polluted_columns | operator_decision | reason |
- List of ≤ 5 deferred residue pids with rationale (e.g., "NE220 - no S&S canonical, no neweracap.com URL found, customer hasn't sold this in 6 months → defer").
- Real-yield totals: Tier 1 fixes (count) vs Tier 2 (count) vs manual `[r]eplace` (count) vs `[a]ccept`/`[s]kip` (count) vs `[d]elete` (count).
- Final R10 status: "satisfied via re-audit=0" OR "satisfied via BR_WRITE coverage for all original-polluted pids" OR "deferred residue listed above with operator-confirmed acceptance".
- Total operator wall-clock time spent on Task 2.
- Total OpenAI cost for the whole phase (audit + fix orchestrator + Model rebuild + manual CLI's verifier-after-fix calls).
</output>
