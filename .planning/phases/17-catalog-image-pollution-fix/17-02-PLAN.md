---
phase: 17-catalog-image-pollution-fix
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/lib/supplier-canonical.ts
  - scripts/audit-image-pollution.ts
  - scripts/fix-image-pollution.ts
  - scripts/fix-image-pollution-manual.ts
  - tests/lib/supplier-canonical.test.ts
autonomous: true
requirements: [R17-02]

must_haves:
  truths:
    - "`resolveSupplierCanonical(pid, colorName)` returns the per-color front URL for an S* pid (S&S branch) — confirmed by mocked /products/?style= response containing multiple colors."
    - "`resolveSupplierCanonical(pid, colorName)` with NO colorName argument behaves identically to the pre-17-02 Phase 16 signature (first front-image-bearing variant). Backward-compatible."
    - "CSW (L*) branch uses best-effort filename-substring match on `colorName` (lowercase, hyphens for spaces); falls back to first image when no filename match found (preserves Phase 16 behavior — no regression)."
    - "When colorName is provided but no S&S variant matches it, the function falls back to the first front-image-bearing variant AND returns a `wasFallback: true` flag (extended `CanonicalResult` interface) so callers can decide strict vs loose."
    - "All 3 call sites — `scripts/audit-image-pollution.ts`, `scripts/fix-image-pollution.ts`, `scripts/fix-image-pollution-manual.ts` — pass the BR row's colorName cell into `resolveSupplierCanonical` (or its DI-seam analog `supplierCanonicalFn`)."
    - "Re-running the 2026-05-14 Tier 1-only fix on the same audit TSV produces ≥ 1 BR_WRITE row where the previous run logged a verifier-fail with reason 'same hoodie style, different color' or similar (i.e., per-color match unlocks at least one of the 11 previously-rejected pids)."
  artifacts:
    - path: "src/lib/supplier-canonical.ts"
      provides: "Per-color supplier-canonical resolver (extended Phase 16 dispatcher)"
      exports: ["resolveSupplierCanonical", "CanonicalResult"]
      contains: "colorName?: string"
    - path: "tests/lib/supplier-canonical.test.ts"
      provides: "Extended test coverage for per-color path (S&S + CSW), backward-compat path, fallback flag"
      contains: "per-color"
  key_links:
    - from: "src/lib/supplier-canonical.ts"
      to: "S&S Canada /products/?style=N endpoint"
      via: "client-side filter on response[].colorName (case-insensitive trim equality)"
      pattern: "colorName\\.toUpperCase\\(\\)\\.trim\\(\\)"
    - from: "scripts/audit-image-pollution.ts"
      to: "src/lib/supplier-canonical.ts"
      via: "supplierCanonicalFn(pid, colorName) — passes BR row colorName"
      pattern: "supplierCanonicalFn\\([^,]+, .*colorName"
    - from: "scripts/fix-image-pollution.ts"
      to: "src/lib/supplier-canonical.ts"
      via: "Tier 1 supplier fetch now per-color"
      pattern: "supplierCanonicalFn\\([^,]+, .*colorName"
    - from: "scripts/fix-image-pollution-manual.ts"
      to: "src/lib/supplier-canonical.ts"
      via: "manual CLI reference-front lookup is per-color"
      pattern: "supplierCanonicalFn\\([^,]+, .*colorName"
---

<objective>
Modify `src/lib/supplier-canonical.ts` to accept an optional `colorName` parameter and filter the S&S `/products/?style=` response to that specific color client-side (verified in RESEARCH Finding 4 — S&S returns all variants in one call, supports no server-side colorName filter, so no extra API cost). Update all 3 call sites in `scripts/audit-image-pollution.ts`, `scripts/fix-image-pollution.ts`, `scripts/fix-image-pollution-manual.ts` to pass the BR row's `colorName` cell into the resolver. Extend CSW branch with best-effort filename-match for color (per RESEARCH Finding 4 paragraph 2). Backward-compatible: omitting `colorName` preserves the Phase 16 "first front-image-bearing variant" behavior.

Purpose: eliminate the 11+ "same product, wrong color" verifier rejections observed in the 2026-05-14 Tier 1 fix run (rejection reasons: "same hoodie style, different color" ×7, "same softshell jacket style, different color" ×2, "candidate is red, reference is black" ×2). These pids have CORRECT supplier canonicals for the right color — they were rejected because the resolver returned the FIRST color's URL regardless of the BR row's actual color.

Output:
- Modified `src/lib/supplier-canonical.ts` (signature extended; `CanonicalResult` extended with `wasFallback` field).
- Modified 3 call-site scripts (one new arg threaded through DI seam).
- Extended `tests/lib/supplier-canonical.test.ts` with new per-color test groups.
- Re-running Tier 1 against the existing 2026-05-14 audit TSV produces verifier-passes on pids that previously failed for color reasons.
</objective>

<execution_context>
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-catalog-image-pollution-fix/17-CONTEXT.md
@.planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md
@.planning/research/phase17-prep/PRODUCTION-AUDIT-FINDINGS.md
@.planning/phases/17-catalog-image-pollution-fix/17-01-PLAN.md
@src/lib/supplier-canonical.ts
@scripts/audit-image-pollution.ts
@scripts/fix-image-pollution.ts
@scripts/fix-image-pollution-manual.ts
@scripts/fetch-ss-images-fixed.ts
@scripts/scrape-csw-product.ts
@tests/lib/supplier-canonical.test.ts

<interfaces>
<!-- Current Phase 16 signature (src/lib/supplier-canonical.ts) — extend, do not break. -->
export interface CanonicalResult {
  url: string;
  source: 'ss' | 'csw';
  styleId?: number | string;
  // NEW for 17-02:
  wasFallback?: boolean;  // true iff colorName was provided but no exact match found; result is first-variant fallback
}

export async function resolveSupplierCanonical(
  pid: string,
  colorName?: string,    // NEW — optional 2nd positional arg
): Promise<CanonicalResult | null>;

<!-- S&S /products/?style= response shape (verified via scripts/fetch-ss-images-fixed.ts:118-124).
     One array entry per (color, size) combination. The fields we need: -->
//   colorName: string                    (e.g. "BLACK", "ROYAL", "HEATHER GOLD MELANGE")
//   colorFrontImage: string              (e.g. "Images/Style/3001_fm.jpg")
//   sizeName: string                     (size; we don't filter by size — front is per-color, not per-size)

<!-- BR row colorName column header (verified in scripts/fix-image-pollution.ts brIndex.headerMap['colorName']).
     BR has a 'colorName' column populated for every row. -->

<!-- CSW shape (existing — already in supplier-canonical.ts). Note: CSW returns
     a single `images` array; filenames sometimes include color tokens. Per
     RESEARCH Pattern B and Open Question 3: substring match is best-effort,
     fallback to first image preserves no-regression behavior. -->
const product: { images: Array<{ src: string }> } = await fetchCSWProduct(handle);
// Images.src has the filename embedded — extract with `src.split('/').pop()` and
// do case-insensitive substring match on a hyphenated color slug.

<!-- DI seam in audit + fix scripts (existing — already passes supplierCanonicalFn through deps). -->
//   audit-image-pollution.ts:   deps.supplierCanonicalFn(pid)    ← becomes (pid, colorName)
//   fix-image-pollution.ts:     deps.supplierCanonicalFn(pid)    ← becomes (pid, colorName)
//   fix-image-pollution-manual.ts: deps.supplierCanonicalFn(pid) ← becomes (pid, colorName)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BR row colorName cell → resolver | Operator-controlled (Google Sheet); could be malformed or contain non-ASCII. |
| S&S response → variant matching | Vendor-controlled; colorName casing may shift; trim required. |
| CSW image filename → color match | Vendor-controlled URL fragments; substring match is best-effort. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | T (Tampering) — colorName injection routes to wrong product | S&S branch client-side filter | mitigate | Comparison is `String(p.colorName ?? '').toUpperCase().trim() === target` — exact normalized equality, not substring. Cross-product collision (e.g., BR colorName "BLACK" matching S&S colorName "BLACK FOREST") is impossible. Verifier-after-fix downstream (Phase 16 Tier 1) catches any color-mismatch survival. |
| T-17-06 | T — colorName containing tabs/newlines corrupts trail TSV downstream | callers pass colorName into trail rows | mitigate | Trail writer (`src/lib/image-pollution-trail.ts:sanitize`) already collapses `[\t\n\r]+` for the 4 unsafe fields. This plan adds nothing new to that surface. |
| T-17-07 | I (Information Disclosure) — CSW filename match reveals product internals in log | best-effort match logs the chosen filename | accept | Logged filename is already a public URL fragment. No new disclosure. |
| T-17-08 | D — extra S&S /products/?style= call per audit row when colorName specified | Verified NO: S&S already returns all variants in one call (Finding 4) — filtering is client-side. Same call count as Phase 16. | accept | No new API cost. Caller can re-validate by counting `fetch(SS_BASE` invocations across an audit run pre vs post 17-02 — should be identical. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend resolveSupplierCanonical with optional colorName parameter (S&S + CSW branches)</name>
  <files>
    src/lib/supplier-canonical.ts,
    tests/lib/supplier-canonical.test.ts
  </files>
  <read_first>
    - src/lib/supplier-canonical.ts (entire file — 291 lines; the S&S branch is around lines 200-260, CSW around lines 260-290; KNOWN_SUPPLIER_PREFIXES at 55-81 — leave this alone for 17-02; 17-04 touches it)
    - tests/lib/supplier-canonical.test.ts (existing test file — extend, do not rewrite)
    - scripts/fetch-ss-images-fixed.ts lines 100-180 (verified shape of /products/?style= response — multi-color array)
    - scripts/scrape-csw-product.ts lines 50-170 (CSW image array shape — filenames extracted from `images[].src` via `.split('/').pop()`)
    - .planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md "Pattern 2: Per-color supplier-canonical signature" lines 380-451 (S&S example) and "Finding 4: S&S `/products/?style=N` returns ALL colors in one call" lines 162-185 (the CSW caveat is there)
    - .planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md "Pattern B: Per-color supplier-canonical (17-02 + 17-04)" lines 769-854 (the merged code excerpt)
    - .planning/research/phase17-prep/PRODUCTION-AUDIT-FINDINGS.md "11 such rejections this run" (line 49) — the 11 verifier-fail reasons that document the bug
  </read_first>
  <behavior>
    Unit tests — mock `fetch` (or whatever the S&S/CSW path uses) via `vi.spyOn(globalThis, 'fetch')` to return synthetic responses; assert downstream behavior on returned `CanonicalResult`.

    S&S branch:
    - Test 1 (per-color happy path): S&S `/products/?style=N` mock returns `[{colorName:'BLACK', colorFrontImage:'Images/3001_BLACK_fm.jpg'}, {colorName:'WHITE', colorFrontImage:'Images/3001_WHITE_fm.jpg'}, {colorName:'ROYAL', colorFrontImage:'Images/3001_ROYAL_fm.jpg'}]`. Call `resolveSupplierCanonical('S05610', 'ROYAL')`. Assert: returned url contains `'3001_ROYAL'`; `wasFallback === undefined` or `false`; source === 'ss'.
    - Test 2 (case-insensitive + whitespace-tolerant colorName match): same mock; call `resolveSupplierCanonical('S05610', 'royal')` and `resolveSupplierCanonical('S05610', '  Royal  ')`. Both return the ROYAL variant. Assert URL contains `'3001_ROYAL'`.
    - Test 3 (colorName not found — fallback to first): same mock; call `resolveSupplierCanonical('S05610', 'NONEXISTENT-COLOR')`. Assert: returned URL contains `'3001_BLACK'` (the first variant), `wasFallback === true`.
    - Test 4 (no colorName — backward compatibility): same mock; call `resolveSupplierCanonical('S05610')` (no 2nd arg). Assert: returned URL contains `'3001_BLACK'` (first variant); `wasFallback === undefined` or `false` (NOT a fallback — the operator never asked for a specific color).
    - Test 5 (S&S returns variants with empty colorFrontImage — skip): mock `[{colorName:'BLACK', colorFrontImage:''}, {colorName:'ROYAL', colorFrontImage:'Images/3001_ROYAL_fm.jpg'}]`. Call with `colorName='BLACK'`. Per the per-color match logic, BLACK matches colorName but its colorFrontImage is empty — the matcher SHOULD fall through to the first variant with a non-empty front image (ROYAL). Assert: URL contains `'3001_ROYAL'`, `wasFallback === true`.
    - Test 6 (empty /products/ response): mock `[]`. Call `resolveSupplierCanonical('S05610', 'BLACK')`. Assert: returns `null`.
    - Test 7 (S&S API call count unchanged — Finding 4 invariant): track `fetch` invocations across Tests 1-5. Each individual test makes exactly 2 fetches (one to `/styles/?search=` for styleID resolve, one to `/products/?style=` for variants). The colorName argument does NOT cause an extra fetch. Assert via `fetchSpy.mock.calls.length === 2` per test.

    CSW branch:
    - Test 8 (CSW per-color filename match): mock CSW response `{ images: [{src: 'https://cdn.shopify.com/.../L00660-Black-front.jpg'}, {src: '.../L00660-Royal-front.jpg'}] }`. Call `resolveSupplierCanonical('L00660', 'Royal')`. Assert: returned URL contains `'Royal-front.jpg'`; source === 'csw'; `wasFallback === undefined` or `false`.
    - Test 9 (CSW multi-word color — hyphenated slug match): mock `{ images: [{src:'.../L00660-heather-gold-melange-front.jpg'}, {src:'.../L00660-black-front.jpg'}] }`. Call with `colorName='Heather Gold Melange'`. The slug `'heather-gold-melange'` must match the first image. Assert: URL contains `'heather-gold-melange-front'`.
    - Test 10 (CSW filename match fails — fallback): same mock as Test 8; call with `colorName='NONEXISTENT'`. Assert: URL is the FIRST image (Phase 16 fallback); `wasFallback === true`.
    - Test 11 (CSW no colorName — backward compat): same mock as Test 8; call without 2nd arg. Assert: URL is first image; `wasFallback === undefined` or `false`.

    Public surface:
    - Test 12 (CanonicalResult interface shape): import `CanonicalResult` from the module; assert the exported type accepts `{ url, source, styleId, wasFallback }` shape. Compile-time test — write a const of that shape and assign-via-`satisfies`-cast.
  </behavior>
  <action>
    Modify `src/lib/supplier-canonical.ts`:

    1. **Extend `CanonicalResult` interface** (lines ~88-95):

       ```
       export interface CanonicalResult {
         url: string;
         source: 'ss' | 'csw';
         styleId?: number | string;
         /** True iff caller passed colorName but no exact match was found and the result fell back to the first front-image-bearing variant. Phase 17 17-02. */
         wasFallback?: boolean;
       }
       ```

       JSDoc the new field per Phase 16's style (1-line then a longer note if needed).

    2. **Extend `resolveSupplierCanonical` signature** (current line ~200 region):

       Change `export async function resolveSupplierCanonical(pid: string): Promise<CanonicalResult | null>` to `export async function resolveSupplierCanonical(pid: string, colorName?: string): Promise<CanonicalResult | null>`.

       Update the JSDoc above the function to:
       ```
       /**
        * Resolve the canonical (supplier-source-of-truth) front image URL for a pid.
        *
        * Phase 17 17-02: when `colorName` is provided, the S&S branch filters the
        * /products/?style= response to that specific color (case-insensitive,
        * whitespace-trimmed equality). The CSW branch does best-effort
        * filename-substring match (CSW images don't carry first-class color
        * metadata — see scripts/scrape-csw-product.ts). If `colorName` is
        * provided but no match is found, the result falls back to the first
        * front-image-bearing variant AND sets `wasFallback: true` so callers can
        * distinguish loose vs strict matches.
        *
        * Phase 16 behavior (no `colorName` arg) is preserved: returns the first
        * front-image-bearing variant, `wasFallback` unset.
        *
        * @param pid Style/product identifier (BR productId).
        * @param colorName Optional BR row colorName cell value. Case-insensitive,
        *                  whitespace-trimmed match on S&S; substring match on CSW.
        */
       ```

    3. **S&S branch — implement per-color filter** (around lines ~210-260):

       After the existing `/products/?style=` fetch returns the variants array (`products`), apply this match logic IN PLACE of the current "find first with non-empty colorFrontImage":

       ```
       // Phase 17 17-02: per-color filter, with fallback to first.
       let match: typeof products[0] | undefined;
       let wasFallback = false;

       if (colorName) {
         const target = colorName.toUpperCase().trim();
         match = products.find(
           (p) =>
             p.colorFrontImage &&
             String(p.colorName ?? '').toUpperCase().trim() === target,
         );
         if (!match) {
           wasFallback = true;
         }
       }
       // Fall back to first front-image-bearing variant (preserves Phase 16 default).
       if (!match) {
         match = products.find((p) => p.colorFrontImage);
       }
       if (!match || !match.colorFrontImage) return null;

       return {
         url: makeSSLargeUrl(match.colorFrontImage),
         source: 'ss',
         styleId: ssId,
         ...(wasFallback ? { wasFallback: true } : {}),
       };
       ```

       Critical: do not break the existing log line that says `[supplier-canonical] S&S resolved <pid>` — adjust it to include the colorName (when provided) and the wasFallback flag for operator observability:

       ```
       logger.info(
         `[supplier-canonical] S&S resolved ${pid}${colorName ? ` colorName=${colorName}` : ''}${wasFallback ? ' (fallback)' : ''} -> ${match.colorFrontImage}`,
       );
       ```

    4. **CSW branch — implement filename-substring match** (around lines ~260-290):

       The current CSW branch fetches `product.images[0]` (or the first image with a `.src`). Replace with:

       ```
       // Phase 17 17-02: CSW best-effort filename match.
       let chosen: { src: string } | undefined;
       let wasFallback = false;

       if (colorName) {
         const colorSlug = colorName.toLowerCase().trim().replace(/\s+/g, '-');
         chosen = product.images.find((img) => {
           const filename = String(img.src.split('/').pop() ?? '').toLowerCase();
           return filename.includes(colorSlug);
         });
         if (!chosen) {
           wasFallback = true;
         }
       }
       if (!chosen) chosen = product.images[0];
       if (!chosen?.src) return null;

       return {
         url: chosen.src,
         source: 'csw',
         styleId: handle,
         ...(wasFallback ? { wasFallback: true } : {}),
       };
       ```

       Log line update analogous to S&S — include colorName + (fallback) marker.

    5. **Do NOT modify** the H08 early-return at the top of the function, the `KNOWN_SUPPLIER_PREFIXES` dispatcher (Plan 17-04 handles that), the throttle helpers, the auth helpers, or the `resolveSSStyleId` function. Stay minimal.

    6. **Extend `tests/lib/supplier-canonical.test.ts`** (do not rewrite — extend with the 12 new test cases above). Pattern: import the function under test, mock the global `fetch` with `vi.spyOn(globalThis, 'fetch')` and have it return `new Response(JSON.stringify(payload), { status: 200, headers: { 'content-type': 'application/json' } })` per request URL. Group new tests under a `describe('17-02 per-color resolver', () => { ... })` block to keep existing Phase 16 tests untouched.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/supplier-canonical.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "resolveSupplierCanonical\\(.*pid: string.*colorName\\?: string" src/lib/supplier-canonical.ts` matches exactly one line (signature extension).
    - `grep -nE "wasFallback\\?: boolean" src/lib/supplier-canonical.ts` matches exactly one line (CanonicalResult extension).
    - `grep -nE "colorName\\.toUpperCase\\(\\)\\.trim\\(\\)" src/lib/supplier-canonical.ts` matches at least one line (S&S match logic).
    - `grep -nE "colorName\\.toLowerCase\\(\\)\\.trim\\(\\)\\.replace" src/lib/supplier-canonical.ts` matches at least one line (CSW slug logic).
    - `grep -nE "wasFallback = true" src/lib/supplier-canonical.ts` matches at least 2 lines (one in S&S branch, one in CSW branch).
    - `grep -nE "describe\\(.17-02 per-color resolver" tests/lib/supplier-canonical.test.ts` matches one line.
    - `npx vitest run tests/lib/supplier-canonical.test.ts` reports ≥ 12 new test cases passing (in addition to Phase 16's existing count).
    - `npx vitest run tests/lib/supplier-canonical.test.ts` shows no Phase 16 test regression.
  </acceptance_criteria>
  <done>
    `resolveSupplierCanonical` accepts the new optional `colorName` arg; S&S branch filters per-color with case-insensitive trim-equality; CSW branch best-effort filename-substring match; both branches fall back to first variant with `wasFallback: true` when colorName provided but no match found; backward-compatible when colorName omitted. 12 new tests passing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread colorName through all 3 call sites (audit, fix orchestrator, manual CLI)</name>
  <files>
    scripts/audit-image-pollution.ts,
    scripts/fix-image-pollution.ts,
    scripts/fix-image-pollution-manual.ts,
    tests/scripts/audit-image-pollution.test.ts,
    tests/scripts/fix-image-pollution.test.ts,
    tests/scripts/fix-image-pollution-manual.test.ts
  </files>
  <read_first>
    - scripts/audit-image-pollution.ts (search for `supplierCanonicalFn(` — typically 1-2 call sites in Pass 2 verifier-canonical resolution)
    - scripts/fix-image-pollution.ts (search for `supplierCanonicalFn(` — typically in Tier 1 fix at the top of the per-pid loop)
    - scripts/fix-image-pollution-manual.ts (search for `supplierCanonicalFn(` — used in `handleReplace` for reference-front lookup)
    - For each script: the line that builds `brIndex.headerMap['colorName']` and reads the cell — copy the existing column-read pattern (`String(brEntry.row[brIndex.headerMap['colorName'] ?? -1] ?? '')`). Phase 16 scripts already extract colorName from BR rows for other purposes; just thread it into the supplierCanonicalFn call.
    - tests/scripts/audit-image-pollution.test.ts, tests/scripts/fix-image-pollution.test.ts, tests/scripts/fix-image-pollution-manual.test.ts — each has at least one test that asserts the call shape of `supplierCanonicalFn` (`.toHaveBeenCalledWith(pid)`). Update those assertions to `.toHaveBeenCalledWith(pid, colorName)`.
  </read_first>
  <behavior>
    For each of the 3 scripts, add one new test asserting per-color propagation:

    audit-image-pollution.ts:
    - Test A1 (audit passes colorName): synthetic BR with pid 'S05610' and colorName 'ROYAL'. Audit runs Pass 2 verifier-canonical resolution. Assert: `deps.supplierCanonicalFn.mock.calls[0]` is `['S05610', 'ROYAL']` (length 2, second arg is the BR row's colorName).

    fix-image-pollution.ts:
    - Test F1 (Tier 1 passes colorName): synthetic audit row for pid 'S05610' content_mismatch on FrontImage; BR has colorName 'ROYAL'. Tier 1 fix runs. Assert: `deps.supplierCanonicalFn.mock.calls[0]` is `['S05610', 'ROYAL']`.

    fix-image-pollution-manual.ts:
    - Test M1 (handleReplace passes colorName): synthetic manual queue row for pid 'L00660' with BR colorName 'Heather Gold Melange'. Operator enters URL → handleReplace runs verifier-after-fix. The reference-front resolution calls `supplierCanonicalFn('L00660', 'Heather Gold Melange')`. Assert that exact call shape.

    Existing test updates (NOT new tests — adjust assertions in tests that already mock supplierCanonicalFn):
    - Any `.toHaveBeenCalledWith(pid)` for `supplierCanonicalFn` becomes `.toHaveBeenCalledWith(pid, expect.any(String))` (or `expect.any(String)` if the test doesn't care about the specific color) so existing tests don't break.
  </behavior>
  <action>
    For each of `scripts/audit-image-pollution.ts`, `scripts/fix-image-pollution.ts`, `scripts/fix-image-pollution-manual.ts`:

    1. Locate the `brIndex.headerMap['colorName']` extraction (it already exists in all 3 scripts for other column reads — Phase 16 reads colorName for trail logging in some paths). If not already extracted in the call-site function, extract it once:

       ```
       const colorName = String(brEntry.row[brIndex.headerMap['colorName'] ?? -1] ?? '');
       ```

    2. Change every call to `deps.supplierCanonicalFn(pid)` to `deps.supplierCanonicalFn(pid, colorName)`. Be exhaustive — `grep -nE "supplierCanonicalFn\\(" scripts/{audit,fix,fix-…}.ts` MUST return zero matches without a second arg after Task 2 lands.

       - In `audit-image-pollution.ts`: typically one call inside Pass 2 (verifier-canonical) and possibly one inside Pass 3 (shape-drift uses Phase 15's verifier — that path may NOT call supplierCanonicalFn; verify by reading the existing code). Only update the sites that exist.
       - In `fix-image-pollution.ts`: Tier 1 fix calls supplierCanonicalFn at the top of the per-pid block. There may also be a call in the source-of-truth resolution for back/side fixes (re-fetching the canonical front for the verifier-after-fix reference).
       - In `fix-image-pollution-manual.ts`: `handleReplace` calls supplierCanonicalFn to fetch the reference image for verifier-after-fix.

    3. **Do NOT modify** the trail-row writers, the BR-write flow, the Drive flow, the choice menu, the dry-run gates, or any unrelated logic. Stay surgical — this task is JUST threading one extra string argument.

    4. **Update tests** in `tests/scripts/{audit,fix,fix-image-pollution-manual}*.test.ts`:
       - Find every `supplierCanonicalFn` mock-call assertion and update to expect 2 args (the second being the colorName or `expect.any(String)`).
       - Add the new tests A1/F1/M1 described in `<behavior>` above. Each in a `describe('17-02 colorName propagation', () => { ... })` block within the existing test file.

    5. **Smoke-test verification** (NOT automated — operator runbook addition for the SUMMARY): the planner intends for an operator to re-run the 2026-05-14 Tier 1 fix against the existing audit TSV. The pids that previously failed with `notes='same hoodie style, different color'` etc. should now produce verifier-pass BR_WRITE rows. This is a manual gate after 17-01 + 17-02 + 17-04 ship. Document the runbook step in the SUMMARY (`npx tsx scripts/fix-image-pollution.ts --tier1-only --audit-file tmp/image-pollution-audit-2026-05-13.tsv`).
  </action>
  <verify>
    <automated>npx vitest run tests/scripts/audit-image-pollution.test.ts tests/scripts/fix-image-pollution.test.ts tests/scripts/fix-image-pollution-manual.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "supplierCanonicalFn\\(([^,]+,[^,)]+)\\)" scripts/audit-image-pollution.ts scripts/fix-image-pollution.ts scripts/fix-image-pollution-manual.ts` matches at least 3 lines (one per script — every call site has 2 args now).
    - `grep -nE "supplierCanonicalFn\\([^,)]+\\)$" scripts/audit-image-pollution.ts scripts/fix-image-pollution.ts scripts/fix-image-pollution-manual.ts` returns 0 (no single-arg calls remain). Note: the `$` anchors against trailing close-paren on same line; multi-line calls aren't a concern here per Phase 16 style.
    - `grep -cE "describe\\(.17-02 colorName propagation" tests/scripts/audit-image-pollution.test.ts tests/scripts/fix-image-pollution.test.ts tests/scripts/fix-image-pollution-manual.test.ts` returns 3 (one per test file).
    - `npx vitest run tests/scripts/audit-image-pollution.test.ts tests/scripts/fix-image-pollution.test.ts tests/scripts/fix-image-pollution-manual.test.ts` reports ALL tests passing (existing + 3 new).
    - `npx vitest run` (full suite) reports no regressions across Phase 14/15/16 tests.
  </acceptance_criteria>
  <done>
    All 3 call sites pass colorName (extracted from the BR row's colorName cell) as the 2nd arg to `supplierCanonicalFn`. Existing Phase 16 tests updated to expect 2 args. 3 new "17-02 colorName propagation" tests added (one per script). Full suite green.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` (full suite) is green.
- `grep` acceptance criteria above all pass.
- Manual operator smoke (in SUMMARY runbook): re-run `npx tsx scripts/fix-image-pollution.ts --tier1-only --audit-file tmp/image-pollution-audit-2026-05-13.tsv`. Expected outcome: at least 1 BR_WRITE row appears in the trail TSV where the 2026-05-14 run logged a VERIFIER_FAIL with reason matching `'same .+style, different color'` or `'candidate is .+, reference is .+'`. Real-yield TBD — researcher's disciplined estimate is 50-100 Tier 1 fixes total after 17-02 + 17-04 (DO NOT trust the 36-pid dry-run number from 2026-05-14; that was off by 36×).
</verification>

<success_criteria>
- `resolveSupplierCanonical` accepts and honors the optional `colorName` arg for both S&S and CSW.
- `CanonicalResult.wasFallback` surfaces when colorName was provided but unmatched.
- Backward compatibility: no-colorName call path identical to Phase 16.
- All 3 call sites in audit / fix / manual scripts pass the BR row's colorName.
- All Phase 16 tests still pass.
- New tests cover: per-color match, case-insensitive, whitespace tolerance, fallback flag, no-colorName backward compat, CSW filename match, CSW multi-word slug match, CSW fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/17-catalog-image-pollution-fix/17-02-SUMMARY.md` recording: (a) the new tests added per file with one-line descriptions, (b) the operator runbook step to re-run Tier 1 against the 2026-05-14 audit TSV, (c) actual Tier 1 yield vs the 50-100 estimate (real-yield TBD until operator runs), (d) any S&S colorName edge cases discovered (e.g., S&S uses 'HTHR ROYAL' but BR uses 'Heather Royal' — surface these as candidates for follow-up normalization).
</output>
