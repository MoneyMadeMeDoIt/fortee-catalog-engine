---
phase: 17-catalog-image-pollution-fix
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fix-image-pollution-manual.ts
  - tests/scripts/fix-image-pollution-manual.test.ts
autonomous: true
requirements: [R17-06]

must_haves:
  truths:
    - "When operator runs `npx tsx scripts/fix-image-pollution-manual.ts --dry-run` and chooses `[d]elete` (typing literal 'DELETE'), the Drive file is NOT trashed (currently it IS — B-2 bug). A `logger.info` line announces what WOULD have happened."
    - "When operator runs with `--dry-run` and chooses `[r]eplace` with a verifier-passing URL, the original Drive file (origFileId) is NOT trashed even when newFileId differs from origFileId. A `logger.info` line announces what WOULD have happened."
    - "Live runs (no `--dry-run` flag) behave identically to Phase 16 — Drive trashes still fire when origFileId !== newFileId per T-16-01 compare-before-trash."
    - "Trail TSV behavior in `--dry-run`: the DRIVE_DELETE trail row is NOT written when the actual trash call is skipped. Matches the existing Phase 16 pattern that BR_WRITE trail rows are also skipped on `--dry-run`."
  artifacts:
    - path: "scripts/fix-image-pollution-manual.ts"
      provides: "Manual CLI with closed --dry-run Drive-trash footgun (B-2 fix)"
      exports: ["runManualFix", "handleManualRow"]
    - path: "tests/scripts/fix-image-pollution-manual.test.ts"
      provides: "Tests for the new --dry-run Drive-trash gate at lines 429 and 712"
      contains: "B-2 dry-run blocks trash"
  key_links:
    - from: "scripts/fix-image-pollution-manual.ts"
      to: "deps.args.dryRun (existing field)"
      via: "Drive-trash call gated behind `!deps.args.dryRun` (matching the Sheets-write gate at lines 404 + 741)"
      pattern: "!deps\\.args\\.dryRun"
---

<objective>
Patch the `--dry-run` Drive-trash footgun in `scripts/fix-image-pollution-manual.ts` (B-2 in `PRODUCTION-AUDIT-FINDINGS.md`). Lines 429 and 712 call `deps.trashDriveFileFn(deps.driveClient, origFileId)` WITHOUT first checking `args.dryRun`. The Sheets-write calls at lines 404 and 741 ARE gated; the trash calls were missed. An operator running with `--dry-run` and typing literal 'DELETE' currently destroys Drive files irrecoverably — exactly the kind of destructive action `--dry-run` is supposed to prevent.

Purpose: close the footgun. Each gate is a 1-line `if (!deps.args.dryRun) { ... }` wrap. Add a logger.info in the else branch announcing what WOULD have happened so operators see the dry-run effect clearly. Add unit tests asserting the trash is NOT called in dry-run mode at both call sites.

Output:
- Patched `scripts/fix-image-pollution-manual.ts` (2 small gates added).
- 2 new unit tests in `tests/scripts/fix-image-pollution-manual.test.ts`.
</objective>

<execution_context>
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-catalog-image-pollution-fix/17-CONTEXT.md
@.planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md
@.planning/research/phase17-prep/PRODUCTION-AUDIT-FINDINGS.md
@scripts/fix-image-pollution-manual.ts
@tests/scripts/fix-image-pollution-manual.test.ts

<interfaces>
<!-- Existing dry-run gate pattern (Phase 16 — already correctly applied to Sheets writes). -->
//   scripts/fix-image-pollution-manual.ts line 404:
//     if (!deps.args.dryRun) {
//       await deps.writeUpdatesFn(...);  // GATED — correct
//     }
//   scripts/fix-image-pollution-manual.ts line 741:
//     if (!deps.args.dryRun) {
//       await deps.writeUpdatesFn(...);  // GATED — correct
//     }

<!-- B-2 missing gates (these MUST be wrapped). -->
//   scripts/fix-image-pollution-manual.ts line 429 (delete handler):
//     try {
//       await deps.trashDriveFileFn(deps.driveClient, origFileId);  // UNGATED — BUG
//     } catch (err) { ... }
//   scripts/fix-image-pollution-manual.ts line 712 (replace handler trash-after-upload):
//     try {
//       await deps.trashDriveFileFn(deps.driveClient, origFileId);  // UNGATED — BUG
//     } catch (err) { ... }

<!-- RESEARCH Pattern D excerpt (lines 911-955) shows the desired post-patch shape. -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator `--dry-run` intent → script behavior | Operator expects ZERO mutations. Currently violated for Drive-trash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-21 | T (Tampering — data destruction) | `--dry-run` Drive-trash bypass (B-2) | mitigate | Wrap both `deps.trashDriveFileFn` call sites in `if (!deps.args.dryRun)` blocks. In the else branch, emit `logger.info('[fix-image-pollution-manual] dry-run: would have trashed <fileId>')`. The DRIVE_DELETE trail row is also gated behind the same `if` — under dry-run no trash row is written (matches BR_WRITE gate behavior). |
| T-17-22 | R (Repudiation) — operator can't tell what dry-run would have changed | Drive ops in dry-run | mitigate | logger.info announces every WOULD-HAVE-TRASHED fileId; operator can audit stdout to understand the dry-run effect. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate both Drive-trash call sites behind !args.dryRun + log the would-have message</name>
  <files>
    scripts/fix-image-pollution-manual.ts,
    tests/scripts/fix-image-pollution-manual.test.ts
  </files>
  <read_first>
    - scripts/fix-image-pollution-manual.ts lines 380-450 (the delete handler — the `[d]elete` branch in `handleManualRow`)
    - scripts/fix-image-pollution-manual.ts lines 660-735 (the replace handler — `handleReplace`'s upload + compare-before-trash block)
    - scripts/fix-image-pollution-manual.ts lines 400-415 (the EXISTING Sheets-write gate — copy this pattern exactly)
    - scripts/fix-image-pollution-manual.ts lines 735-755 (the OTHER existing Sheets-write gate — same pattern)
    - tests/scripts/fix-image-pollution-manual.test.ts (find the existing `[d]elete` and `[r]eplace` tests — extend them)
    - .planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md "Pattern D: `--dry-run` Drive-trash gate (B-2)" lines 911-955 (the desired patch shape)
  </read_first>
  <behavior>
    - Test 1 (B-2 dry-run blocks delete-handler trash): mock `args.dryRun = true`. Operator queue has 1 row; operator chooses `[d]` and types 'DELETE'. Assert: `deps.trashDriveFileFn.mock.calls.length === 0`; `deps.writeUpdatesFn.mock.calls.length === 0` (blank cell write is also gated — already correct in Phase 16); `logger.info` called with a message containing `'dry-run: would have trashed'` and the origFileId. No DRIVE_DELETE trail row written.
    - Test 2 (B-2 dry-run blocks replace-handler trash): mock `args.dryRun = true`. Operator chooses `[r]` and supplies a URL. verifier passes. uploadToDrive returns a URL with a DIFFERENT fileId than origFileId. Assert: `deps.trashDriveFileFn.mock.calls.length === 0`; `deps.writeUpdatesFn.mock.calls.length === 0`; `logger.info` called with `'dry-run: would have trashed'`; no DRIVE_DELETE trail row.
    - Test 3 (live mode delete-handler still trashes — no regression): `args.dryRun = false`. Operator chooses `[d]` and types 'DELETE'. Assert: `deps.trashDriveFileFn.mock.calls.length === 1` with origFileId; `deps.writeUpdatesFn` called once (blank); DRIVE_DELETE trail row written.
    - Test 4 (live mode replace-handler still trashes when fileIds differ — no regression): `args.dryRun = false`. Operator `[r]`, verifier passes, uploadToDrive returns different fileId. Assert: trashDriveFile called once; writeUpdates called once; DRIVE_DELETE trail row written.
    - Test 5 (live mode update-in-place — no trash even in live mode): `args.dryRun = false`. uploadToDrive returns SAME fileId as origFileId. Assert: trashDriveFile NOT called (T-16-01 invariant preserved); writeUpdates called once; NO DRIVE_DELETE trail row. (This test should already exist in the Phase 16 test file — confirm it still passes.)
  </behavior>
  <action>
    Modify `scripts/fix-image-pollution-manual.ts`:

    1. **Patch the delete-handler trash gate** (around line 429 — the `[d]elete` branch inside `handleManualRow`):

       Current code (per the existing Phase 16 source):
       ```
       const origFileId = extractFileId(currentUrl);
       if (origFileId) {
         try {
           await deps.trashDriveFileFn(deps.driveClient, origFileId);
         } catch (err) {
           logger.warn(
             `[fix-image-pollution-manual] trashDriveFile failed for ${row.pid}: ${err}`,
           );
         }
         await deps.appendTrailRowFn({
           timestamp_iso: new Date().toISOString(),
           pid: row.pid,
           operation: 'DRIVE_DELETE',
           column_or_path: origFileId,
           ...
         });
       }
       ```

       Patched code:
       ```
       const origFileId = extractFileId(currentUrl);
       if (origFileId) {
         if (!deps.args.dryRun) {
           try {
             await deps.trashDriveFileFn(deps.driveClient, origFileId);
           } catch (err) {
             logger.warn(
               `[fix-image-pollution-manual] trashDriveFile failed for ${row.pid}: ${err}`,
             );
           }
           await deps.appendTrailRowFn({
             timestamp_iso: new Date().toISOString(),
             pid: row.pid,
             operation: 'DRIVE_DELETE',
             column_or_path: origFileId,
             old_value: origFileId,
             new_value: '',
             tier: 3,
             run_id: runId,
             notes: 'operator deleted in manual queue',
           });
         } else {
           logger.info(
             `[fix-image-pollution-manual] dry-run: would have trashed ${origFileId} (delete handler, pid=${row.pid})`,
           );
         }
       }
       ```

    2. **Patch the replace-handler trash gate** (around line 712 — inside `handleReplace`'s upload-and-trash block):

       Current code:
       ```
       if (
         origFileId &&
         newFileIdFromUpload &&
         origFileId !== newFileIdFromUpload
       ) {
         try {
           await deps.trashDriveFileFn(deps.driveClient, origFileId);
         } catch (err) {
           logger.warn(...);
         }
         await deps.appendTrailRowFn({ operation: 'DRIVE_DELETE', ... });
       }
       ```

       Patched code:
       ```
       if (
         origFileId &&
         newFileIdFromUpload &&
         origFileId !== newFileIdFromUpload
       ) {
         if (!deps.args.dryRun) {
           try {
             await deps.trashDriveFileFn(deps.driveClient, origFileId);
           } catch (err) {
             logger.warn(
               `[fix-image-pollution-manual] trashDriveFile failed for ${row.pid}: ${err}`,
             );
           }
           await deps.appendTrailRowFn({
             timestamp_iso: new Date().toISOString(),
             pid: row.pid,
             operation: 'DRIVE_DELETE',
             column_or_path: origFileId,
             old_value: origFileId,
             new_value: '',
             tier: 3,
             run_id: runId,
             notes: `replaced by ${newFileIdFromUpload}`,
           });
         } else {
           logger.info(
             `[fix-image-pollution-manual] dry-run: would have trashed ${origFileId} (replace handler, pid=${row.pid}, replacedBy=${newFileIdFromUpload})`,
           );
         }
       }
       ```

    3. **Do NOT modify** the verifier-after-fix logic, the choice-menu logic, the URL prompt, the FORCE confirmation flow, the resume-from-trail logic, or any unrelated code.

    4. **Extend `tests/scripts/fix-image-pollution-manual.test.ts`** with the 5 tests above. Group them under `describe('17-06 B-2 dry-run blocks trash', () => { ... })`. The DI seam already mocks `trashDriveFileFn` and `args.dryRun` — just write the new assertions.
  </action>
  <verify>
    <automated>npx vitest run tests/scripts/fix-image-pollution-manual.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "if \\(!deps\\.args\\.dryRun\\) \\{" scripts/fix-image-pollution-manual.ts` returns ≥ 4 (the 2 pre-existing Sheets-write gates + the 2 new Drive-trash gates).
    - `grep -nE "dry-run: would have trashed" scripts/fix-image-pollution-manual.ts` matches exactly 2 lines (one per patched site).
    - `grep -nE "trashDriveFileFn\\(" scripts/fix-image-pollution-manual.ts` matches exactly 2 lines (both inside the new gates).
    - `grep -cE "describe\\(.17-06 B-2 dry-run blocks trash" tests/scripts/fix-image-pollution-manual.test.ts` returns 1.
    - `npx vitest run tests/scripts/fix-image-pollution-manual.test.ts` reports the 5 new tests passing, plus all existing Phase 16 tests still pass.
    - `npx vitest run` (full suite) reports no regressions.
  </acceptance_criteria>
  <done>
    Both `trashDriveFileFn` call sites (lines 429 + 712 in original Phase 16 source) are gated behind `!deps.args.dryRun`. Else branch emits `logger.info` with the would-have-trashed message + context. DRIVE_DELETE trail rows also gated (matches BR_WRITE gate behavior — no trail row in dry-run). 5 tests passing; no Phase 16 regression.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/scripts/fix-image-pollution-manual.test.ts` reports all tests passing (existing + 5 new).
- `grep` acceptance criteria all pass.
- Manual smoke test (operator, optional): create a synthetic 1-pid queue, run `npx tsx scripts/fix-image-pollution-manual.ts --dry-run`, choose `[d]`, type 'DELETE'. Expected: stdout shows `'dry-run: would have trashed <fileId>'`; Drive file NOT trashed (verify by visiting the URL — still loads).
</verification>

<success_criteria>
- T-17-21 footgun closed: `--dry-run` mode does NOT trash Drive files for either `[d]elete` or `[r]eplace` choices.
- T-17-22 observability: operator sees what would have happened via `logger.info` lines.
- T-16-01 compare-before-trash invariant preserved in live mode.
- No regression in Phase 16 manual CLI tests.
- Same patch style as the existing Phase 16 Sheets-write gate (line 404 + 741) — consistent operator experience.
</success_criteria>

<output>
After completion, create `.planning/phases/17-catalog-image-pollution-fix/17-06-SUMMARY.md` recording:
- The 2 patch sites (line numbers in original + post-patch state).
- 5 test cases added.
- Operator smoke-test outcome (if performed).
- Any deviation from the planned shape (e.g., the line numbers shifted from the planned 429/712 because Phase 16 SUMMARY plans got merged with different formatting — note the actual line numbers post-patch).
</output>
