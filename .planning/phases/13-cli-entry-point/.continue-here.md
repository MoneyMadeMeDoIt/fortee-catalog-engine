---
phase: 13-cli-entry-point
task: 0
total_tasks: 2
status: not_started
last_updated: 2026-03-27T01:08:02.393Z
---

<current_state>
Phase 13 (CLI Entry Point) is planned but NOT yet executed. The plan (13-01-PLAN.md) needs a minor update before execution — it references `createShopifyClient()` which was replaced by `createDriveClient()` in the Google Drive migration done at the end of the last session.

Phases 08-12 are all complete. 339 tests passing across 27 test files.
</current_state>

<completed_work>

- Phase 08: Image Quality Scorer — `scoreImageQuality()` with calibrated thresholds
- Phase 09: Image Sourcing — `sourceImages()` parallel fetch from OMG/CSW/S&S
- Phase 10: AI Image Generation — `generateGarmentView()` + `enhanceFrontImage()` with $200 budget
- Phase 11: Image Standardization — 85% uniform scale, `standardizeImagesToSheets()` with Google Drive upload
- Phase 12: Audit Runner — `auditProductImages()` orchestrating the full pipeline
- Google Drive migration — replaced Shopify staged uploads with `uploadToDrive()` in `src/sheets/drive.ts`
</completed_work>

<remaining_work>

- Phase 13 Plan Update: Replan to replace `createShopifyClient` → `createDriveClient` in 13-01-PLAN.md
- Task 1: Create `scripts/audit-images.ts` CLI with --style-id, --all, --dry-run flags + unit tests
- Task 2: Human verify CLI against real environment (checkpoint)
</remaining_work>

<decisions_made>

- Google Drive replaces Shopify staged uploads for image hosting (user's images already in Drive)
- Drive folder structure: root (1xIjATpaEdqJYHRiuy0Iy6wIYUcNCXC8k) / supplierCode / styleId / filename
- NO Shopify product mutations in entire v2.0 pipeline — sheets only
- CLI uses `npx tsx` not `npx ts-node` (project convention)
- `--dry-run` logs which rows would be processed without calling the pipeline
- CostTracker shared across all products in --all mode
</decisions_made>

<blockers>
- Service account needs Editor access to Drive folder 1xIjATpaEdqJYHRiuy0Iy6wIYUcNCXC8k
- OPENAI_API_KEY needed in .env for AI generation
- SS_ACCOUNT_NUMBER / SS_API_KEY optional for S&S back/side images
</blockers>

<context>
This is the LAST phase of v2.0 Image Automation milestone. The CLI is pure wiring — it calls auditProductImages() which orchestrates everything. The main complexity is that the plan was written before the Google Drive migration, so it needs updating to use createDriveClient() instead of createShopifyClient(). The plan checker already verified the plan structure, so a targeted replan (--gaps or manual edit) should suffice.
</context>

<next_action>
Start with: `/gsd:plan-phase 13 --research` to regenerate the plan with Drive client, OR manually edit 13-01-PLAN.md to replace ShopifyClient references with Drive client. Then `/gsd:execute-phase 13`.
</next_action>
