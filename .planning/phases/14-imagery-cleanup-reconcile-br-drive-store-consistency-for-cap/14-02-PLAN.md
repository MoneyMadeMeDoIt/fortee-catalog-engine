---
phase: 14-imagery-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - tmp/audit-acceptance.tsv
  - tmp/cross-pollution-resolution.tsv
autonomous: true
requirements: ["R1.5000-recon", "R3.cross-sweep", "R5.168-cross"]
user_setup: []

must_haves:
  truths:
    - "5000 has zero orphan colors on store (every store color appears in BR)"
    - "168 has zero CROSS-POLLUTION and zero STORE-DRIFT in fresh audit"
    - "CROSS-POLLUTION sweep produces a checked-in resolution TSV with every row classified"
  artifacts:
    - path: "tmp/cross-pollution-resolution.tsv"
      provides: "Per-row classification of all CROSS-POLLUTION audit rows"
      contains: "MOVE\\|KEEP\\|TRASH"
    - path: "tmp/audit-acceptance.tsv"
      provides: "Documented exceptions for any remaining CROSS-POLLUTION/DUPE-DRIVE rows"
  key_links:
    - from: "scripts/drop-br-colors.ts"
      to: "src/shopify/resolve-store-product.ts"
      via: "import resolveStoreProduct to fetch the live 5000 product"
      pattern: "resolveStoreProduct"
---

<objective>
Reconcile 5000 and 168 to a clean state. Sweep the 826 CROSS-POLLUTION rows into a per-pid resolution TSV.

Purpose: Delete the 15 orphan colors on 5000 (matches the original "drop low-priority" intent), backfill freed slots with drift fix, and fix 168 now that the audit accepts Richardson_-prefixed files.
Output: Clean audit for pids 5000 and 168; TSV resolution doc for cross-pollution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-SPEC.md
@.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-CONTEXT.md
@.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-01-SUMMARY.md
@scripts/drop-br-colors.ts
@scripts/fix-store-drift.ts
@scripts/audit-product-imagery.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh audit baseline before any mutation</name>
  <files>tmp/imagery-audit.tsv</files>
  <read_first>scripts/audit-product-imagery.ts</read_first>
  <action>Run `npx tsx -r dotenv/config scripts/audit-product-imagery.ts > tmp/imagery-audit.log 2>&1`. After completion, capture the issue counts from the Summary section into a comment at the top of `tmp/audit-baseline-14-02.txt` (timestamp + per-check counts). This is the source of truth for downstream tasks in this plan.</action>
  <verify>`tmp/imagery-audit.tsv` has been regenerated within the last 10 minutes; `tmp/audit-baseline-14-02.txt` exists with timestamp and counts.</verify>
  <acceptance_criteria>
    - Fresh `tmp/imagery-audit.tsv` exists
    - `tmp/audit-baseline-14-02.txt` shows current STORE-DRIFT, CROSS-POLLUTION, DUPE-DRIVE, BAD-ALT counts
  </acceptance_criteria>
  <done>Baseline counts captured for verification.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the 15 orphan colors from 5000 store + backfill</name>
  <files>tmp/orphan-5000-delete.log</files>
  <read_first>scripts/drop-br-colors.ts, src/shopify/resolve-store-product.ts</read_first>
  <action>Write a one-shot helper script `scripts/delete-orphan-store-colors.ts` (or extend drop-br-colors.ts with a --skip-br mode + DROP map) that uses `resolveStoreProduct(client, '5000')` to find the live product, then queries its media, finds all media whose alt matches the 15 orphan colors (Aquatic, Antique Jade Dome, Antique Orange, Berry, Blackberry, Blue Dusk, Brown Savana, Cobalt, Dusty Rose, Electric Green, Lilac, Midnight, Neon Blue, Neon Green, Russet) — match `<color> (front|back|left side|right side|side)$` case-insensitive — and deletes them via `productDeleteMedia`. ALWAYS dry-run first (log expected media count for each color). Then execute live and write count of deleted media to `tmp/orphan-5000-delete.log`. After delete, run `npx tsx -r dotenv/config scripts/fix-store-drift.ts --pids 5000` to backfill any freed slots with missing per-color media.</action>
  <verify>Re-run `npx tsx -r dotenv/config scripts/audit-product-imagery.ts --pids 5000`: STORE-DRIFT for pid 5000 == 0 AND no STORE-EXTRA-COLOR rows for pid 5000.</verify>
  <acceptance_criteria>
    - `tmp/orphan-5000-delete.log` shows ≥ 30 deleted media (15 colors × 2-4 views each)
    - Audit on pid 5000 produces 0 STORE-DRIFT rows
    - Audit on pid 5000 produces 0 STORE-EXTRA-COLOR rows
    - `unisex-heavy-cotton-t-shirt-5000` media count drops below 250 cap with no orphans
  </acceptance_criteria>
  <done>5000 audit clean — store colors match BR.</done>
</task>

<task type="auto">
  <name>Task 3: Run drift fix on 168 with Richardson prefix accepted</name>
  <files>tmp/drift-168.log</files>
  <read_first>scripts/fix-store-drift.ts</read_first>
  <action>With the audit's KNOWN_SUPPLIER_PREFIXES allowlist (from plan 14-01) accepting Richardson_168_* files, the BR for pid 168 now references valid Drive files. Run `npx tsx -r dotenv/config scripts/fix-store-drift.ts --pids 168 > tmp/drift-168.log 2>&1`. The script will fetch the store product (use resolveStoreProduct internally if the script was updated, otherwise use the existing handle resolution), find missing front/back/sides per color, and attach them. Note: pid 168 had 4 STORE-DRIFT rows for sides counts; expected drift fix will attach ~30+ media.</action>
  <verify>Re-run `npx tsx -r dotenv/config scripts/audit-product-imagery.ts --pids 168`: STORE-DRIFT for pid 168 == 0 AND CROSS-POLLUTION for pid 168 == 0.</verify>
  <acceptance_criteria>
    - `tmp/drift-168.log` shows attached media count > 0
    - Audit on pid 168 produces 0 STORE-DRIFT rows
    - Audit on pid 168 produces 0 CROSS-POLLUTION rows
  </acceptance_criteria>
  <done>Pid 168 audit fully clean.</done>
</task>

<task type="auto">
  <name>Task 4: Generate cross-pollution resolution TSV</name>
  <files>tmp/cross-pollution-resolution.tsv</files>
  <read_first>tmp/imagery-audit.tsv, scripts/audit-product-imagery.ts</read_first>
  <action>Write a one-shot script `scripts/generate-cross-pollution-tsv.ts` that reads the fresh `tmp/imagery-audit.tsv`, filters to CROSS-POLLUTION rows, and writes `tmp/cross-pollution-resolution.tsv` with columns `pid | filename | suggested_action | reason`. Default suggested_action heuristic: (a) if filename's pid suffix appears in the BR sheet for ANY pid, mark MOVE-TO-{thatPid}; (b) if filename matches a known supplier prefix (Richardson_, S&S, Gildan, etc.) and the parent pid uses that supplier, mark KEEP-WHITELIST; (c) otherwise mark TRASH-ORPHAN. The script does NOT auto-apply — it produces the TSV for review. Reason column explains the heuristic match.</action>
  <verify>`tmp/cross-pollution-resolution.tsv` exists with 826+ data rows; each row has a non-empty suggested_action; row counts by action are logged to stdout.</verify>
  <acceptance_criteria>
    - `tmp/cross-pollution-resolution.tsv` exists with header `pid\tfilename\tsuggested_action\treason`
    - Every CROSS-POLLUTION row from the latest audit has a corresponding line in the TSV
    - At least one of {MOVE, KEEP, TRASH} appears in the action column distribution
  </acceptance_criteria>
  <done>Sweep TSV ready for plan 14-03 to apply.</done>
</task>

</tasks>

<verification>
- [ ] Audit re-run shows STORE-DRIFT for 5000 and 168 == 0
- [ ] No STORE-EXTRA-COLOR rows for any pid touched in this plan
- [ ] `tmp/cross-pollution-resolution.tsv` exists with all 826+ rows classified
- [ ] No new CROSS-POLLUTION introduced for any other pid
</verification>

<success_criteria>
- 5000 reconciled forward (orphans deleted, freed slots backfilled)
- 168 audit fully clean
- Cross-pollution sweep TSV ready for application
- All mutations dry-run logged in `tmp/`
</success_criteria>

<output>
After completion, create `.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-02-SUMMARY.md`
</output>
