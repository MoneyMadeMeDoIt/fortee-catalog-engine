---
phase: 14-imagery-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - scripts/audit-product-imagery.ts
  - .planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-VERIFICATION.md
autonomous: false
requirements: ["R3.cross-sweep", "R6.bad-alt-triage", "R11.verification"]
user_setup: []

must_haves:
  truths:
    - "Cross-pollution resolution TSV is fully applied (MOVE/TRASH actions executed)"
    - "BAD-ALT count drops to 0 OR audit regex extended to whitelist user-approved decoration alts"
    - "Final audit shows 0 unjustified issues across all 4 categories"
    - "VERIFICATION.md captures the final audit state and per-acceptance-criteria pass/fail"
  artifacts:
    - path: ".planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-VERIFICATION.md"
      provides: "Phase verification report — embedded final audit summary, all acceptance criteria checked"
      min_lines: 40
  key_links:
    - from: "tmp/cross-pollution-resolution.tsv"
      to: "scripts/apply-cross-pollution-resolution.ts"
      via: "TSV-driven mutation script reads classifications and executes Drive moves/trashes"
      pattern: "cross-pollution-resolution.tsv"
---

<objective>
Apply the cross-pollution sweep, resolve BAD-ALT decisions with user input, and produce the final phase verification report.

Purpose: Close out the phase with a verified-clean audit. Embed the final audit numbers in VERIFICATION.md and document any accepted variances.
Output: Cleaned-up Drive state, BAD-ALT decision applied, VERIFICATION.md committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-SPEC.md
@.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-CONTEXT.md
@.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-02-SUMMARY.md
@scripts/audit-product-imagery.ts
@scripts/cleanup-all-drive-orphans.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply cross-pollution resolution TSV</name>
  <files>tmp/cross-pollution-applied.log</files>
  <read_first>tmp/cross-pollution-resolution.tsv, scripts/cleanup-all-drive-orphans.ts</read_first>
  <action>Write `scripts/apply-cross-pollution-resolution.ts` that reads `tmp/cross-pollution-resolution.tsv` and applies actions: MOVE-TO-{pid} → Google Drive move file to that pid's folder; KEEP-WHITELIST → no action (already covered by audit allowlist); TRASH-ORPHAN → trash the file. ALWAYS dry-run first; require `--apply` flag for mutations. Log every action to `tmp/cross-pollution-applied.log` with file ID, action, before/after location. Use existing Drive client patterns from `scripts/cleanup-all-drive-orphans.ts`.</action>
  <verify>Dry-run reports the same action counts as the resolution TSV. Live run completes without errors. Re-run audit: CROSS-POLLUTION drops to 0 OR remaining rows match KEEP-WHITELIST and have a documented allowlist entry in `KNOWN_SUPPLIER_PREFIXES`.</verify>
  <acceptance_criteria>
    - `tmp/cross-pollution-applied.log` records every action from the TSV
    - Re-run audit produces 0 CROSS-POLLUTION OR remaining rows are KEEP-WHITELIST
    - No file moved without a corresponding `MOVE-TO-{pid}` row in the TSV
  </acceptance_criteria>
  <done>Cross-pollution sweep applied, audit reflects clean state.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>How should L01210 ("Left Chest Print" ×2, "Back Print") and L01250 ("(empty)", "Back Print", "Left Chest Print") BAD-ALT alts be handled?</decision>
  <context>These alts may be intentional decoration-zone samples showing where prints will be placed. They don't follow the canonical `<color> <view>` pattern, so the audit flags them. Two paths: (a) keep them and extend audit's BAD-ALT regex to whitelist `(left chest print|back print|right chest print|sleeve print)$` patterns; (b) delete them as accidental orphans.</context>
  <options>
    <option id="keep-and-whitelist">
      <name>Keep — extend audit BAD-ALT regex with decoration-zone whitelist</name>
      <pros>Preserves potentially intentional design samples; future products with same pattern won't re-flag</pros>
      <cons>Adds another exception to audit logic; user must confirm the alts are intentional</cons>
    </option>
    <option id="delete">
      <name>Delete — remove all 13 BAD-ALT media from L01210 and L01250</name>
      <pros>Simplest; audit goes clean without further regex exceptions</pros>
      <cons>Loses any potentially intentional decoration-zone samples; if they're real, user has to re-upload later</cons>
    </option>
  </options>
  <resume-signal>Select: keep-and-whitelist or delete</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply BAD-ALT decision</name>
  <files>scripts/audit-product-imagery.ts (if keep-and-whitelist) OR tmp/bad-alt-deleted.log (if delete)</files>
  <read_first>scripts/audit-product-imagery.ts</read_first>
  <action>If checkpoint resolved to `keep-and-whitelist`: extend audit's alt-parsing regex to also accept `^(.+?)\s+(left chest print|right chest print|back print|front print|sleeve print)$` and skip these from BAD-ALT counting. Add comment: `// Decoration-zone alts are user-intentional design samples for L01210/L01250 (and similar future products).` If checkpoint resolved to `delete`: write a one-shot deletion script that finds the 13 BAD-ALT media on L01210 and L01250 via `productDeleteMedia` and logs the deletions to `tmp/bad-alt-deleted.log`. Run dry-run first; live execute.</action>
  <verify>Re-run audit: BAD-ALT == 0 OR remaining BAD-ALT rows match the user-approved decoration-zone whitelist regex.</verify>
  <acceptance_criteria>
    - Audit shows 0 BAD-ALT OR remaining matches the whitelist pattern
    - The chosen path's artifact is committed (regex extension OR deletion log)
  </acceptance_criteria>
  <done>BAD-ALT resolved per checkpoint decision.</done>
</task>

<task type="auto">
  <name>Task 3: Final audit and write VERIFICATION.md</name>
  <files>.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-VERIFICATION.md</files>
  <read_first>.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-SPEC.md, tmp/imagery-audit.tsv</read_first>
  <action>Run final `npx tsx -r dotenv/config scripts/audit-product-imagery.ts > tmp/imagery-audit-final.log 2>&1`. Then write `14-VERIFICATION.md` with: (a) baseline counts (from tmp/audit-baseline-14-02.txt), (b) final counts (from tmp/imagery-audit-final.log Summary section), (c) per-requirement pass/fail (R1-R11 from SPEC.md acceptance criteria), (d) any accepted-variance entries from `tmp/audit-acceptance.tsv`. Format: markdown with a delta table, then per-requirement checklist with ✓/✗, then a "Notes" section listing any rows still flagged but accepted.</action>
  <verify>`14-VERIFICATION.md` exists with all 11 acceptance criteria from SPEC.md scored ✓ or ✗. If any ✗: file lists the gap and a remediation suggestion. If all ✓: file declares phase complete.</verify>
  <acceptance_criteria>
    - File `.planning/phases/14-.../14-VERIFICATION.md` exists with min 40 lines
    - All 6 SPEC.md requirements (R1-R6) plus 5 acceptance criteria mapped to ✓/✗
    - Final audit summary numbers embedded
    - If any ✗, remediation noted
  </acceptance_criteria>
  <done>Phase verification documented; user can review and decide whether to call phase complete.</done>
</task>

</tasks>

<verification>
- [ ] Final audit run produces a clean report (0 unjustified issues)
- [ ] VERIFICATION.md exists and is complete
- [ ] All BAD-ALT cases resolved per user choice
- [ ] All cross-pollution rows applied or whitelisted
</verification>

<success_criteria>
- Phase 14 acceptance criteria all met OR documented exceptions
- VERIFICATION.md captures final state with embedded audit summary
- Project memory note updated with phase completion
</success_criteria>

<output>
After completion, create `.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-03-SUMMARY.md`
</output>
