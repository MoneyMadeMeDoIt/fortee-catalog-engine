---
phase: 14-imagery-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shopify/resolve-store-product.ts
  - scripts/audit-product-imagery.ts
  - scripts/dedupe-drive-duplicates.ts
autonomous: false
requirements: ["R1.5000-recon", "R2.sibling-invariant", "R5.168-cross", "R4.dupe-round2", "R10.audit-extension"]
user_setup: []

must_haves:
  truths:
    - "Calling resolveStoreProduct(client, '5000') returns the unisex-heavy-cotton-t-shirt-5000 product without ambiguity"
    - "Audit run on pid 168 produces 0 CROSS-POLLUTION rows"
    - "Dedupe script trashes 92 supplier-original Drive duplicates across the 3 documented naming patterns"
  artifacts:
    - path: "src/shopify/resolve-store-product.ts"
      provides: "resolveStoreProduct helper with throw-on-multi"
      min_lines: 30
      exports: ["resolveStoreProduct", "MultipleStoreProductsError", "NoStoreProductError"]
    - path: "scripts/audit-product-imagery.ts"
      provides: "KNOWN_SUPPLIER_PREFIXES allowlist for cross-pollution check"
      contains: "KNOWN_SUPPLIER_PREFIXES"
    - path: "scripts/dedupe-drive-duplicates.ts"
      provides: "Extended STRAY_PATTERNS for underscored, Profile, HR-suffixed dupes"
      contains: "STRAY_PATTERNS"
  key_links:
    - from: "scripts/audit-product-imagery.ts"
      to: "KNOWN_SUPPLIER_PREFIXES"
      via: "filename prefix allowlist check inside cross-pollution detection"
      pattern: "KNOWN_SUPPLIER_PREFIXES"
---

<objective>
Build the foundational helper and extend two existing scripts so subsequent plans can run safely against pids with multiple matches, supplier-prefix Drive files, and supplier-original duplicate file patterns.

Purpose: Tonight's mistakes (silent-pick from `first: 1` and unrecognized Richardson_ prefix) need structural fixes before any further reconciliation work.
Output: `src/shopify/resolve-store-product.ts` + extended `audit-product-imagery.ts` + extended `dedupe-drive-duplicates.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-SPEC.md
@.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-CONTEXT.md
@src/shopify/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build resolveStoreProduct helper</name>
  <files>src/shopify/resolve-store-product.ts</files>
  <read_first>src/shopify/client.ts, scripts/drop-br-colors.ts</read_first>
  <action>Create `src/shopify/resolve-store-product.ts` exporting `resolveStoreProduct(client, pid, options?)` plus error classes `MultipleStoreProductsError` and `NoStoreProductError`. Implementation: query `products(first: 50, query: "handle:*${pid}*")`, filter results to handles where the last hyphen-separated segment === pid (case-insensitive). If 0 surviving matches throw NoStoreProductError(pid). If >1 throw MultipleStoreProductsError(pid, [{id,handle}...]). If exactly 1, log `[resolve] pid={pid} → handle={handle}` via logger.info and return `{id, handle, title}`. Optional `options.queryPattern` lets callers override the wildcard. Use `createShopifyClient` types from src/shopify/client.ts.</action>
  <verify>npx tsc --noEmit src/shopify/resolve-store-product.ts (no errors); inline smoke test: `import { resolveStoreProduct } from '../src/shopify/resolve-store-product.js'; const c = await createShopifyClient('DEST_SHOPIFY_'); console.log(await resolveStoreProduct(c, '5000'))` resolves to `unisex-heavy-cotton-t-shirt-5000`.</verify>
  <acceptance_criteria>
    - File `src/shopify/resolve-store-product.ts` exists with min 30 lines
    - Exports include `resolveStoreProduct`, `MultipleStoreProductsError`, `NoStoreProductError`
    - Smoke test returns the expected handle for pid 5000 without throwing
    - Helper logs match-count via logger.info on every call
  </acceptance_criteria>
  <done>Smoke test resolves pid 5000 to unisex-heavy-cotton-t-shirt-5000 without ambiguity.</done>
</task>

<task type="auto">
  <name>Task 2: Add KNOWN_SUPPLIER_PREFIXES to audit cross-pollution check</name>
  <files>scripts/audit-product-imagery.ts</files>
  <read_first>scripts/audit-product-imagery.ts</read_first>
  <action>Add a `const KNOWN_SUPPLIER_PREFIXES: Record<string, string[]> = { '168': ['Richardson_168_'] };` near the top of the file (after the existing constants block). In the cross-pollution check (search for `doesn't match pid prefix` to find the spot), modify the prefix-validation function: a Drive filename passes if it starts with `${pid}_`, OR its lowercase form starts with the pid, OR it starts with any prefix in `KNOWN_SUPPLIER_PREFIXES[pid]`. Comment block above the constant: `// Known supplier-prefixed filenames that are legitimate despite not starting with the pid. Add per-pid entries when a supplier consistently uses a brand-prefix naming (e.g., Richardson cap line uses 'Richardson_168_*').`</action>
  <verify>Re-run audit on pid 168 only: `npx tsx -r dotenv/config scripts/audit-product-imagery.ts --pids 168 2>&1 | grep -c "CROSS-POLLUTION"` returns 0 (or matches only files outside the Richardson_168_ pattern).</verify>
  <acceptance_criteria>
    - `scripts/audit-product-imagery.ts` contains the literal string `KNOWN_SUPPLIER_PREFIXES`
    - The constant maps `'168'` to an array containing `'Richardson_168_'`
    - Audit on pid 168 produces 0 CROSS-POLLUTION rows for files matching the Richardson_168_* pattern
  </acceptance_criteria>
  <done>Audit on pid 168 produces 0 CROSS-POLLUTION rows.</done>
</task>

<task type="auto">
  <name>Task 3a: Extend STRAY_PATTERNS in dedupe-drive-duplicates.ts (dry-run only)</name>
  <files>scripts/dedupe-drive-duplicates.ts, tmp/dedupe-dryrun-14-01.log</files>
  <read_first>scripts/dedupe-drive-duplicates.ts</read_first>
  <action>Read the existing STRAY_PATTERNS regex array. Append three new patterns covering the missed naming formats: (1) underscored CSW-style `^\d+_[A-Z_]+_(FRONT|BACK|SIDE)_(low|HR|hi)?_?\d+x\d+\.(webp|jpg|png|jpeg)$/i`, (2) S&S Profile-style `^[A-Z]\d+-[A-Za-z_-]+-Profile(_\d+x)?\.(webp|jpg|png|jpeg)$/i`, (3) HR-suffixed `^\d+_[a-z_]+_(front|back|side)_HR\.(jpg|png|jpeg|webp)$/i`. Each pattern must only match when a canonical `_std.png` exists for the same (color, view) — preserve the existing `canonicalExists` guard logic. Run dedupe in --dry-run only and capture the full output to `tmp/dedupe-dryrun-14-01.log`. DO NOT run live — the next checkpoint pauses for user review of the candidate list.</action>
  <verify>`tmp/dedupe-dryrun-14-01.log` exists and contains ≥ 92 "would trash" lines across 20 pids. The file lists every candidate path.</verify>
  <acceptance_criteria>
    - `scripts/dedupe-drive-duplicates.ts` STRAY_PATTERNS contains all 3 new regex entries
    - `tmp/dedupe-dryrun-14-01.log` exists with ≥ 92 candidates listed
    - No live mutations have occurred
  </acceptance_criteria>
  <done>Dry-run candidate list ready for user review.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Approve the DUPE-DRIVE candidate list in tmp/dedupe-dryrun-14-01.log for live trashing?</decision>
  <context>The dry-run produced ≥ 92 candidate Drive files matching the 3 new supplier-original patterns. Each candidate has a canonical `_std.png` sibling for the same (color, view), so trashing the candidate is non-destructive — the canonical version stays. User should spot-check the log for: (a) any pid where the candidate count seems unexpectedly high (signal: pattern over-matching), (b) any filename that looks like it might be the canonical version not a duplicate.</context>
  <options>
    <option id="approve">
      <name>Approve — run live trash on the full candidate list</name>
      <pros>Closes out the DUPE-DRIVE category in one shot</pros>
      <cons>Trusts the canonicalExists guard — if a regex over-matches and the guard misses an edge case, a canonical file gets trashed (recoverable from Drive trash within 30 days)</cons>
    </option>
    <option id="adjust">
      <name>Adjust — user flags specific files/pids to skip; rerun with exclusions</name>
      <pros>User can carve out edge cases before live execution</pros>
      <cons>Requires user to specify exclusions inline; another dry-run round before live</cons>
    </option>
    <option id="abort">
      <name>Abort — skip DUPE-DRIVE this phase</name>
      <pros>Maximum safety; dedupe deferred to a future review</pros>
      <cons>92 dupes remain flagged in audit; phase exit criteria require these to be either trashed or marked accepted-variance</cons>
    </option>
  </options>
  <resume-signal>Select: approve | adjust | abort</resume-signal>
</task>

<task type="auto">
  <name>Task 3b: Live-run dedupe (gated on checkpoint approval)</name>
  <files>tmp/dedupe-live-14-01.log</files>
  <read_first>tmp/dedupe-dryrun-14-01.log</read_first>
  <action>If checkpoint resolved to `approve`: run `npx tsx -r dotenv/config scripts/dedupe-drive-duplicates.ts > tmp/dedupe-live-14-01.log 2>&1` to trash the candidate list. If `adjust`: apply user-specified exclusions (e.g., a `--skip-pid` flag or hardcoded skip list) and re-run dry-run + live. If `abort`: write `tmp/dedupe-leftovers.tsv` documenting the 92 rows as accepted-variance and skip live mutation.</action>
  <verify>Re-run audit: `npx tsx -r dotenv/config scripts/audit-product-imagery.ts 2>&1 | grep -c "DUPE-DRIVE"` returns ≤ 5 (or matches the abort-path documented count).</verify>
  <acceptance_criteria>
    - If approved: `tmp/dedupe-live-14-01.log` shows trashed file count matching dry-run candidate count (±5 for race conditions)
    - After re-audit, DUPE-DRIVE count is ≤ 5 OR documented in tmp/dedupe-leftovers.tsv
  </acceptance_criteria>
  <done>DUPE-DRIVE category closed per user decision.</done>
</task>

</tasks>

<verification>
- [ ] `src/shopify/resolve-store-product.ts` smoke-tests cleanly against pid 5000
- [ ] `scripts/audit-product-imagery.ts --pids 168` reports 0 CROSS-POLLUTION
- [ ] `scripts/dedupe-drive-duplicates.ts` drops DUPE-DRIVE count to ≤ 5
- [ ] `npx tsc --noEmit` passes for all modified files
</verification>

<success_criteria>
- Helper exists, audit and dedupe extensions in place
- All 3 verifications pass before plan 14-02 starts
- No regressions in other audit categories
</success_criteria>

<output>
After completion, create `.planning/phases/14-imagery-cleanup-reconcile-br-drive-store-consistency-for-cap/14-01-SUMMARY.md`
</output>
