---
phase: 19-ai-category-keyword-generation
plan: 03
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - scripts/gen-categories-keywords.ts
autonomous: false
requirements: [CAT-01, CAT-02, CAT-03, KW-01, KW-02, KW-03, OPS-01, OPS-02]
must_haves:
  truths:
    - "A dry-run over the full ~291 products produces a preview TSV with every baseCategory resolving non-null and every categoriesPath a valid leaf"
    - "Interrupting the apply after ~50 products and re-running resumes from the checkpoint and completes the rest with zero duplicate Claude calls"
    - "After --apply, all variant rows of a given productId hold identical baseCategory / categories / keywords"
    - "A second --apply produces written_new=0 and overwritten_changed=0 (idempotent)"
    - "The operator approved the live token-spending --apply via a blocking human-verify checkpoint"
  artifacts:
    - path: "scripts/gen-categories-keywords.ts"
      provides: "Verification-hardened categorizer (any small fixes surfaced by the live dry-run audit)"
      min_lines: 250
  key_links:
    - from: "19-03 verification"
      to: "scripts/gen-categories-keywords.ts dry-run preview"
      via: "audit of preview TSV: 0 null baseCategory, 0 invalid leaf, 0 dirty keyword"
      pattern: "gen-cat-kw-preview"
---

<objective>
Verify the Phase 19 categorizer end-to-end against the live sheet, then gate the live token-spending `--apply` behind a blocking human-verify checkpoint — mirroring Phase 18-03 (`link-br-images.ts` live verification + checkpoint). This is where the operator confirms the package is legitimate, reviews the dry-run preview quality, and authorizes the run that spends Claude tokens and writes the live 24,175-row sheet.

Purpose: Phases 19-01/02 build and unit-prove the tool; this plan proves it on real data and obtains explicit operator authorization before any irreversible live write or token spend. It also exercises the interrupt/resume contract (OPS-01) on real scale (~50 products → stop → resume).
Output: a verified, approved run plus any small hardening fixes the live dry-run surfaces.
</objective>

<execution_context>
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-ai-category-keyword-generation/19-CONTEXT.md
@.planning/research/PITFALLS.md
@scripts/gen-categories-keywords.ts
@scripts/lib/category-schema.ts
@src/shopify/variants.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy + prerequisite gate (BLOCKING — before any token spend)</name>
  <what-built>
    Phase 19-02 added `@anthropic-ai/sdk` to package.json and the script requires `ANTHROPIC_API_KEY`. Per the planner package-legitimacy gate and threat T-19-SC, the operator must confirm the package and prerequisites before any run that spends money.
  </what-built>
  <how-to-verify>
    1. Confirm `@anthropic-ai/sdk` on npmjs.com is the OFFICIAL Anthropic SDK (publisher: anthropic; not a typosquat). Visit https://www.npmjs.com/package/@anthropic-ai/sdk and verify weekly downloads + repository link to github.com/anthropics/anthropic-sdk-typescript.
    2. Confirm `ANTHROPIC_API_KEY` is present in `.env` (P-2) — do NOT paste the key into chat. Confirm only that it exists.
    3. Confirm the operator accepts the expected spend: ~291 Haiku 4.5 calls (cheap, well under a dollar). No OpenAI cap is involved (D-01).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to the live dry-run, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Full live dry-run + preview-quality audit (no writes, no apply)</name>
  <read_first>
    - scripts/gen-categories-keywords.ts (dry-run + preview TSV emitter from 19-02 Task 3).
    - scripts/lib/category-schema.ts (SAFE_BASE_CATEGORIES, TAXONOMY_LEAF_PATHS, isCleanKeyword).
    - .planning/research/PITFALLS.md Pitfall 8/9/10 (taxonomy mismatch, label consistency, jargon).
  </read_first>
  <action>
    Run the full dry-run: `NODE_OPTIONS=--use-system-ca npx tsx scripts/gen-categories-keywords.ts` (default dry-run, all ~291 products). This DOES spend a small amount of Claude tokens (one call per product) but writes NOTHING to the sheet — that is acceptable post-Task-1 approval.
    Audit the produced `tmp/gen-cat-kw-preview-{ts}.tsv`:
    - 0 rows where the proposed baseCategory is NOT in SAFE_BASE_CATEGORIES (and getCategoryGroup() non-null for 100%). Any null-resolving proposal must show as "flagged / left unchanged", never as a write.
    - 0 rows where categoriesPath is not a TAXONOMY_LEAF_PATHS member (no free-form/hallucinated paths — Pitfall 8).
    - 0 keywords failing isCleanKeyword (no color/size/style#/GSM/jargon — Pitfall 10); spot-check 10 products by hand.
    - Audience preservation (D-09): for known youth/kids pids, the keywords include the audience term so title-builder isYouth() still fires.
    - Label consistency (Pitfall 9): build a frequency table of baseCategory + categoriesPath; flag any near-duplicate drift for the same garment type.
    If the audit surfaces a systematic issue (e.g. the model favors a wrong leaf for polos), apply a SMALL prompt/blocklist fix in scripts/lib/category-schema.ts buildPrompt or the blocklists, re-run the affected pids with `--pid`, and re-audit. Do NOT broaden scope — fixes are limited to prompt wording / blocklist entries / leaf-path mapping.
  </action>
  <verify>
    <automated>cd "C:/Users/ugofo/Downloads/Claude code project/Auto add product" && ls tmp/gen-cat-kw-preview-*.tsv >/dev/null 2>&1 && echo "preview-emitted"</automated>
  </verify>
  <done>A full dry-run preview exists; audit confirms 0 null-resolving baseCategory writes, 0 invalid leaf paths, 0 dirty keywords; audience terms preserved for youth pids; any surfaced issue fixed via a narrow prompt/blocklist change and re-audited clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Operator approves live --apply (BLOCKING — writes the live sheet)</name>
  <what-built>
    The dry-run preview (Task 2) is reviewed and clean. The next step writes baseCategory / categories / keywords to all 24,175 BR rows and is irreversible without the backup TSV. Per the planner rule (gate the live --apply behind a human-verify checkpoint, mirroring Phase 18-03), the operator must approve.
  </what-built>
  <how-to-verify>
    1. Open the latest `tmp/gen-cat-kw-preview-{ts}.tsv` and skim ~15 products across garment types (tee, hoodie, polo, jacket, youth). Confirm baseCategory, categoriesPath, and keywords look right.
    2. Confirm the baseCategory-CHANGED flags are acceptable (these are the decoration-affecting changes — D-14). Verify none would push a product into an unsupported group.
    3. Confirm you accept that --apply writes the live sheet (a backup TSV of the 3 columns is written first per D-14) and spends ~291 Claude calls.
  </how-to-verify>
  <resume-signal>Type "apply approved" to run the live --apply, or describe required changes.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Live --apply + interrupt/resume proof + idempotency + same-value-per-pid check</name>
  <read_first>
    - scripts/gen-categories-keywords.ts (checkpoint/resume + apply + backup from 19-02).
    - 19-CONTEXT.md D-11 (same 3 values to all rows of a pid), D-13 (checkpoint/resume + idempotent), D-14 (backup before write).
    - ROADMAP.md Phase 19 Success Criteria 4 + 5 (interrupt-after-50 resume; no per-row variance).
  </read_first>
  <action>
    Interrupt/resume proof (OPS-01, ROADMAP SC-4): start the apply, stop it after ~50 products (Ctrl+C or `--limit 50` then a second run). Confirm the checkpoint JSON lists ~50 completedPids. Re-run `--apply`; confirm it SKIPS the completed pids (no duplicate Claude calls — the products_skipped counter reflects them) and completes the remaining ~241.
    After completion, verify:
    - Backup TSV `tmp/gen-cat-kw-backup-{ts}.tsv` exists (written before the first write, D-14).
    - Same-value-per-pid (ROADMAP SC-5, D-11): for a sample of 5 products with many variant rows, confirm all rows share identical baseCategory / categories / keywords (read back via raw values.get and assert uniqueness per pid).
    - Idempotency (OPS-02, ROADMAP SC-5/idempotent): run `--apply` a third time; confirm the summary reports written_new=0 and overwritten_changed=0 (skip-if-filled / delta-only no-op).
    - Decoration safety: confirm getCategoryGroup() is non-null for every written baseCategory by reading the baseCategory column distinct values and checking each against getCategoryGroup (none null).
    Record final counters and the verification results in the SUMMARY.
  </action>
  <verify>
    <automated>cd "C:/Users/ugofo/Downloads/Claude code project/Auto add product" && test -f tmp/gen-categories-keywords-checkpoint.json && ls tmp/gen-cat-kw-backup-*.tsv >/dev/null 2>&1 && echo "checkpoint+backup-present"</automated>
  </verify>
  <done>Live --apply completed; interrupt-after-50 then resume completed the rest with zero duplicate calls; all rows of each sampled pid share identical values; a re-run reports written_new=0/overwritten_changed=0; every distinct written baseCategory resolves non-null via getCategoryGroup(); backup TSV exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator approval → irreversible live write + token spend | Checkpoints gate both spend and the 24k-row write |
| @anthropic-ai/sdk → build | Supply-chain confirmation before any run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-SC | Tampering | @anthropic-ai/sdk legitimacy | mitigate | Blocking human-verify Task 1 confirms official Anthropic publisher on npmjs.com before any token-spending run |
| T-19-07 | Tampering | live --apply to shared 24k-row sheet | mitigate | Blocking approval (Task 3) after preview review; backup TSV before write; idempotent re-run is a no-op (Task 4) |
| T-19-08 | Integrity | decoration contract after write | mitigate | Post-write distinct-baseCategory × getCategoryGroup() non-null check (Task 4) |
</threat_model>

<verification>
- Task 1 + Task 3 are blocking human-verify gates (NOT auto-approvable — autonomous: false).
- Full dry-run preview TSV emitted and audited clean (0 null baseCat, 0 invalid leaf, 0 dirty keyword).
- Interrupt-after-50 → resume completes remainder with zero duplicate Claude calls (OPS-01).
- All rows of each sampled productId share identical values (D-11 / ROADMAP SC-5).
- Re-run --apply reports written_new=0 / overwritten_changed=0 (OPS-02 idempotent).
- Every distinct written baseCategory resolves non-null via getCategoryGroup() (CAT-01/CAT-03 decoration safety).
</verification>

<success_criteria>
- Operator approved the package + prerequisites and the live --apply via blocking checkpoints.
- baseCategory (CAT-01/CAT-03), categories (CAT-02), keywords (KW-01/KW-02) written for all ~291 products in one call each (KW-03), fanned identically to all rows (D-11).
- Checkpoint resume proven on real scale (OPS-01); re-run is a no-op (OPS-02).
- Zero null-resolving baseCategory in the sheet after the run (push contract intact).
</success_criteria>

<output>
Create `.planning/phases/19-ai-category-keyword-generation/19-03-SUMMARY.md` when done.
</output>
