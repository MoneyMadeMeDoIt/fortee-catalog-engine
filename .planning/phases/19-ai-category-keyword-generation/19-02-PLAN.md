---
phase: 19-ai-category-keyword-generation
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - scripts/gen-categories-keywords.ts
  - package.json
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, KW-01, KW-02, KW-03, OPS-01, OPS-02]
user_setup:
  - service: anthropic
    why: "Claude Haiku 4.5 structured-output call per product (D-01). Replaces the OpenAI-cap blocker."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console → Settings → API Keys (operator-provided, P-2)"
must_haves:
  truths:
    - "Running the script with no flags (default dry-run) prints a per-product preview and writes NOTHING to the sheet"
    - "One Anthropic structured-output call is made per unique productId (~291), not per row"
    - "The same baseCategory, categories, and keywords values are written to ALL variant rows of a product"
    - "A baseCategory that would resolve null is never written; the existing value is left unchanged and logged"
    - "Interrupting after N products and re-running resumes from a checkpoint without re-calling Claude for completed products"
    - "Re-running on an already-filled batch reports written_new=0 / overwritten_changed=0 (idempotent)"
  artifacts:
    - path: "scripts/gen-categories-keywords.ts"
      provides: "Per-productId Claude Haiku categorizer with dry-run/apply, checkpoint/resume, idempotent skip, backup"
      min_lines: 250
      exports: ["buildUpdatesForProduct"]
    - path: "package.json"
      provides: "@anthropic-ai/sdk dependency (P-1)"
      contains: "@anthropic-ai/sdk"
  key_links:
    - from: "scripts/gen-categories-keywords.ts"
      to: "scripts/lib/category-schema.ts"
      via: "import categorySchema + buildPrompt + SAFE_BASE_CATEGORIES + isCleanKeyword"
      pattern: "category-schema"
    - from: "scripts/gen-categories-keywords.ts"
      to: "@anthropic-ai/sdk client.messages.parse"
      via: "zodOutputFormat(categorySchema) in output_config.format"
      pattern: "zodOutputFormat"
    - from: "scripts/gen-categories-keywords.ts"
      to: "src/sheets/writer.ts writeUpdates"
      via: "chunked batchUpdate of fanned-out EnrichmentUpdate[]"
      pattern: "writeUpdates"
    - from: "scripts/gen-categories-keywords.ts"
      to: "src/shopify/variants.ts getCategoryGroup"
      via: "post-validation: never write a null-resolving baseCategory (D-07)"
      pattern: "getCategoryGroup"
---

<objective>
Build `scripts/gen-categories-keywords.ts`: for each unique `productId` in Bestsellers-Ready (~291), make ONE `client.messages.parse()` call to `claude-haiku-4-5` with `output_config.format = zodOutputFormat(categorySchema)`, then fan the returned `{ baseCategory, categoriesPath, keywords[] }` out to all that product's variant rows. Mirror Phase 18's `link-br-images.ts` for dry-run/backup/header-reread/raw-values.get and Phase 18's checkpoint resilience.

Purpose: Fills `baseCategory` (decoration-safe, CAT-01/CAT-03), `categories` (display-only leaf path, CAT-02), and `keywords` (consumer tags read by title-builder, KW-01/KW-02) in one structured call per product (KW-03). Checkpointed (OPS-01) and idempotent (OPS-02) so an interrupt or cap-halt resumes without re-spending.
Output: the script + the `@anthropic-ai/sdk` dependency. Live `--apply` is gated behind the 19-03 human-verify checkpoint — this plan delivers the tool and proves dry-run + idempotency logic via the pure `buildUpdatesForProduct` core.
</objective>

<execution_context>
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-ai-category-keyword-generation/19-CONTEXT.md
@.planning/research/PITFALLS.md
@scripts/link-br-images.ts
@scripts/lib/br-image-parser.ts
@src/sheets/writer.ts
@src/shopify/variants.ts
@src/lib/title-builder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @anthropic-ai/sdk and wire the per-product Claude structured-output call</name>
  <read_first>
    - 19-CONTEXT.md D-01, D-02, D-03, D-04 (model, parse() + zodOutputFormat, one call per product, NODE_OPTIONS).
    - scripts/lib/category-schema.ts (from 19-01) — categorySchema + buildPrompt are the contract.
    - .planning/research/PITFALLS.md Pitfall 12 (cap mid-batch: distinguish rate-limit-retry from quota-exit).
  </read_first>
  <action>
    Run `npm install @anthropic-ai/sdk` (P-1; zod already present). Verify it lands in package.json dependencies.
    In scripts/gen-categories-keywords.ts, create a `classifyProduct(input, client)` helper that calls `client.messages.parse({ model: 'claude-haiku-4-5', max_tokens: <bounded, ~512>, messages:[{role:'user', content: buildPrompt(input)}], output_config: { format: zodOutputFormat(categorySchema) } })` and returns the parsed `{ baseCategory, categoriesPath, keywords }`. Import zodOutputFormat from '@anthropic-ai/sdk/helpers/zod'. Per D-02 do NOT set `effort` and do NOT set a thinking budget (Haiku errors on effort). Per D-03 this is ONE call per product.
    Add a System Setup note in the script header and SUMMARY: all invocations require the `NODE_OPTIONS=--use-system-ca` prefix (D-04, AV TLS interception) and `ANTHROPIC_API_KEY` in .env (P-2). Throw a clear actionable error at startup if ANTHROPIC_API_KEY is missing (do not print the key).
    Error handling (Pitfall 12): catch the Anthropic 429 — if it is a usage/quota error, save the checkpoint and exit cleanly with a message telling the operator to re-run after the cap clears; if it is a transient rate-limit, retry with bounded backoff. Distinguish by the error type/status the SDK exposes; do NOT infinite-retry a quota error.
  </action>
  <verify>
    <automated>cd "C:/Users/ugofo/Downloads/Claude code project/Auto add product" && node -e "require('@anthropic-ai/sdk'); require('@anthropic-ai/sdk/helpers/zod'); console.log('sdk+zod-helper ok')"</automated>
  </verify>
  <done>@anthropic-ai/sdk is in package.json; classifyProduct issues exactly one parse() call per product with zodOutputFormat(categorySchema); no `effort` param; missing-key startup error is actionable; 429 quota vs rate-limit are handled distinctly.</done>
</task>

<task type="auto">
  <name>Task 2: Pure buildUpdatesForProduct fan-out + productId grouping + post-validation (D-07/D-11)</name>
  <read_first>
    - scripts/link-br-images.ts — buildPlan() (lines 132-230) for the DI'd pure-core pattern, EnrichmentUpdate shape, columnToLetter usage, sheetRow=ri+2 + row-1 guard, raw values.get (NOT readAllRows).
    - 19-CONTEXT.md D-07 (never write null-resolving baseCategory; leave unchanged + log), D-11 (group + fan-out on productId), D-12 (header-index map + writeUpdates), D-14 (dry-run preview + flag baseCategory changes).
    - src/shopify/variants.ts getCategoryGroup (post-validation gate).
  </read_first>
  <action>
    Export a PURE `buildUpdatesForProduct({ productResult, rowsForPid, headers, idColIdx, colorColIdx, catColIdx, keywordsColIdx, baseCatColIdx, tab })` that returns `{ updates: EnrichmentUpdate[], preview, stats }` for ONE product's rows — no network, DI'd exactly like 18's buildPlan so it is unit-testable. Behavior:
    - D-07 post-validation: if `getCategoryGroup(productResult.baseCategory)` is null (defense in depth even though the schema constrains it), DO NOT emit a baseCategory update for any row; leave existing value, push a `flagged` log entry. (The schema already prevents this; this is the belt-and-suspenders gate D-07 mandates.)
    - D-11 fan-out: emit the SAME baseCategory / categoriesPath / keywords (joined to the BR keywords cell format — comma-or-space joined per existing column convention; check an existing keywords cell to match) to EVERY row of this productId.
    - D-14 baseCategory-change flag: when the new baseCategory differs from the row's existing baseCategory, mark it in the preview so the dry-run highlights decoration-affecting changes.
    - OPS-02 idempotency: if a cell already equals the target value, skip it (delta-only), incrementing skipped_already_current. For keywords/categories, "already filled with identical value" → skip.
    - P5 row-1 guard: sheetRow = ri + 2; assert >= 2 before emitting any range; reuse columnToLetter from src/sheets/column-map.ts.
    Add a `groupRowsByProductId(dataRows, idColIdx)` helper returning Map<pid, {rowIndex, row}[]> (rowIndex is the 0-based dataRows index so sheetRow math stays correct). Rows with empty productId are skipped (counted).
    Reuse the keyword color/size/jargon blocklists exported from category-schema.ts for a final isCleanKeyword pass before fan-out — if the model returns a dirty keyword that somehow passed the schema, drop it and log (KW-02 belt-and-suspenders).
  </action>
  <verify>
    <automated>cd "C:/Users/ugofo/Downloads/Claude code project/Auto add product" && npx vitest run scripts/gen-categories-keywords.test.ts</automated>
  </verify>
  <done>buildUpdatesForProduct is pure/DI'd; a null-resolving baseCategory emits no baseCategory update and is flagged; identical values across all rows of a pid; idempotent skip on already-current cells; row-1 guard enforced; productId grouping correct. Unit tests cover fan-out, idempotency, and the null-baseCategory leave-unchanged path.</done>
</task>

<task type="auto">
  <name>Task 3: CLI wiring — dry-run preview + checkpoint/resume + backup + apply</name>
  <read_first>
    - scripts/link-br-images.ts — main() (lines 472-655): raw values.get header read, idColIdx/colorColIdx resolution, dry-run-default + --apply, backup TSV before write, TSV emit + stats, writeUpdates call, fatal-catch tail.
    - 19-CONTEXT.md D-12 (raw values.get + writeUpdates, never readAllRows), D-13 (per-product checkpoint + idempotent skip-if-filled), D-14 (dry-run default, --apply backs up affected columns first).
    - .planning/research/PITFALLS.md Pitfall 4/6 (header drift / partial-write checkpoint), Pitfall 13 (re-run drift summary).
  </read_first>
  <action>
    Wire main(): parse args (`--apply` → applyMode, default dry-run; optional `--force` to re-process already-filled products; optional `--limit N` to cap products for a smoke test; optional `--pid X` for a single product). Read the header row and ALL data rows via raw `sheetsApi.spreadsheets.values.get` (D-12 — never readAllRows, which drops columns outside SHEET_COLUMNS). Resolve column indices by NAME from the live header for productId, colorName, baseCategory, categories, keywords; throw if any is missing.
    Checkpoint (OPS-01/D-13): use `tmp/gen-categories-keywords-checkpoint.json` of shape `{ completedPids: string[], failedPids: string[], timestamp }`. Write it after EACH product's Claude call + accumulation (fsync). On startup, load it and SKIP pids in completedPids. This mirrors the resilience pattern noted in MEMORY (finalize idempotent partial-state recovery).
    Idempotent skip-if-filled (OPS-02/D-13): before calling Claude for a pid, if all three target cells are already non-empty for that product's first row AND `--force` is not set, skip the API call entirely (record as skipped). `--force` re-processes.
    Bounded concurrency (D-03): process products with a small concurrency limit (e.g. 8-12) over the pid list; keep it simple (a hand-rolled worker pool or sequential-with-small-batches is fine — no Batches API). Update the checkpoint per completed product even under concurrency.
    Dry-run (D-14, default): for each processed product emit a preview row to `tmp/gen-cat-kw-preview-{ts}.tsv` (pid, old→new baseCategory with a CHANGED flag when decoration-affecting, categoriesPath, keywords) and write NOTHING to the sheet. Print the Pitfall-13 summary: `{ written_new, overwritten_changed, skipped_already_current, flagged_unsafe_basecat, products_called, products_skipped }`.
    Apply (D-14): on `--apply`, FIRST write a backup TSV of the three affected columns to `tmp/gen-cat-kw-backup-{ts}.tsv` (pid, color, baseCategory, categories, keywords) — mirror writeBackupTsv in link-br-images.ts. Then flush accumulated EnrichmentUpdate[] via writeUpdates (chunked). Fatal-catch tail that exits non-zero, mirroring link-br-images.ts.
    Do NOT run a live --apply in this plan — that is gated by the 19-03 blocking human-verify checkpoint (mirrors 18-03). The Done criterion is dry-run + checkpoint behavior, provable with `--pid` against a single product or `--limit 2` once a key is present, or via unit tests of the pure core if no key is available.
  </action>
  <verify>
    <automated>cd "C:/Users/ugofo/Downloads/Claude code project/Auto add product" && npx tsx --check scripts/gen-categories-keywords.ts && echo "typecheck-ok"</automated>
  </verify>
  <done>Default run is dry-run and writes nothing; checkpoint JSON is written per product and skips completed pids on resume; skip-if-filled avoids the API call unless --force; --apply backs up the three columns before any write and uses writeUpdates; the Pitfall-13 summary prints all counters; script typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude output → BR sheet | Model `baseCategory` is decoration-critical; categories/keywords feed display + title-builder |
| script → live 24,175-row shared sheet | A bad write or header drift corrupts a sheet a human co-edits |
| npm install @anthropic-ai/sdk | Supply-chain: new package pulled into the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Integrity/Elevation | baseCategory write path | mitigate | Schema enum (19-01) + getCategoryGroup() post-validation (D-07): a null-resolving value is never written, existing value preserved + logged |
| T-19-05 | DoS / cost | OpenAI/Anthropic cap mid-batch | mitigate | Per-product checkpoint (OPS-01); 429 quota → checkpoint + clean exit, not infinite retry (Pitfall 12) |
| T-19-06 | Tampering | live sheet overwrite | mitigate | dry-run default (D-14); backup TSV of the 3 columns before --apply; delta-only idempotent writes; row-1 guard (P5) |
| T-19-SC | Tampering | npm install @anthropic-ai/sdk | mitigate | @anthropic-ai/sdk is the official first-party Anthropic SDK (legitimacy verified in 19-03 checkpoint before any token spend) |
</threat_model>

<verification>
- `node -e "require('@anthropic-ai/sdk')"` resolves (Task 1).
- `npx tsx --check scripts/gen-categories-keywords.ts` typechecks (Task 3).
- `npx vitest run scripts/gen-categories-keywords.test.ts` passes (Task 2 pure-core tests: fan-out identical-across-rows, idempotent skip, null-basecat leave-unchanged, row-1 guard).
- Manual (operator, once ANTHROPIC_API_KEY present): `NODE_OPTIONS=--use-system-ca npx tsx scripts/gen-categories-keywords.ts --pid <one> ` produces a preview TSV and writes nothing.
</verification>

<success_criteria>
- One structured-output Claude call per unique productId; results fanned to all variant rows with identical values (KW-03, D-11).
- baseCategory constrained to the decoration-safe enum and post-validated; null-resolving never written (CAT-01, CAT-03, D-07).
- categories filled with a taxonomy leaf path (CAT-02); keywords are clean consumer tags (KW-01, KW-02).
- Default dry-run writes nothing; --apply backs up first then writes via writeUpdates (D-14).
- Per-product checkpoint resumes without re-spending (OPS-01); skip-if-filled is idempotent (OPS-02).
</success_criteria>

<output>
Create `.planning/phases/19-ai-category-keyword-generation/19-02-SUMMARY.md` when done.
</output>
