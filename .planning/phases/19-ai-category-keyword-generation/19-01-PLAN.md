---
phase: 19-ai-category-keyword-generation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/lib/category-schema.ts
  - scripts/lib/category-schema.test.ts
autonomous: true
requirements: [CAT-01, CAT-03, KW-01, KW-02]
must_haves:
  truths:
    - "Every member of the baseCategory safe enum resolves non-null via getCategoryGroup()"
    - "The zod schema rejects a baseCategory value outside the closed enum"
    - "The categoriesPath validator only accepts one of the known Shopify-taxonomy leaf paths"
    - "The keyword validator rejects color names, size names, style numbers, GSM values, and wholesale jargon, and caps at 15"
    - "sanitizeForPrompt strips backticks, dollar signs, and curly braces so descriptions cannot break the prompt"
  artifacts:
    - path: "scripts/lib/category-schema.ts"
      provides: "SAFE_BASE_CATEGORIES enum, categorySchema (zod), TAXONOMY_LEAF_PATHS, buildPrompt, sanitizeForPrompt, isCleanKeyword"
      min_lines: 120
      exports: ["SAFE_BASE_CATEGORIES", "categorySchema", "TAXONOMY_LEAF_PATHS", "buildPrompt", "sanitizeForPrompt", "isCleanKeyword"]
    - path: "scripts/lib/category-schema.test.ts"
      provides: "Unit tests proving every enum member is decoration-safe + keyword/path/sanitize rules"
      min_lines: 80
  key_links:
    - from: "scripts/lib/category-schema.ts"
      to: "src/shopify/variants.ts getCategoryGroup"
      via: "import + per-enum-member non-null assertion in test"
      pattern: "getCategoryGroup"
---

<objective>
Build the pure, network-free core of the Phase 19 categorizer: a Zod schema whose `baseCategory` is a CLOSED enum of decoration-SAFE garment values, a validated `categoriesPath` (Shopify Standard Taxonomy leaf), a bounded clean-keyword array, plus `buildPrompt()` and `sanitizeForPrompt()`. This mirrors Phase 18's split of a pure core (`scripts/lib/br-image-parser.ts`) from the I/O script, so the contract is unit-testable without an Anthropic API key.

Purpose: The `baseCategory` field is decoration-CRITICAL (per D-05: `getCategoryGroup()` returning null makes `product-push.ts:104` THROW). The only safe guarantee is to schema-constrain the model's output to values we have PROVEN resolve non-null. A unit test asserting non-null for every enum member is the contract that prevents a future catalog-wide push break.
Output: `scripts/lib/category-schema.ts` (pure module) + `scripts/lib/category-schema.test.ts` (vitest).
</objective>

<execution_context>
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-ai-category-keyword-generation/19-CONTEXT.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@src/shopify/variants.ts
@src/lib/title-builder.ts
@scripts/lib/br-image-parser.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Decoration-safe baseCategory enum + per-member getCategoryGroup non-null test (RED first)</name>
  <read_first>
    - src/shopify/variants.ts — getCategoryGroup() (lines 9-23) and SUPPORTED_CATEGORIES (lines 39-48). This is the gate.
    - .planning/research/FEATURES.md — "baseCategory Controlled Vocabulary (~15 values)" (lines 106-128).
    - 19-CONTEXT.md D-05, D-06, D-07.
  </read_first>
  <behavior>
    VERIFIED during planning (node check against getCategoryGroup):
    - These 15 controlled-vocab values: 9 resolve non-null (T-Shirts, Long Sleeve Shirts, Polo Shirts, Tank Tops, Hoodies, Jackets, Vests, Youth T-Shirts, Youth Hoodies); "Sweatshirts" resolves NULL (bare word matches neither `crew`+`fleece` nor `crew`); Shorts/Sweatpants/Caps/Beanies/Bags resolve NULL.
    - "Crewneck Sweatshirt" resolves to crewnecks (matches `crew`) and is the safe replacement for the null "Sweatshirts" value.
    - The 8 SUPPORTED_CATEGORIES strings all resolve non-null.
    - SAFE_BASE_CATEGORIES MUST therefore = the 9 safe controlled-vocab values + "Crewneck Sweatshirt" + the 8 SUPPORTED_CATEGORIES strings (deduped). It MUST NOT contain Sweatshirts, Shorts, Sweatpants, Caps, Beanies, Bags.
    - Test: for EVERY member m of SAFE_BASE_CATEGORIES, expect(getCategoryGroup(m)).not.toBeNull().
    - Test: expect(getCategoryGroup('Sweatshirts')).toBeNull() and the same for Caps/Beanies/Bags/Shorts/Sweatpants — documents WHY they are excluded (regression guard).
    - Test: categorySchema.safeParse({ baseCategory: 'Caps', ... }).success === false (enum rejects null-resolving values).
  </behavior>
  <action>
    Create scripts/lib/category-schema.ts. Export `SAFE_BASE_CATEGORIES` as a `const` string tuple containing EXACTLY the decoration-safe set defined in <behavior> (9 safe controlled-vocab values + "Crewneck Sweatshirt" + the 8 SUPPORTED_CATEGORIES strings, deduped). Do NOT include any value that resolves null. Per D-06, the enum is built from the verified recognized set, not hand-waved. Add a top-of-file comment block recording the planning verification (which values resolve which group, and which are excluded and why), citing D-05/D-06/D-07.
    Create scripts/lib/category-schema.test.ts. Write the RED tests in <behavior> FIRST (import getCategoryGroup from ../../src/shopify/variants.js). The per-member non-null loop is the load-bearing assertion — it is the contract that protects product-push. Then make them GREEN by finalizing the enum tuple. Per D-07, the schema is the guardrail; downstream the script will LEAVE existing baseCategory unchanged when the model cannot pick a safe value (implemented in 19-02), so the schema never needs an "unknown" escape hatch — an out-of-enum value is simply rejected.
    Do NOT add network code, Anthropic imports, or sheet I/O here — pure module only.
  </action>
  <verify>
    <automated>cd "C:/Users/ugofo/Downloads/Claude code project/Auto add product" && npx vitest run scripts/lib/category-schema.test.ts</automated>
  </verify>
  <done>All tests pass; SAFE_BASE_CATEGORIES contains zero null-resolving members; the per-member getCategoryGroup non-null loop passes for every enum entry; Sweatshirts/Caps/Beanies/Bags/Shorts/Sweatpants are proven excluded.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Zod combined schema (categoriesPath leaf + bounded clean keywords) + buildPrompt + sanitizeForPrompt</name>
  <read_first>
    - .planning/research/FEATURES.md — "Key paths for the Fortee catalog" taxonomy table (lines 85-100); "Format rules" + "What NOT to include in tags" (lines 149-171).
    - .planning/research/PITFALLS.md — Pitfall 10 (keyword stuffing/jargon), Pitfall 11 (prompt injection), Pitfall 8 (taxonomy mismatch).
    - src/lib/title-builder.ts — isYouth() (lines 49-54): reads keywords for audience. D-09.
    - 19-CONTEXT.md D-02, D-08, D-09, D-10.
  </read_first>
  <behavior>
    - TAXONOMY_LEAF_PATHS: a closed set of the Shopify Standard Taxonomy leaf paths from FEATURES.md (T-Shirts, Sweatshirts, Hoodies & Sweatshirts, Polo Shirts, Shirts, Tank Tops, Shorts, Sweatpants & Joggers, Jackets & Coats, Vests, Baseball Caps, Beanies, Bags). categorySchema.categoriesPath MUST be a z.enum over these (D-08: always a leaf, never free-form).
    - categorySchema.keywords: z.array(z.string()).min(8).max(15) (D-10 ≤15; FEATURES min 8). Each keyword must pass isCleanKeyword.
    - isCleanKeyword(kw) returns false for: any uppercase char (must be lowercase); spaces (must be hyphenated); any digit (blocks GSM/style#/size like "size-xl"→ actually blocks via digit + jargon list); known color names (red, black, navy, white, grey, gray, green, blue, royal, maroon, …); size tokens (xs, s, m, l, xl, xxl, 2xl, 3xl, small, medium, large); wholesale jargon (bulk, wholesale, gsm, partid, blank, case, moq, style). Test each category with a representative bad token → false, and a good token (mens, fleece-hoodie, heavyweight, corporate-gift) → true.
    - categorySchema.safeParse rejects: baseCategory not in enum; categoriesPath not in enum; keywords array of length 7 or 16; keywords containing "color-black" or "gsm-250" or "Mens" (uppercase).
    - sanitizeForPrompt(s): replaces/strips backtick, $, {, } (Pitfall 11) and truncates to 500 chars; returns '' for null/undefined. Test: input with `${x}` and ``` ` ``` and {{brand}} produces a string with none of those characters.
    - buildPrompt({ productName, description, brandName, gender, fit, weightGSM, currentBaseCategory }): returns a string that (a) wraps each field in XML delimiters (<product_description>…</product_description> etc.) using sanitizeForPrompt, (b) names the SAFE_BASE_CATEGORIES enum and the TAXONOMY_LEAF_PATHS as the allowed output sets, (c) states the keyword rules (lowercase-hyphen, ≤15, consumer terms; exclude color/size/style#/GSM/jargon), (d) per D-09 instructs preservation of the audience term for youth/kids/women/men. Test: buildPrompt output contains the gender value, contains "youth" guidance text, and does NOT contain raw backticks from an injected description.
  </behavior>
  <action>
    Extend scripts/lib/category-schema.ts. Add TAXONOMY_LEAF_PATHS (const tuple), the full `categorySchema` (zod object: baseCategory z.enum(SAFE_BASE_CATEGORIES), categoriesPath z.enum(TAXONOMY_LEAF_PATHS), keywords bounded clean array via .refine using isCleanKeyword), `isCleanKeyword`, `sanitizeForPrompt`, and `buildPrompt`. The schema object is what 19-02 passes to zodOutputFormat (D-02) — keep it a plain exported zod object with no SDK coupling. Use zod v4 (already in deps).
    Add the RED tests in <behavior> to category-schema.test.ts first, then implement to GREEN. Keep the keyword color/size/jargon blocklists as exported const arrays so 19-02's post-validation can reuse them (single source of truth, no drift).
    Do NOT import @anthropic-ai/sdk here — buildPrompt returns a plain string and categorySchema is a plain zod object; the SDK wiring lives in 19-02.
  </action>
  <verify>
    <automated>cd "C:/Users/ugofo/Downloads/Claude code project/Auto add product" && npx vitest run scripts/lib/category-schema.test.ts</automated>
  </verify>
  <done>categorySchema validates a good payload and rejects bad baseCategory/path/keyword payloads; isCleanKeyword blocks color/size/style#/GSM/jargon and accepts consumer tags; sanitizeForPrompt removes `,$,{,} and truncates to 500; buildPrompt embeds the allowed sets, the audience-preservation instruction (D-09), and sanitizes injected input. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supplier-scraped `description`/`productName` → prompt string | Untrusted scraped text crosses into the LLM prompt (Pitfall 11 prompt injection) |
| model output → BR sheet (decoration contract) | An out-of-vocab `baseCategory` would break product-push (D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | buildPrompt() interpolating scraped description | mitigate | sanitizeForPrompt strips `,$,{,} + XML-delimits all field values; truncate to 500 chars (Pitfall 11) |
| T-19-02 | Elevation/Integrity | baseCategory feeding getCategoryGroup() | mitigate | categorySchema constrains baseCategory to a CLOSED enum every member of which is unit-test-proven non-null (D-05/D-06) |
| T-19-03 | Integrity | keyword field read by title-builder isYouth() | mitigate | buildPrompt instructs audience-term preservation (D-09); isCleanKeyword keeps tokens lowercase-hyphen so the isYouth regex still matches |
</threat_model>

<verification>
- `npx vitest run scripts/lib/category-schema.test.ts` passes.
- Manual: confirm the per-member non-null loop count equals SAFE_BASE_CATEGORIES.length (no silent skip).
- Grep guard: `grep -v '^ *//' scripts/lib/category-schema.ts | grep -c "Sweatshirts'"` — the bare null-resolving value must not appear as an enum member (only "Crewneck Sweatshirt" is allowed).
</verification>

<success_criteria>
- scripts/lib/category-schema.ts exports SAFE_BASE_CATEGORIES, categorySchema, TAXONOMY_LEAF_PATHS, buildPrompt, sanitizeForPrompt, isCleanKeyword — all pure, no network.
- Every SAFE_BASE_CATEGORIES member resolves non-null via getCategoryGroup() (test-enforced).
- categorySchema enforces the closed enum, leaf-path enum, and bounded clean-keyword rules.
- All vitest tests pass; no Anthropic SDK import in this module.
</success_criteria>

<output>
Create `.planning/phases/19-ai-category-keyword-generation/19-01-SUMMARY.md` when done.
</output>
