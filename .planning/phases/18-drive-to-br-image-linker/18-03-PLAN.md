---
phase: 18-drive-to-br-image-linker
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - scripts/check-br-image-urls.ts
  - tests/scripts/check-br-image-urls.test.ts
autonomous: false
requirements: [IMG-01, IMG-02, IMG-03, IMG-04, OPS-02, OPS-03]
tags: [drive, image-linker, verification, url-render, idempotency, checkpoint]

must_haves:
  truths:
    - "The real --dry-run diff is reviewed: hyphenated-brand pids (Q-Tees H08050) show clean color tokens with no brand leak (IMG-03, D-07)"
    - "A sample of written/about-to-be-written Drive URLs returns image/* content over HTTP HEAD; non-image responses are flagged (URL render verification — phase intent open item)"
    - "--apply overwrites the 7 image columns and adds the 4 new columns on the live sheet without header drift (IMG-01, IMG-02, OPS-03)"
    - "A second --apply reports written_new=0 and overwritten_changed=0 (idempotent — OPS-02, D-11)"
    - "Drive-absent (pid,color) pairs appear in the miss TSV with existing cells preserved (IMG-03, D-08)"
  artifacts:
    - path: "scripts/check-br-image-urls.ts"
      provides: "Sample URL validator: HTTP HEAD a random sample of Drive URLs, assert Content-Type image/*, flag 403/HTML (P7)"
      min_lines: 40
    - path: "tests/scripts/check-br-image-urls.test.ts"
      provides: "Unit test for the content-type classification (image vs html/403) against mocked responses"
      min_lines: 30
  key_links:
    - from: "scripts/check-br-image-urls.ts"
      to: "Drive uc?id URLs"
      via: "HTTP HEAD (or ranged GET) + Content-Type inspection"
      pattern: "content-type"
---

<objective>
Verify the linker end-to-end against the live Bestsellers-Ready sheet and prove the five
phase success criteria hold: (1) dry-run diff is correct and brand-leak-free, (2) the
canonical Drive URL form actually renders (HTTP HEAD returns image/*), (3) --apply writes
the 7 columns + 4 new headers without drift, (4) a second --apply is a no-op (idempotent),
(5) Drive-absent pairs are preserved and logged. Includes a small reusable URL-render
validator script (`scripts/check-br-image-urls.ts`) and a blocking human-verify checkpoint
because --apply mutates a shared 24k-row customer-facing sheet.

Purpose: This is the goal-backward proof that the catalog's image columns are correctly and
safely populated. The URL-render check closes the open verification item (does the
`uc?id=<fileId>` form render when written into BR cells, given files moved via the Drive UI
may lack public permission — PITFALLS P7).
Output: `scripts/check-br-image-urls.ts` + test; a reviewed dry-run; a live --apply; a
confirmed idempotent second run; operator sign-off.
</objective>

<execution_context>
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-drive-to-br-image-linker/18-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/18-drive-to-br-image-linker/18-01-SUMMARY.md
@.planning/phases/18-drive-to-br-image-linker/18-02-SUMMARY.md
</context>

<read_first>
- `scripts/link-br-images.ts` (from 18-02) — the linker under test; understand its CLI flags,
  the diff/miss/backup TSV paths, and the `urlForFileId` helper / `uc?id=<fileId>` form.
- `src/sheets/drive.ts` (lines 260-280) — `uploadToDrive` sets `role:reader, type:anyone` on
  NEW files; files moved via the Drive UI may lack this permission (P7 root cause).
- `.planning/research/PITFALLS.md` (P7, P1, P13) — Drive URL render/permission, wrong-color
  validation sample, re-run drift verification.
- STATE.md "Phase 19 gate" notes — the operator already flagged: verify DirectSideImage really
  holds LEFT-side URLs before overwriting, and run ~10 HTTP HEAD checks post-link.
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: URL-render validator script + content-type classifier (tested)</name>
  <files>scripts/check-br-image-urls.ts, tests/scripts/check-br-image-urls.test.ts</files>
  <behavior>
    classifyHeadResponse({ status, contentType }):
    - status 200 + content-type "image/png" → "ok"
    - status 200 + content-type "text/html" → "not-image" (Drive permission interstitial)
    - status 403 → "forbidden" (missing public permission)
    - status 302/redirect handled by following (or treat as ok if final is image/*)
    A `main()` that: reads N (default 10) random Drive URLs from a column of BR (or from a
    passed TSV of the just-applied updates), issues HTTP HEAD (fallback to a 1-byte ranged GET
    if HEAD is unsupported by the redirect target), and prints a pass/flag table.
  </behavior>
  <action>Create `scripts/check-br-image-urls.ts` exporting a pure `classifyHeadResponse(...)`
  per `<behavior>` plus a `main()` that samples URLs and HEAD/ranged-GETs them
  (NODE_OPTIONS=--use-system-ca will be set by the operator per machine TLS note). Accept
  `--from-tsv <path>` (validate the URLs in a 18-02 diff TSV) or default to sampling the live
  BR FrontImage column. Prefix any network invocation guidance in console output. Write
  `tests/scripts/check-br-image-urls.test.ts` covering each classify case against mocked
  response objects (no real network in the unit test).</action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/scripts/check-br-image-urls.test.ts</automated>
  </verify>
  <done>classifyHeadResponse returns the correct label for image/html/403; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Real dry-run + brand-leak audit + DirectSide=LeftSide sanity</name>
  <files>scripts/link-br-images.ts</files>
  <action>Run the linker in dry-run against the live sheet:
  `NODE_OPTIONS=--use-system-ca npx tsx scripts/link-br-images.ts --dry-run` (or scope with
  `--supplier` for a first pass). Inspect the generated `tmp/link-br-images-plan-<ts>.tsv`:
  (a) grep the diff for Q-Tees / H08050 rows and any other hyphenated-brand pids and confirm
  the parsed color column has NO brand-token leak (IMG-03/D-07); (b) confirm the stats show a
  plausible overwritten_changed + written_new split and a miss count consistent with the
  452/452 Drive completion (PITFALLS P2 warning sign: misses far higher than expected ⇒ join
  bug); (c) DirectSideImage=LeftSide sanity (STATE gate): for ~5 pids, confirm the existing
  DirectSideImage cell currently holds a LEFT-side image before accepting the overwrite — if
  any hold right-side images, STOP and flag the mapping as inverted. Do NOT apply yet. This
  task makes no code change unless the audit surfaces a parser/join defect, in which case file
  a gap against 18-01/18-02 rather than patching here.</action>
  <verify>
    <automated>npx tsx scripts/link-br-images.ts --dry-run --supplier __NONE__</automated>
    <human-check>Operator has opened tmp/link-br-images-plan-<ts>.tsv, confirmed zero brand-leak in hyphenated-brand color tokens, confirmed the miss count is plausible, and confirmed DirectSideImage holds left-side URLs for the spot-checked pids.</human-check>
  </verify>
  <done>Dry-run TSV reviewed; no brand leak; miss count plausible; LeftSide↔DirectSideImage mapping confirmed non-inverted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The deterministic Drive→BR image linker (18-01 parser + 18-02 linker + this plan's URL validator). A reviewed dry-run diff exists; nothing has been written to the live sheet yet.</what-built>
  <how-to-verify>
    1. Review `tmp/link-br-images-plan-<ts>.tsv` (the dry-run diff) and `tmp/link-br-images-misses-<ts>.tsv` one more time — confirm the old→new changes are what you expect and no brand-name tokens appear in any color.
    2. Approve the live overwrite. The executor will then run:
       `NODE_OPTIONS=--use-system-ca npx tsx scripts/link-br-images.ts --apply`
       which writes `tmp/br-image-backup-<ts>.tsv` FIRST, then overwrites the 7 image columns and adds the 4 new columns.
    3. After apply, the executor runs the URL sampler:
       `NODE_OPTIONS=--use-system-ca npx tsx scripts/check-br-image-urls.ts` (10 random URLs) — confirm all return image/*; any 403/HTML means a Drive permission gap to repair before sign-off (P7).
    4. The executor runs `--apply` a SECOND time — confirm the summary reports written_new=0 and overwritten_changed=0 (idempotent, OPS-02/D-11).
    5. Open the Bestsellers-Ready sheet: confirm the 4 new headers (RightSide, ModelFront, ModelSide, ModelBack) exist in the correct positions with data aligned under them (no header drift, OPS-03), and a few image cells render in-browser.
  </how-to-verify>
  <resume-signal>Type "approved" after the backup is written, --apply succeeds, the URL sample is all image/*, the second --apply is a no-op, and headers/data are aligned. Otherwise describe the issue (e.g. brand leak, 403 URLs, header drift, non-zero second-run changes).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator approval → live --apply | The only point a destructive write to the shared 24k-row sheet occurs; gated behind reviewed dry-run + blocking checkpoint |
| Drive URL → customer browser | A written URL that 403s or returns HTML is a broken storefront image; HEAD sample catches it before sign-off |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Information disclosure | written URL returns Drive permission HTML instead of image | mitigate | check-br-image-urls.ts HEAD sample classifies image/html/403; 403 ⇒ repair permission before sign-off (P7) |
| T-18-09 | Tampering | inverted LeftSide↔DirectSideImage overwrite | mitigate | Task 2 spot-checks existing DirectSideImage holds left-side URLs before apply (STATE gate) |
| T-18-10 | Tampering | re-run drift / oscillation on second apply | mitigate | checkpoint step 4 asserts second --apply is a zero-change no-op (OPS-02/D-11) |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new packages — built-in fetch/https + existing deps; no install task |
</threat_model>

<verification>
- `npx vitest run tests/scripts/check-br-image-urls.test.ts` green.
- Dry-run TSV exists and is brand-leak-free for hyphenated-brand pids.
- Post-apply: URL HEAD sample is 100% image/*; any non-image flagged and resolved.
- Second --apply: written_new=0, overwritten_changed=0.
- BR sheet: 4 new headers present and aligned; miss TSV lists Drive-absent pairs with cells preserved.
</verification>

<success_criteria>
- All five Phase 18 ROADMAP success criteria demonstrably TRUE (dry-run-diff, apply-overwrite, never-blank-miss, no-brand-leak, idempotent second run).
- Canonical URL form confirmed to render (image/* on HEAD sample) — open verification item closed.
- Operator sign-off recorded via the blocking checkpoint.
</success_criteria>

<artifacts_this_phase_produces>
**New files:** `scripts/check-br-image-urls.ts`, `tests/scripts/check-br-image-urls.test.ts`
**New symbols:** `classifyHeadResponse(...)`, `main()` (URL sampler)
**Live state change (on approved --apply):** BR `FrontImage`, `BackImage`, `DirectSideImage`, `RightSide`, `ModelFront`, `ModelSide`, `ModelBack` populated with canonical Drive URLs; 4 new columns added.
**New tmp/ artifacts:** `tmp/br-image-backup-<ts>.tsv` (pre-apply backup, written by the linker on --apply).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/18-drive-to-br-image-linker/18-03-SUMMARY.md` when done
</output>
