---
phase: 16-catalog-image-pollution-audit-fix
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - scripts/audit-image-pollution.ts
  - tests/scripts/audit-image-pollution.test.ts
autonomous: true
requirements: [R1, R2]

must_haves:
  truths:
    - "Running `npx tsx scripts/audit-image-pollution.ts --all` produces tmp/image-pollution-audit-{YYYY-MM-DD}.tsv with one row per polluted pid covering all 6 image columns and 4 pollution classes (shared_url, content_mismatch, model_pollution, shape_drift) plus the recommended invalid_image_format Pass 1 class."
    - "Audit reads BR raw rows via sheets.spreadsheets.values.get + header-index pattern, NOT readAllRows (SheetRow lacks Model* fields per RESEARCH Pitfall 1)."
    - "H08* pids are skipped silently and counted in headwear_skipped (summary + TSV header)."
    - "Audit performs ONLY reads — no writes to BR or Drive. Test 7 static invariant enforces this at compile time."
    - "Pass 1 (structural) executes before any AI call. Pass 2 (content) runs only on Pass-1-clean pids. Pass 3 (shape) runs only on Pass-1+2-clean pids per D-07."
    - "--re-audit flag re-runs the audit and writes a separate dated TSV, used for R10 phase-close verification."
    - "--post-mortem flag prints a class-breakdown of pids in the manual queue (no fix run needed)."
  artifacts:
    - path: "scripts/audit-image-pollution.ts"
      provides: "3-pass image pollution audit CLI"
      exports: ["runImagePollutionAudit", "RunImagePollutionAuditArgs", "RunImagePollutionAuditDeps", "POLLUTION_CLASSES"]
    - path: "tmp/image-pollution-audit-{YYYY-MM-DD}.tsv"
      provides: "Per-pid pollution classification with recommended fix tier"
      contains: "pid\tpollution_class\taffected_columns\taffected_drive_urls\texpected_supplier_url\trecommended_fix_tier\tpass_detected_in\tnotes"
  key_links:
    - from: "scripts/audit-image-pollution.ts"
      to: "src/lib/verify-same-product.ts (Pass 2)"
      via: "verifySameProductFn DI hook"
      pattern: "verifySameProduct"
    - from: "scripts/audit-image-pollution.ts"
      to: "src/lib/ai-image-generator.ts (Pass 3)"
      via: "verifyGarmentTypeMatch (Phase 15 reuse)"
      pattern: "verifyGarmentTypeMatch"
    - from: "scripts/audit-image-pollution.ts"
      to: "src/lib/supplier-canonical.ts"
      via: "resolveSupplierCanonical (Pass 2 canonical fetch)"
      pattern: "resolveSupplierCanonical"
    - from: "scripts/audit-image-pollution.ts"
      to: "src/sheets/drive.ts (getDriveFileMetadata)"
      via: "Pass 1 invalid_image_format detection"
      pattern: "getDriveFileMetadata"
---

<objective>
Build the read-only audit script `scripts/audit-image-pollution.ts`. Three detection passes:
- Pass 1 (structural, no AI): shared-URL detection across pids AND invalid_image_format detection via Drive metadata mime-type.
- Pass 2 (AI content): compare BR FrontImage vs supplier canonical; compare each Model* vs the corresponding garment-only column.
- Pass 3 (AI shape): reuse Phase 15's verifyGarmentTypeMatch on Back/Side vs Front.

Output is `tmp/image-pollution-audit-{YYYY-MM-DD}.tsv`, classified per pollution class, with `recommended_fix_tier` ready for Plan 03 to consume.

Purpose: deterministic + cheap structural detection BEFORE AI calls (D-01, D-02); AI calls cascaded by pass per D-07 to avoid double-flagging; ALL 6 image columns covered including Model* (R2); zero writes (R1 read-only constraint enforced by Test 7 invariant).

Output:
- 1 new CLI script
- 1 new test file with DI-seam coverage of all three passes + read-only invariant
</objective>

<execution_context>
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-catalog-image-pollution-audit-fix/16-SPEC.md
@.planning/phases/16-catalog-image-pollution-audit-fix/16-CONTEXT.md
@.planning/phases/16-catalog-image-pollution-audit-fix/16-RESEARCH.md
@.planning/phases/16-catalog-image-pollution-audit-fix/16-PATTERNS.md
@.planning/phases/16-catalog-image-pollution-audit-fix/16-01-PLAN.md
@scripts/audit-garment-types.ts
@scripts/fetch-ss-images-fixed.ts
@tests/scripts/audit-garment-types.test.ts
@src/sheets/client.ts
@src/sheets/types.ts
@src/sheets/column-map.ts
@src/shopify/image-standardizer.ts

<interfaces>
<!-- From Plan 01 — primitives this script consumes -->
import { appendTrailRow, getOrCreateTrailRunId, TrailRow } from '../src/lib/image-pollution-trail.js';
import { verifySameProduct } from '../src/lib/verify-same-product.js';
import { resolveSupplierCanonical, CanonicalResult } from '../src/lib/supplier-canonical.js';
import { extractFileId, downloadFromDrive, getDriveFileMetadata } from '../src/sheets/drive.js';
import { verifyGarmentTypeMatch } from '../src/lib/ai-image-generator.js';

<!-- Project helpers — DO NOT reimplement -->
import { createSheetsClient } from '../src/sheets/client.js';
import { createDriveClient } from '../src/sheets/drive.js';
import { downloadImage } from '../src/shopify/image-standardizer.js';

<!-- Phase 15 audit script — analog for the entire CLI skeleton -->
// scripts/audit-garment-types.ts:34-307 (DI seam, parseArgs, env validation, chunked --all, isDirectRun guard)

<!-- Raw-row read pattern — scripts/fetch-ss-images-fixed.ts:72-86 -->
const resp = await sheets.spreadsheets.values.get({ spreadsheetId, range: `'Bestsellers-Ready'` });
const rows = (resp.data.values ?? []) as string[][];
const h: Record<string, number> = {};
rows[0].forEach((x, i) => { h[x] = i; });
const pidIdx = h['productId'];
const frontIdx = h['FrontImage'];
const backIdx = h['BackImage'];
const sideIdx = h['DirectSideImage'];
const modelFIdx = h['ModelFrontImage'];
const modelSIdx = h['ModelSideImage'];
const modelBIdx = h['ModelBackImage'];

<!-- Allowed image mime types for Pass 1 invalid_image_format -->
const VALID_IMAGE_MIMES = new Set(['image/png', 'image/jpeg', 'image/jpg', 'image/webp', 'image/gif']);
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BR sheet → audit script | Sheet contents arbitrary, including non-Drive URLs and empty cells. |
| LLM verifier → TSV | gpt-4o-mini reason flows into the audit TSV via sanitize (Plan 01 lib). |
| Drive metadata → mime-type classifier | Drive API returns mimeType strings — bounded but case-sensitive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | T (Tampering) — R8 safety rail foundation | audit-image-pollution.ts | mitigate | Static read-only invariant test (Test 7 in tests/scripts/audit-image-pollution.test.ts) parses the source file and asserts NO `uploadToDrive`, `writeUpdates`, `trashDriveFile`, no import from `'../src/sheets/writer.js'`. Enforces that audit cannot delete or write — only flag. |
| T-16-02 | T — verifier false-pass | audit-image-pollution.ts (Pass 2, 3) | mitigate | Audit logs each Pass-2/3 verifier call as VERIFIER_PASS or VERIFIER_FAIL row in trail TSV via appendTrailRow with tier=0. Post-mortem possible by inspecting trail. |
| T-16-05 | D (DoS) — cost spike + Drive quota | audit-image-pollution.ts | mitigate | D-07: Pass 3 only runs on Pass-1+2-clean pids (avoids double-flagging + reduces Vision calls). D-22: H08* skipped before any AI call. --limit flag caps run size for dev iterations. Pass 1 is free (no AI). Budget per D-23: ~$0.65 / ~50min. Drive metadata calls in Pass 1 are batched at <=10 req/sec (token-bucket; --max-drive-rps tunable) to respect Drive's default per-user quota (100 requests per 100 seconds). 403/429 errors trigger exponential backoff with jitter (max 5 retries) and fall back to skip-with-trail-log on persistent failure. |
| T-16-03 | R (Repudiation) — verifier false-fail | audit-image-pollution.ts (Pass 2, 3) | mitigate | Every VERIFIER_FAIL flagged item appears in audit TSV with `notes` = verifier's `reason` string. Operator can post-mortem and rerun individual pids with `--pid X`. Post-fix re-audit (`--re-audit`) catches false-fail bleed-through. |
| T-16-06 | T (TSV injection) | audit TSV writer | mitigate | TSV writes flow through appendTrailRow (Plan 01); sanitize already applied. The audit-specific TSV (image-pollution-audit-*.tsv) MUST use the same sanitize on operator-or-LLM-controlled fields (pollution_class is enum-bounded; notes is LLM-controlled). |

**Block on:** T-16-04 (read-only invariant test MUST exist and pass before any merge).
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit script skeleton + Pass 1 (structural)</name>
  <files>
    scripts/audit-image-pollution.ts,
    tests/scripts/audit-image-pollution.test.ts
  </files>
  <read_first>
    - scripts/audit-garment-types.ts (entire file — CLI skeleton analog: DI seam, parseArgs, env validation, isDirectRun guard, dedup pattern)
    - scripts/fetch-ss-images-fixed.ts (lines 72-86 — raw-row read with header-index pattern Phase 16 MUST use)
    - src/sheets/types.ts (verify SheetRow does NOT include Model* — confirms raw-read is mandatory)
    - tests/scripts/audit-garment-types.test.ts (lines 177-214 — Test 7 read-only static invariant; copy verbatim with renamed symbol checks)
    - .planning/phases/16-catalog-image-pollution-audit-fix/16-PATTERNS.md (section "scripts/audit-image-pollution.ts" + "Pattern 2 — Read-only invariant test")
    - .planning/phases/16-catalog-image-pollution-audit-fix/16-RESEARCH.md (Pass 1 algorithm, lines starting near "Pass 1 — Structural detection")
  </read_first>
  <behavior>
    DI seam + skeleton:
    - Test 1 (DI seam): `runImagePollutionAudit(mockDeps)` accepts injected dependencies and returns `{ pidsScanned, pidsPolluted, classCounts, headwearSkipped }`.
    - Test 2 (env validation): main() exits 1 when SS_ACCOUNT_NUMBER missing.
    - Test 3 (parseArgs): supports `--all`, `--pid <id>`, `--dry-run`, `--re-audit`, `--post-mortem`, `--limit N`, `--help/-h`.

    Pass 1 (structural):
    - Test 4 (shared-URL detection): given mock rows where pids 8882, 5200, CE520L all have FrontImage URL containing fileId `1abc...zk`, audit emits 3 rows with `pollution_class=shared_url`, `affected_columns=FrontImage`, `affected_drive_urls=<the shared url>`, `notes` listing the collision count (e.g., "shared with 5200, CE520L").
    - Test 5 (shared-URL — Model column): given mock rows where pid 1234.ModelFrontImage shares fileId with pid 5678.FrontImage, BOTH rows emitted with `pollution_class=shared_url`.
    - Test 6 (invalid_image_format): given mocked `getDriveFileMetadata` returning `{mimeType: 'application/pdf', ...}` for one Drive URL, audit emits row with `pollution_class=invalid_image_format`, `affected_columns=<the column>`, `notes=non-image mime: application/pdf`.
    - Test 6b (rate-limit backoff): mocked `getDriveFileMetadataFn` rejects with `{code: 403, errors: [{reason: 'quotaExceeded'}]}` twice then resolves successfully. Audit retries with backoff and eventually succeeds. Assert call count >= 3 and audit completes without crashing.
    - Test 7 (READ-ONLY INVARIANT — STATIC): parse `scripts/audit-image-pollution.ts` source. Assert: no import from `'../src/sheets/writer'`. No occurrence of `uploadToDrive`, `writeUpdates`, `trashDriveFile` symbols anywhere outside a leading safety comment block (the first 20 lines may mention these names in a "MUST NEVER call" disclaimer; everything below is forbidden). Reuse the pattern from `tests/scripts/audit-garment-types.test.ts:177-214` (lines 177-214) — copy that test verbatim and swap symbol names + import path.
    - Test 8 (H08* exclusion in Pass 1): mock row with pid='H08010' shared with non-headwear → row is NOT emitted; `headwearSkipped` counter incremented by 1 (not by 6 — once per pid, not per column).
  </behavior>
  <action>
    Create `scripts/audit-image-pollution.ts` by COPYING the structure of `scripts/audit-garment-types.ts` (entire file) and adapting.

    1. Top-of-file safety comment (mandatory — Test 7 looks for this disclaimer):
       ```
       // READ-ONLY 3-pass image pollution audit for Phase 16 (SPEC R1, R2; CONTEXT D-01 through D-09).
       // MUST NEVER call uploadToDrive, writeUpdates, trashDriveFile, or modify Drive/Sheets.
       // Only output: tmp/image-pollution-audit-{YYYY-MM-DD}.tsv + trail rows (tier=0 audit-side events).
       // Mirrors scripts/audit-garment-types.ts skeleton; extends with raw-row reader for Model* columns.
       ```

    2. Imports — bring in the Plan 01 primitives + Phase 15 verifier + drive client. Do NOT import from `src/sheets/writer.js` (read-only invariant).

    3. CLI types (extend the DI seam):
       ```
       export interface RunImagePollutionAuditArgs {
         all: boolean;
         pid?: string;
         dryRun: boolean;
         reAudit: boolean;
         postMortem: boolean;
         limit?: number;
         help: boolean;
       }

       export interface RunImagePollutionAuditDeps {
         args: RunImagePollutionAuditArgs;
         sheetsClient: sheets_v4.Sheets;
         driveClient: drive_v3.Drive;
         openai: OpenAI;
         readRawRowsFn: (sheets: sheets_v4.Sheets, spreadsheetId: string, tab: string) => Promise<string[][]>;
         downloadImageFn: typeof downloadImage;
         downloadFromDriveFn: typeof downloadFromDrive;
         getDriveFileMetadataFn: typeof getDriveFileMetadata;
         verifyShapeFn: typeof verifyGarmentTypeMatch;
         verifySameProductFn: typeof verifySameProduct;
         supplierCanonicalFn: typeof resolveSupplierCanonical;
         appendTrailRowFn: typeof appendTrailRow;
         spreadsheetId: string;
         sheetName: string; // 'Bestsellers-Ready' for Phase 16
       }
       ```

    4. Pollution class enum (export so tests can reference):
       ```
       export const POLLUTION_CLASSES = ['shared_url', 'invalid_image_format', 'content_mismatch', 'model_pollution', 'shape_drift'] as const;
       export type PollutionClass = typeof POLLUTION_CLASSES[number];
       ```

    5. `readRawRows` default impl (top of file): wraps `sheets.spreadsheets.values.get` per `scripts/fetch-ss-images-fixed.ts:72-86`. Returns `string[][]` (header row + data rows).
       Add a code comment ABOVE the call: `// Cannot use readAllRows — SheetRow (src/sheets/types.ts) lacks Model* columns. See RESEARCH Pitfall 1 + PATTERNS Option B.`

    6. `runImagePollutionAudit(deps)` skeleton (Task 1 implements Pass 1 only; later tasks fill Pass 2 + 3):
       - Initialize counters: `pidsScanned=0`, `pidsPolluted=0`, `headwearSkipped=0`, `classCounts: Record<PollutionClass, number> = {shared_url:0, invalid_image_format:0, content_mismatch:0, model_pollution:0, shape_drift:0}`.
       - Read all BR rows via `deps.readRawRowsFn(deps.sheetsClient, deps.spreadsheetId, deps.sheetName)`.
       - Build header-index map for the 6 image columns + productId.
       - Dedupe by `productId` (one entry per unique pid).
       - For each unique pid, IF `/^H08/i.test(pid)`: `headwearSkipped++`, skip. ELSE add to `targetPids`.
       - Run `pass1Structural(targetPids, deps)` returning `Map<pid, PollutionRow[]>`. Implementation:
         * Build `fileIdToPids: Map<string, Set<{pid, columns}>>` by iterating every (pid, column) in BR and calling `extractFileId(url)`.
         * Any fileId with `pidSet.size > 1` → emit one row per pid with `pollution_class='shared_url'`, `affected_columns=<comma-separated columns>`, `affected_drive_urls=<url>`, `recommended_fix_tier=<1 if resolveSupplierCanonical exists, else 3>`, `pass_detected_in=1`, `notes=shared with: <other pids>`.
         * For each (pid, column) with a Drive URL, call `getDriveFileMetadataFn(driveClient, fileId)`. If `!VALID_IMAGE_MIMES.has(mimeType)` → emit row with `pollution_class='invalid_image_format'`, `recommended_fix_tier=1 (if canonical) else 3`, `notes=non-image mime: <mimeType>`.
       - After Pass 1, write the audit TSV via a local helper `writeAuditRow(row)` that appends to `tmp/image-pollution-audit-${YYYY-MM-DD}.tsv` (write header on first row; sanitize LLM/operator strings using the same `/[\t\n\r]+/g → ' '` pattern from Plan 01).
       - Log each VERIFIER_PASS / VERIFIER_FAIL to trail via `appendTrailRowFn` with `tier=0`. (Pass 1 doesn't call verifier, but the audit-side `appendTrailRow` for completeness logs `notes` like "pass1 shared_url collision detected" on each polluted pid as a tier=0 event.)
       - Per-pid try/catch fall-through (copy from `audit-garment-types.ts:158-161`).
       - Return summary `{pidsScanned, pidsPolluted, classCounts, headwearSkipped}`.

    7. `main()` entry:
       - parseArgs with the 6 flags above.
       - Env validation (`OPENAI_API_KEY`, `GOOGLE_SPREADSHEET_ID`, `GOOGLE_SERVICE_ACCOUNT_EMAIL`, `GOOGLE_PRIVATE_KEY`, `SS_ACCOUNT_NUMBER`, `SS_API_KEY`). Mirror `audit-garment-types.ts:249-259`.
       - Construct concrete deps (createSheetsClient, createDriveClient, new OpenAI). Use `sheetName: 'Bestsellers-Ready'` (NOT Sheet1 — Phase 16 scope).
       - Call `runImagePollutionAudit(deps)`.
       - Print summary (D-09 header format): `# audit_run_id`, `# pids_scanned`, `# pids_polluted`, `# class_counts: shared_url=X invalid_image_format=X content_mismatch=X model_pollution=X shape_drift=X`, `# headwear_skipped_count`.

    9. Drive metadata rate-limit gate (revision iteration 1, WARNING 3):
       - Wrap `getDriveFileMetadataFn` in a rate-limited helper at module scope. Default ceiling: 10 requests/second (Drive's default per-user quota is 100 requests per 100 seconds; 10 req/sec stays under).
       - Implement as a simple token-bucket or interval-spaced queue (e.g., `await sleep(Math.max(0, 100 - (now - lastCall)))` before each call).
       - On error code 403 with `quotaExceeded` reason, OR code 429: exponential backoff with jitter (start 1s, double up to 30s, max 5 retries). After max retries, log the failure to trail with `notes='drive_quota_exceeded'` and skip the metadata check for that URL (do NOT crash the whole audit).
       - Add CLI flag `--max-drive-rps N` (default 10) to tune the ceiling.
       - Total Pass 1 Drive metadata calls per audit: ~2,580 (430 pids x 6 columns). At 10 req/sec sustained, Pass 1 metadata stage runs in ~258 seconds.

        8. isDirectRun guard: `const isDirectRun = process.argv[1]?.replace(/\\/g, '/').includes('audit-image-pollution');`

    Create `tests/scripts/audit-image-pollution.test.ts` mirroring `tests/scripts/audit-garment-types.test.ts` structure:
    - Build a mock `RunImagePollutionAuditDeps` with vi.fn for every function dep.
    - For Pass 1 tests, drive `readRawRowsFn` to return synthetic header + data rows.
    - For Test 7 (READ-ONLY INVARIANT), copy `tests/scripts/audit-garment-types.test.ts:177-214` verbatim, swap symbol names: assert no `uploadToDrive`, no `writeUpdates`, no `trashDriveFile`, no `from '../src/sheets/writer'`. ALLOW these symbol mentions inside the first 20 lines of the file (the safety-comment disclaimer).

    Critical scope note for Task 1: only Pass 1 is implemented. Task 2 will fill Pass 2 + 3 + TSV emit for those rows. Tests for Pass 2 + 3 are NOT in Task 1.
  </action>
  <verify>
    <automated>npx vitest run tests/scripts/audit-image-pollution.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f scripts/audit-image-pollution.ts`.
    - `grep -c "^export " scripts/audit-image-pollution.ts` >= 4 (runImagePollutionAudit, RunImagePollutionAuditArgs, RunImagePollutionAuditDeps, POLLUTION_CLASSES).
    - `grep -nE "MUST NEVER call uploadToDrive" scripts/audit-image-pollution.ts` matches one line (top-of-file safety disclaimer; Test 7 allows it ONLY in the first 20 lines).
    - `tail -n +21 scripts/audit-image-pollution.ts | grep -cE "(uploadToDrive|writeUpdates|trashDriveFile)" | grep -v '^#'` returns 0 (no write symbols below the disclaimer).
    - `grep -nE "from '../src/sheets/writer'" scripts/audit-image-pollution.ts` returns no matches.
    - `grep -nE "VALID_IMAGE_MIMES" scripts/audit-image-pollution.ts` matches at least one line.
    - `grep -nE "extractFileId\\(" scripts/audit-image-pollution.ts` matches at least one line.
    - `grep -nE "/\\^H08/i" scripts/audit-image-pollution.ts` matches at least one line (headwear exclusion).
    - `grep -nE "max-drive-rps|maxDriveRps" scripts/audit-image-pollution.ts` matches at least one line (Drive rate-limit CLI flag).
    - `grep -nE "quotaExceeded|drive_quota" scripts/audit-image-pollution.ts` matches at least one line (Drive 403/429 backoff handler).
    - `npx vitest run tests/scripts/audit-image-pollution.test.ts` reports >= 8 tests passing (Tests 1–8 above).
  </acceptance_criteria>
  <done>
    Pass 1 detection complete: shared_url + invalid_image_format detected and written to audit TSV. CLI skeleton with DI seam + env validation in place. Read-only static invariant (Test 7) GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pass 2 (content) + Pass 3 (shape) + summary</name>
  <files>
    scripts/audit-image-pollution.ts,
    tests/scripts/audit-image-pollution.test.ts
  </files>
  <read_first>
    - scripts/audit-image-pollution.ts (current state after Task 1)
    - tests/scripts/audit-image-pollution.test.ts (current state after Task 1)
    - src/lib/verify-same-product.ts (Plan 01 — verifier signature)
    - src/lib/ai-image-generator.ts (lines 129-181 — verifyGarmentTypeMatch — Phase 15 reuse for Pass 3)
    - src/lib/supplier-canonical.ts (Plan 01 — resolveSupplierCanonical signature)
    - .planning/phases/16-catalog-image-pollution-audit-fix/16-RESEARCH.md (sections "Pass 2 — AI content verification" + "Pass 3 — AI shape verification")
    - .planning/phases/16-catalog-image-pollution-audit-fix/16-CONTEXT.md (D-03, D-04, D-05, D-06, D-07)
  </read_first>
  <behavior>
    Pass 2 (AI content):
    - Test 9 (D-03 front check): pid 6110 with `colorFrontImage` mock returning baby-onesie URL via canonical resolver; mocked verifySameProductFn returns `{match: false, reason: 'candidate is baby onesie, reference is adult t-shirt'}` → audit emits row with `pollution_class=content_mismatch`, `affected_columns=FrontImage`, `recommended_fix_tier=1`, `notes` includes the verifier reason.
    - Test 10 (D-04 dispatch): supplier-canonical mocked to return `null` for pid 'XYZ999' (no supplier mapped) → audit emits row with `notes` including 'no_canonical_available' AND does NOT call verifySameProductFn for that pid's Pass 2 (cannot verify without canonical) → Pass 3 still runs.
    - Test 11 (D-05 Model* check): pid 8882 with ModelFrontImage; mocked verifySameProductFn returns `{match: false, reason: '...A2009 hoodie...'}` when comparing ModelFrontImage vs FrontImage → audit emits row with `pollution_class=model_pollution`, `affected_columns=ModelFrontImage`, `recommended_fix_tier=1`.

    Pass 3 (AI shape):
    - Test 12 (D-06 shape-drift): pid L00100 BackImage; mocked verifyShapeFn returns `{match: false, reason: 'crewneck vs hoodie'}` → audit emits row with `pollution_class=shape_drift`, `affected_columns=BackImage`, `recommended_fix_tier=2`.
    - Test 13 (D-07 pass-3-only-on-clean): pid 6110 emitted by Pass 2 (content_mismatch on FrontImage) → Pass 3 does NOT call verifyShapeFn on that pid (asserted via vi.fn call count).

    Trail logging:
    - Test 14 (trail rows): every VERIFIER_PASS and VERIFIER_FAIL goes through appendTrailRowFn with `tier=0`. Assert call counts and rows for a small input.

    Summary:
    - Test 15 (D-09 summary): after audit, console.log output (captured via vi.spyOn(console, 'log')) includes `audit_run_id`, `pids_scanned`, `pids_polluted`, `class_counts`, `headwear_skipped_count`. The audit TSV's first row is a `# audit_run_id=...` header line followed by the column header.
  </behavior>
  <action>
    Extend `scripts/audit-image-pollution.ts` from Task 1. Add functions:

    1. `pass2Content(targetPids, pass1Polluted, deps)`:
       For each pid NOT in pass1Polluted:
       a. Call `deps.supplierCanonicalFn(pid)`. If null, EMIT a `notes='no_canonical_available'` audit row (no class change — purely informational; this pid will still go through Pass 3) and CONTINUE to Pass 3. Do NOT count as polluted.
       b. If canonical found: download canonical via `deps.downloadImageFn(canonical.url)`; download BR FrontImage via `deps.downloadFromDriveFn(deps.driveClient, extractFileId(brFront)!)`.
       c. Call `deps.verifySameProductFn(deps.openai, brFrontBuf, canonicalBuf)`. Log VERIFIER_PASS or VERIFIER_FAIL to trail (tier=0).
       d. If `result.match === false`: emit audit row `pollution_class=content_mismatch`, `affected_columns=FrontImage`, `affected_drive_urls=<br front>`, `expected_supplier_url=canonical.url`, `recommended_fix_tier=1`, `pass_detected_in=2`, `notes=<verifier reason>`.
       e. For each Model* column (ModelFrontImage, ModelSideImage, ModelBackImage) with a Drive URL: download the model image; compare to BR FrontImage via `verifySameProductFn`. If match=false → emit row `pollution_class=model_pollution`, `affected_columns=<the model column>`, `recommended_fix_tier=1`.

    2. `pass3Shape(targetPids, pass1Polluted, pass2Polluted, deps)`:
       For each pid NOT in pass1Polluted AND NOT in pass2Polluted (per D-07):
       a. Download BR FrontImage via `downloadFromDriveFn`.
       b. For each of BackImage and DirectSideImage with a valid Drive URL: download via `downloadFromDriveFn`; call `deps.verifyShapeFn(deps.openai, viewBuf, frontBuf)`.
       c. If `match === false`: emit audit row `pollution_class=shape_drift`, `affected_columns=<BackImage or DirectSideImage>`, `recommended_fix_tier=2`, `pass_detected_in=3`, `notes=<reason>`.
       d. Wrap per-pid try/catch — VERIFIER errors fall through without inflating mismatch counts (mirror `audit-garment-types.ts:158-161`).

    3. Wire Pass 2 + Pass 3 into `runImagePollutionAudit`:
       ```
       const pass1Set = new Set(pass1Rows.map(r => r.pid));
       const pass2Rows = await pass2Content(targetPids, pass1Set, deps);
       const pass2Set = new Set(pass2Rows.map(r => r.pid));
       const pass3Rows = await pass3Shape(targetPids, pass1Set, pass2Set, deps);
       const allRows = [...pass1Rows, ...pass2Rows, ...pass3Rows];
       writeAuditTSV(allRows);
       ```

    4. Audit TSV header (D-09 format — written ONCE on first run-id):
       ```
       # audit_run_id=<iso>
       # pids_scanned=<N>
       # pids_polluted=<N>
       # class_counts: shared_url=<N> invalid_image_format=<N> content_mismatch=<N> model_pollution=<N> shape_drift=<N>
       # headwear_skipped_count=<N>
       pid	pollution_class	affected_columns	affected_drive_urls	expected_supplier_url	recommended_fix_tier	pass_detected_in	notes
       ```
       Note: write the header AFTER all passes complete (we need the final counts), to a new file `tmp/image-pollution-audit-${date}.tsv`. If file exists for today, OVERWRITE (audit is idempotent per pid — re-running produces same TSV; D-15 idempotency).

    5. `--re-audit` flag: same flow, but file name suffix `-reaudit` to avoid overwriting the original: `tmp/image-pollution-audit-${date}-reaudit.tsv`. The summary at end should print: `RE-AUDIT result: <N> pids still polluted` for R10 visibility.

    6. `--post-mortem` flag: read the latest `tmp/image-pollution-manual-queue-*.tsv` (Plan 03's output) and the trail TSV; print a class-breakdown of pids in the queue (how many shared_url, content_mismatch, etc.) WITHOUT running any audit pass. Exit 0. Used by operator after a BLOCKED-QUEUE-OVERFLOW.

    7. `--dry-run`: like Phase 15 — print target pid counts + which pids would receive a verifier call for which Pass, WITHOUT calling Vision or downloading any image. Useful for budget sanity-check.

    8. `--pid <pid>`: single-pid audit; bypasses chunked --all reader. Mirror `audit-garment-types.ts:165-181` filter pattern, but on the raw rows (no SheetRow).

    Extend the test file with Tests 9–15 above. For Pass 2 tests, mock the entire dependency chain — sheets reader, drive download, supplier-canonical, verifier — and assert correct row emission + correct verifier call counts.
  </action>
  <verify>
    <automated>npx vitest run tests/scripts/audit-image-pollution.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "function pass2Content|async function pass2Content" scripts/audit-image-pollution.ts` matches one line.
    - `grep -nE "function pass3Shape|async function pass3Shape" scripts/audit-image-pollution.ts` matches one line.
    - `grep -nE "no_canonical_available" scripts/audit-image-pollution.ts` matches at least one line (D-04 case).
    - `grep -nE "pollution_class=model_pollution|'model_pollution'" scripts/audit-image-pollution.ts` matches at least one line.
    - `grep -nE "pollution_class=shape_drift|'shape_drift'" scripts/audit-image-pollution.ts` matches at least one line.
    - `grep -nE "# audit_run_id" scripts/audit-image-pollution.ts` matches at least one line (D-09 header format).
    - `grep -nE "re-audit|reaudit" scripts/audit-image-pollution.ts` matches at least one line.
    - `grep -nE "post-mortem|postMortem" scripts/audit-image-pollution.ts` matches at least one line.
    - `tail -n +21 scripts/audit-image-pollution.ts | grep -cE "(uploadToDrive|writeUpdates|trashDriveFile)"` returns 0 (still read-only).
    - `npx vitest run tests/scripts/audit-image-pollution.test.ts` reports >= 15 tests passing total (Task 1's 8 + Task 2's 7).
  </acceptance_criteria>
  <done>
    All 3 passes work; D-07 ordering enforced (Pass 3 skips Pass-1/2-polluted pids); D-09 summary header in TSV; --re-audit + --post-mortem + --dry-run flags functional; read-only invariant still GREEN.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `npx vitest run tests/scripts/audit-image-pollution.test.ts` reports >= 15 tests green (DI seam + 3 passes + read-only invariant).
- `npx vitest run` (full suite) shows no regressions in Phase 15's `tests/scripts/audit-garment-types.test.ts`.
- Manual smoke test (operator-only — NOT part of test suite): `npx tsx scripts/audit-image-pollution.ts --pid 6110 --dry-run` prints the pid's pass plan without writing TSV or calling Vision.
</verification>

<success_criteria>
- 3-pass detection implemented and tested with mocked deps.
- All 6 image columns covered (Front, Back, DirectSide, ModelFront, ModelSide, ModelBack) — verified by Test 5 + Test 11.
- Pass 1 catches shared_url (8882 fixture) + invalid_image_format (mocked PDF mime) without AI calls.
- Pass 2 catches content_mismatch (6110 baby onesie fixture) + model_pollution (8882 A2009 model fixture).
- Pass 3 catches shape_drift (Phase 15 verifier reuse).
- H08* skipped silently with counter.
- D-07 enforced: Pass 3 skips pids already polluted by Pass 1 or 2.
- D-09 audit TSV summary header present.
- Read-only invariant (Test 7) PASSES — no writeUpdates/uploadToDrive/trashDriveFile imports below safety disclaimer.
- --re-audit, --post-mortem, --dry-run, --pid, --limit, --all all parse correctly.
- No regressions in tests/scripts/audit-garment-types.test.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/16-catalog-image-pollution-audit-fix/16-02-SUMMARY.md` capturing: total lines of scripts/audit-image-pollution.ts, test counts (broken down by pass), any pids that fall into `no_canonical_available` during dev smoke tests, and the audit TSV header format actually produced.
</output>
