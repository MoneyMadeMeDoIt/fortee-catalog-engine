---
phase: 01-supplier-data-extraction
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/suppliers/ss-canada.ts
  - src/lib/queue.ts
  - tests/suppliers/ss-canada.test.ts
autonomous: true
requirements:
  - SUPP-02
user_setup:
  - service: ss-activewear
    why: "S&S Canada REST API requires authentication"
    env_vars:
      - name: SS_ACCOUNT_NUMBER
        source: "S&S Activewear account settings page"
      - name: SS_API_KEY
        source: "S&S Activewear account settings page -> API section"
must_haves:
  truths:
    - "Running the S&S Canada extractor returns products with title, images, variants (color+size+pricing), fabric composition parsed from HTML description, and size chart measurements"
    - "Three API endpoints (styles, products, specs) are merged by styleID into a single SupplierProduct"
    - "Rate limiting enforces 55 requests per 60 seconds to stay under the 60 req/min S&S cap"
    - "Missing or invalid S&S credentials produce a clear error message before any API calls"
  artifacts:
    - path: "src/suppliers/ss-canada.ts"
      provides: "S&S Canada adapter implementing SupplierAdapter"
      exports: ["SSCanadaAdapter", "createSSClient", "extractSSProduct", "parseFabricFromDescription"]
    - path: "src/lib/queue.ts"
      provides: "Configured p-queue for S&S rate limiting"
      exports: ["createRateLimitedQueue"]
    - path: "tests/suppliers/ss-canada.test.ts"
      provides: "Unit tests for S&S extraction, merging, and fabric parsing"
  key_links:
    - from: "src/suppliers/ss-canada.ts"
      to: "src/suppliers/types.ts"
      via: "implements SupplierAdapter, returns SupplierProduct[]"
      pattern: "import.*SupplierAdapter.*from.*types"
    - from: "src/suppliers/ss-canada.ts"
      to: "src/lib/queue.ts"
      via: "uses rate-limited queue for API calls"
      pattern: "import.*createRateLimitedQueue.*from.*queue"
    - from: "src/suppliers/ss-canada.ts"
      to: "https://api.ssactivewear.com/v2"
      via: "Basic Auth fetch with rate limiting"
      pattern: "api\\.ssactivewear\\.com"
---

<objective>
Build the S&S Canada product extractor that authenticates with their REST API, fetches data from three endpoints (Styles, Products, Specs), merges them by styleID, parses fabric composition from HTML descriptions, and returns normalized SupplierProduct objects with rate limiting.

Purpose: Fulfills SUPP-02 -- fetching product data from S&S Canada via their official REST API.
Output: Working S&S adapter with passing unit tests using fixture data, rate-limited API client.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-supplier-data-extraction/01-RESEARCH.md
@.planning/phases/01-supplier-data-extraction/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- types the executor must use directly -->

From src/suppliers/types.ts (created in Plan 01):
```typescript
export interface ProductImage { url: string; alt?: string; }
export interface SupplierVariant { color: string; size: string; sku?: string; price?: number; colorSwatchImage?: string; colorFrontImage?: string; }
export interface SizeSpec { sizeName: string; specName: string; value: string; }

export interface SupplierProduct {
  styleNumber: string;
  supplier: 'canada-sportswear' | 'ss-canada';
  title: string;
  description: string;
  category: string;
  fabricComposition: string;
  sizeChartUrl: string | null;
  sizeChartData: SizeSpec[] | null;
  images: ProductImage[];
  variants: SupplierVariant[];
  rawData: unknown;
}

export interface SupplierAdapter {
  readonly supplier: 'canada-sportswear' | 'ss-canada';
  fetchProducts(): Promise<SupplierProduct[]>;
  fetchProduct(identifier: string): Promise<SupplierProduct>;
}

export function validateProduct(product: unknown): ValidationResult;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rate-limited queue utility</name>
  <files>src/lib/queue.ts</files>
  <action>
Create `src/lib/queue.ts`:
- Import PQueue from 'p-queue' (ESM import).
- Export `createRateLimitedQueue(options?: { intervalCap?: number; interval?: number })` that returns a configured PQueue instance.
- Default options: `intervalCap: 55, interval: 60_000, carryoverConcurrencyCount: true` (55 req per 60s to leave buffer under S&S 60 req/min limit).
- This is a thin wrapper -- the value is centralizing the rate limit config.
  </action>
  <verify>
    <automated>cd "C:/Users/ugofo/Downloads/Auto add product" && node -e "import('p-queue').then(m => console.log('p-queue ESM import OK'))" 2>&1</automated>
  </verify>
  <done>queue.ts exports createRateLimitedQueue, p-queue imports work with ESM</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement S&S Canada API client and product extractor with tests</name>
  <files>src/suppliers/ss-canada.ts, tests/suppliers/ss-canada.test.ts</files>
  <behavior>
    - parseFabricFromDescription extracts "60% cotton, 40% polyester" from HTML description
    - parseFabricFromDescription returns empty string when description has no fabric pattern
    - parseFabricFromDescription strips HTML tags before parsing
    - mergeSSData correctly combines style + products + specs into SupplierProduct (styleNumber from partNumber, supplier='ss-canada', category from baseCategory)
    - Variants are built from products array: each product SKU = one color+size combo
    - Size chart data is built from specs array: each spec = one measurement
    - Images are deduplicated from products (colorFrontImage) and style (styleImage)
    - createSSClient throws clear error when credentials are missing/empty
  </behavior>
  <action>
1. Write tests FIRST in `tests/suppliers/ss-canada.test.ts` using fixture files from Plan 01:
   - Import fixtures: ss-style-sample.json, ss-products-sample.json, ss-specs-sample.json.
   - Test parseFabricFromDescription with HTML containing "60% cotton, 40% polyester" -- expect extracted string.
   - Test parseFabricFromDescription with empty string -- expect empty string.
   - Test parseFabricFromDescription with HTML containing no percentage patterns -- expect empty string.
   - Test merging style + products + specs: verify styleNumber = partNumber, supplier = 'ss-canada', category = baseCategory, variants length matches products length, sizeChartData length matches specs length.
   - Test variant mapping: color = colorName, size = sizeName, sku from sku field, price from customerPrice (fallback to piecePrice).
   - Test image extraction: unique images only (no duplicates when multiple products share same colorFrontImage).
   - Test createSSClient with empty credentials throws Error with message containing "credentials".

2. Create `src/suppliers/ss-canada.ts`:
   - Import cheerio, dotenv/config, types from types.ts, logger, createRateLimitedQueue.
   - Define local S&S API types: `SSStyle` (styleID, partNumber, title, description, baseCategory, styleImage), `SSProduct` (sku, colorName, sizeName, piecePrice, customerPrice, colorSwatchImage, colorFrontImage, colorName), `SSSpec` (sizeName, specName, value).
   - Export `parseFabricFromDescription(htmlDescription: string): string` -- use cheerio to strip HTML, regex for percentage+material pattern. Return empty string if no match.
   - Export `createSSClient(accountNumber: string, apiKey: string)` -- validate credentials are non-empty (throw if not). Build Basic Auth header using Buffer.from. Create rate-limited queue. Return object with methods: `getStyles()`, `getStyle(id)`, `getProducts(styleId)`, `getSpecs(styleId)`. Each method queues a fetch call through the rate-limited queue. Handle 404 (throw), 429 (throw with rate limit info), other errors (throw with status).
   - Export `extractSSProduct(client, styleId): Promise<SupplierProduct>` -- fetches style, products, specs in parallel via Promise.all. Merges into SupplierProduct. Parses fabric from style description. Builds variants from products. Builds sizeChartData from specs. Deduplicates images (collect unique colorFrontImage URLs from products + styleImage from style).
   - Export `SSCanadaAdapter` class implementing SupplierAdapter. Constructor takes accountNumber and apiKey (reads from process.env if not provided). `fetchProducts()` fetches all styles, then extracts each. `fetchProduct(styleId)` extracts single style.

IMPORTANT: NEVER scrape the S&S website -- only use their REST API (legal risk per research).
IMPORTANT: Load credentials from .env via dotenv. Fail fast with clear message if SS_ACCOUNT_NUMBER or SS_API_KEY are missing.
  </action>
  <verify>
    <automated>cd "C:/Users/ugofo/Downloads/Auto add product" && npx vitest run tests/suppliers/ss-canada.test.ts --reporter=verbose</automated>
  </verify>
  <done>All S&S tests pass. parseFabricFromDescription extracts fabric from HTML. Three-endpoint merge produces correct SupplierProduct. Rate-limited queue configured at 55 req/60s. Missing credentials throw clear error. Variants, images, and size chart data correctly mapped.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/suppliers/ss-canada.test.ts` -- all tests pass
- `src/suppliers/ss-canada.ts` exports SSCanadaAdapter, createSSClient, parseFabricFromDescription
- `src/lib/queue.ts` exports createRateLimitedQueue with 55 req/60s default
- Rate limiting uses p-queue (not hand-rolled setTimeout)
- Credentials loaded from .env, fail fast if missing
- Three endpoints merged correctly by styleID
</verification>

<success_criteria>
- S&S adapter implements SupplierAdapter interface
- HTML description parsing extracts fabric composition
- Three-endpoint merge (styles + products + specs) produces complete SupplierProduct
- Rate limiting configured at 55 req/60s
- All unit tests pass with fixture data
</success_criteria>

<output>
After completion, create `.planning/phases/01-supplier-data-extraction/01-03-SUMMARY.md`
</output>
