---
phase: 04.1-image-standardization-print-area-detection
plan: 02
subsystem: shopify
tags: [sharp, image-processing, print-area-coords, product-push, garment-detection]

# Dependency graph
requires:
  - phase: 04.1-01
    provides: detectGarmentBounds, standardizeImage, derivePrintAreaCoords, ImageProcessResult, PrintAreaCoords types

provides:
  - processProductImages returns ImageProcessResult { files, printAreaCoords } with dynamic coords
  - buildLinkedVariants accepts computed PrintAreaCoords (no more hardcoded constants)
  - pushProduct wires garment detection output into variant metafields end-to-end
  - PRINT_AREA_COORDINATES constant removed — coords computed per-image at runtime
  - Side images bypass garment detection (simple contain-resize)

affects: [phase-05, product-push-live-testing]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Garment detection pipeline: detect -> standardize -> derive coords -> pass to variant builder"
    - "Side images use contain-resize only (no garment detection needed for non-print-area images)"
    - "printAreaCoords: null when no front image succeeds — variants use empty {} JSON"

key-files:
  created: []
  modified:
    - src/shopify/image-standardizer.ts
    - src/shopify/color-swatches.ts
    - src/shopify/variants.ts
    - src/shopify/product-push.ts
    - tests/shopify/image-standardizer.test.ts
    - tests/shopify/variants.test.ts
    - tests/shopify/color-swatches.test.ts

key-decisions:
  - "processProductImages: categoryGroup is now required (not optional default 'tops') — wired from pushProduct"
  - "Side images use sharp contain-resize only — no garment detection, no coords derivation"
  - "printAreaCoords null fallback: when front image fails, buildLinkedVariants uses '{}' JSON"
  - "buildVariants removed from variants.ts — was superseded by buildLinkedVariants in color-swatches.ts"
  - "PRINT_AREA_COORDINATES constant removed — coords now computed dynamically per garment image"

patterns-established:
  - "Image pipeline returns both processed files and computed coords as a single ImageProcessResult"
  - "Front image drives coord derivation; back/side images do not affect coord computation"

requirements-completed: [SHOP-05]

# Metrics
duration: 5min
completed: 2026-03-11
---

# Phase 4.1 Plan 02: Image Pipeline Integration Summary

**Removed hardcoded PRINT_AREA_COORDINATES constant, replaced with garment-detection-derived coords flowing from processProductImages through buildLinkedVariants to variant metafields**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-11T10:55:00Z
- **Completed:** 2026-03-11T11:00:00Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- `processProductImages` now returns `ImageProcessResult { files, printAreaCoords }` with coords derived from garment placement in the front image
- `buildLinkedVariants` signature changed to accept `PrintAreaCoords | null` directly — no more category-based lookup
- `PRINT_AREA_COORDINATES` constant removed from `variants.ts`; old `buildVariants` function removed
- `pushProduct` wired end-to-end: `categoryGroup` flows into image processing, computed coords flow into variant builder
- Side images bypass garment detection pipeline (simple `sharp` contain-resize)
- All 191 tests pass across 19 test files with no regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Update processProductImages and buildLinkedVariants signatures** - `9d0ef7c` (feat)
2. **Task 2: Wire computed coords through pushProduct** - `b658fae` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified
- `src/shopify/image-standardizer.ts` - processProductImages updated: returns ImageProcessResult, categoryGroup required, side images use contain-resize
- `src/shopify/color-swatches.ts` - buildLinkedVariants signature: printAreaCoords replaces categoryGroup; removed import from variants.ts
- `src/shopify/variants.ts` - Removed PRINT_AREA_COORDINATES and buildVariants; kept getCategoryGroup, getTaxonomyCategoryId, SUPPORTED_CATEGORIES, buildHandle, buildFiles
- `src/shopify/product-push.ts` - Destructures imageResult; passes categoryGroup to processProductImages; passes printAreaCoords to buildLinkedVariants
- `tests/shopify/image-standardizer.test.ts` - Updated processProductImages tests to use result.files/result.printAreaCoords; added categoryGroup arg
- `tests/shopify/variants.test.ts` - Removed buildVariants and PRINT_AREA_COORDINATES tests (functions removed); kept getCategoryGroup/SUPPORTED_CATEGORIES/buildFiles tests
- `tests/shopify/color-swatches.test.ts` - Updated buildLinkedVariants calls to pass printAreaCoords; removed PRINT_AREA_COORDINATES import; added null coords test

## Decisions Made
- Side images use simple contain-resize — they don't define print areas, so garment detection is unnecessary overhead
- `printAreaCoords` null when front image processing fails — `buildLinkedVariants` safely falls back to `{}` JSON so variant creation doesn't crash
- `buildVariants` in `variants.ts` was dead code (only referenced in its own test file, not in product-push.ts which had already switched to `buildLinkedVariants`) — removed cleanly

## Deviations from Plan

None — plan executed exactly as written. The test updates were more substantial than described (color-swatches.test.ts also needed updating, not just variants.test.ts), but this was captured within the plan's scope.

## Issues Encountered
None. All edits were straightforward signature changes with clean compilation.

## Next Phase Readiness
- Full image pipeline is now wired: garment detection -> coords derivation -> variant metafields
- `processProductImages` and `buildLinkedVariants` are production-ready with correct signatures
- Phase 4.1 complete — ready for Phase 5 (Scale & Reliability) or live product push testing

---
*Phase: 04.1-image-standardization-print-area-detection*
*Completed: 2026-03-11*
