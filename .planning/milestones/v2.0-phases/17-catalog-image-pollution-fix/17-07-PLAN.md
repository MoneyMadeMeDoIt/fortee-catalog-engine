---
phase: 17-catalog-image-pollution-fix
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/cleanup-checkpoint-test-data.ts
  - tests/scripts/cleanup-checkpoint-test-data.test.ts
autonomous: true
requirements: [R17-07]

must_haves:
  truths:
    - "Running `npx tsx scripts/cleanup-checkpoint-test-data.ts` now scans BOTH `CHECKPOINT-TEST/` AND `MANUAL/CHECKPOINT-TEST-*/` Drive folder trees and trashes orphan files in both locations."
    - "Previous behavior preserved: `CHECKPOINT-TEST/` cleanup runs first (existing logic untouched)."
    - "When no `MANUAL/` folder exists at the root, the new `MANUAL/` scan is a silent no-op (graceful — checkpoint flow may have never created MANUAL artifacts on this account)."
    - "Idempotent: re-running after a successful pass finds zero new files and exits cleanly."
  artifacts:
    - path: "scripts/cleanup-checkpoint-test-data.ts"
      provides: "Cleanup script with MANUAL/ blind-spot patched (B-3 fix)"
      contains: "findManualFolderId"
    - path: "tests/scripts/cleanup-checkpoint-test-data.test.ts"
      provides: "New test file covering the MANUAL/ scan path (R17-07)"
      contains: "MANUAL"
  key_links:
    - from: "scripts/cleanup-checkpoint-test-data.ts"
      to: "drive.files.list (search for MANUAL folder at root)"
      via: "new findManualFolderId helper"
      pattern: "findManualFolderId"
    - from: "scripts/cleanup-checkpoint-test-data.ts"
      to: "trashDriveFile (existing helper)"
      via: "trash every descendant of MANUAL/CHECKPOINT-TEST-*/ folders"
      pattern: "trashDriveFile"
---

<objective>
Extend `scripts/cleanup-checkpoint-test-data.ts` to ALSO scan and trash orphan files under `MANUAL/CHECKPOINT-TEST-*/` (B-3 in `PRODUCTION-AUDIT-FINDINGS.md`). Phase 16's checkpoint flow uploads FORCE-replacement images via `scripts/fix-image-pollution-manual.ts` `handleReplace`, which hardcodes `supplierCode='MANUAL'`. The result: when the checkpoint test ran on disposable pids `CHECKPOINT-TEST-001/-002` on 2026-05-13, leftover files landed in `MANUAL/CHECKPOINT-TEST-001/...` Drive folder, NOT `CHECKPOINT-TEST/CHECKPOINT-TEST-001/...`. The original cleanup script only scanned `CHECKPOINT-TEST/`, leaving orphans behind.

Purpose: one-line description from RESEARCH §B-3: "After CHECKPOINT-TEST/ pass, also locate MANUAL/ folder and trash any descendants whose names start with CHECKPOINT-TEST-". This is a small one-time-use script, but adding the second scan path closes the cleanup gap so future checkpoint runs don't accumulate orphan files.

Output:
- Patched `scripts/cleanup-checkpoint-test-data.ts` with a new `findManualFolderId` + `trashCheckpointArtifactsInManualFolder` block, called after the existing `CHECKPOINT-TEST/` cleanup.
- New test file `tests/scripts/cleanup-checkpoint-test-data.test.ts` (Phase 16 didn't write tests for this one-shot script — Wave 0 gap from 17-RESEARCH.md §Validation Architecture).
</objective>

<execution_context>
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-catalog-image-pollution-fix/17-CONTEXT.md
@.planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md
@.planning/research/phase17-prep/PRODUCTION-AUDIT-FINDINGS.md
@scripts/cleanup-checkpoint-test-data.ts
@scripts/seed-checkpoint-test-data.ts

<interfaces>
<!-- Existing helpers in scripts/cleanup-checkpoint-test-data.ts -->
const ROOT_FOLDER_ID = process.env.GOOGLE_DRIVE_IMAGES_FOLDER_ID ?? '1xIjATpaEdqJYHRiuy0Iy6wIYUcNCXC8k';
const SUPPLIER_PREFIX = 'CHECKPOINT-TEST';
const TEST_PIDS = ['CHECKPOINT-TEST-001', 'CHECKPOINT-TEST-002'];

async function findSupplierFolderId(drive): Promise<string | null>;
async function listAllDescendantFiles(drive, folderId): Promise<{id, name}[]>;
async function trashAllCheckpointFiles(drive): Promise<number>;

import { trashDriveFile } from '../src/sheets/drive.js';
// Note: post-17-01 ships, trashDriveFile has 30s timeout + retry. This script
// benefits from that timeout automatically — no script-level change needed.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drive folder structure → cleanup script | Phase 16 hardcodes 'MANUAL' as the supplier code for force-replace uploads; the new scan must not accidentally trash NON-checkpoint files (e.g., real operator-uploaded MANUAL/<production-pid>/ folders). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-23 | T (Tampering — DATA DESTRUCTION) | new MANUAL/ scan could trash REAL operator-uploaded files | mitigate | The scan ONLY enters subfolders whose names are in `TEST_PIDS = ['CHECKPOINT-TEST-001', 'CHECKPOINT-TEST-002']`. It does NOT recurse into other MANUAL/ subfolders. Any `MANUAL/<real-pid>/...` (e.g., `MANUAL/S05610/...` from a real operator force-replace) is untouched. Test: synthetic MANUAL/ folder containing both `CHECKPOINT-TEST-001/` and `S05610/`; cleanup should trash CHECKPOINT-TEST-001 contents and leave S05610 untouched. |
| T-17-24 | T — accidentally trashes the `MANUAL/` parent folder itself | new scan logic | mitigate | The patch does NOT trash the MANUAL/ parent folder (operator's other manual uploads live there). It only trashes the per-pid subfolder of each checkpoint pid. Specifically: trash all descendants of `MANUAL/CHECKPOINT-TEST-001/`, then trash `MANUAL/CHECKPOINT-TEST-001/` itself. MANUAL/ stays. Test: assert `trashDriveFile` is NOT called with the manualFolderId itself. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MANUAL/ folder scan + per-checkpoint-pid descendant trash</name>
  <files>
    scripts/cleanup-checkpoint-test-data.ts,
    tests/scripts/cleanup-checkpoint-test-data.test.ts
  </files>
  <read_first>
    - scripts/cleanup-checkpoint-test-data.ts (entire file — 191 lines; understand the existing `findSupplierFolderId` + `listAllDescendantFiles` + `trashAllCheckpointFiles` + `deleteBrRows` + `main()` structure)
    - scripts/seed-checkpoint-test-data.ts (sibling script — confirm the test pids `CHECKPOINT-TEST-001`, `CHECKPOINT-TEST-002` are still the seed)
    - scripts/fix-image-pollution-manual.ts (around lines 660-680 — the `handleReplace` path that does `uploadToDrive(... 'MANUAL', ...)` — proving that force-replace creates MANUAL/CHECKPOINT-TEST-*/ trails)
    - .planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md "Pattern E: Cleanup MANUAL/ blind-spot fix (B-3)" lines 957-997 (verbatim code excerpt to use)
    - .planning/phases/16-catalog-image-pollution-audit-fix/16-PHASE-SUMMARY.md (look for "Cleanup script blind spot" — Phase 16's documented finding)
  </read_first>
  <behavior>
    Tests in a NEW file `tests/scripts/cleanup-checkpoint-test-data.test.ts`. Test framework: vitest. Mock the `drive_v3.Drive` shape via `vi.fn()` for `files.list`, `files.create`, `files.update` (the trash invocation surfaces via `files.update` with `{trashed: true}` in `uploadToDrive`'s implementation, but `trashDriveFile` is what the cleanup script imports — so mock the imported helper instead). DI is implicit here — the cleanup script doesn't take a deps object, so the test patches the imported module via `vi.mock('../../src/sheets/drive.js', ...)`.

    Strategy: since the script's `main()` directly imports `trashDriveFile` and `createDriveClient`, the cleanest test pattern is to either (a) refactor the script slightly to accept dependency injection in a new exported function `runCleanup(deps)` mirroring Phase 16's pattern, OR (b) vi.mock the imports at the test file's top. Pick (a) — it's more aligned with the Phase 16 DI seam convention.

    Refactor (small):
    - Extract a new exported `runCleanup(deps: CleanupDeps)` function from the current main().
    - `CleanupDeps` includes: `driveClient`, `sheetsClient`, `trashDriveFileFn`, `findManualFolderIdFn` (new), `listAllDescendantFilesFn`, `deleteBrRowsFn`, plus the env constants `ROOT_FOLDER_ID`, `TEST_PIDS`.
    - `main()` constructs concrete deps and calls `runCleanup(deps)`.

    Tests:
    - Test 1 (existing CHECKPOINT-TEST/ scan still runs): mock `findSupplierFolderId` to return 'CT-FOLDER-ID'; mock `listAllDescendantFiles` for CT to return 3 files; mock `findManualFolderIdFn` to return null (no MANUAL folder). Assert: trashDriveFile called 3 times (the 3 CT descendants) + 1 time for the CT parent folder itself = 4 total. trashDriveFile NEVER called with manual-related IDs.
    - Test 2 (CHECKPOINT-TEST/ scan + MANUAL/CHECKPOINT-TEST-001/ scan): mock findSupplierFolderId → 'CT-FOLDER'; mock findManualFolderIdFn → 'MANUAL-FOLDER'; mock listAllDescendantFiles to return different file sets depending on which folder ID was passed (use `vi.fn().mockImplementation((drive, folderId) => ...)`). For CT-FOLDER → 2 files; for MANUAL-CHECKPOINT-001-folder → 1 file; for MANUAL-CHECKPOINT-002-folder → 1 file. Assert: total trashDriveFile calls = (2 + 1) descendants + 1 CT-parent + 1 MANUAL-CHECKPOINT-001-folder + 1 MANUAL-CHECKPOINT-002-folder = 6.
    - Test 3 (MANUAL/ exists but contains NO checkpoint subfolders): mock findManualFolderIdFn → 'MANUAL-FOLDER'; mock `drive.files.list` (for the per-pid subfolder lookup inside trashCheckpointArtifactsInManualFolder) to return `{files: []}` for both CHECKPOINT-TEST-001 and CHECKPOINT-TEST-002. Assert: no trash calls for MANUAL side; CT side still runs.
    - Test 4 (T-17-23 — MANUAL/ contains REAL operator pids, NOT touched): mock the per-pid subfolder lookup to return `[{id:'CT001-FOLDER', name:'CHECKPOINT-TEST-001'}]` for 'CHECKPOINT-TEST-001'. The MANUAL/ folder ALSO contains `S05610/` (real operator force-replace) — but since the new scan only queries `name = 'CHECKPOINT-TEST-001'` (exact match) and `name = 'CHECKPOINT-TEST-002'` (exact match), the S05610 folder is never enumerated. Assert: no trashDriveFile call with anything S05610-related.
    - Test 5 (T-17-24 — MANUAL/ parent folder NOT trashed): mock both scans to succeed. Assert: trashDriveFile is NOT called with 'MANUAL-FOLDER'. (The cleanup is allowed to trash the CHECKPOINT-TEST root folder; it's NOT allowed to trash MANUAL.)
    - Test 6 (idempotent re-run): all mocks return empty file sets (everything already trashed). Assert: trashDriveFile NEVER called; runCleanup returns cleanly.
    - Test 7 (env constant integrity): TEST_PIDS is the exact array `['CHECKPOINT-TEST-001', 'CHECKPOINT-TEST-002']`. Test by importing the const (export it if not already exported) and assert deep-equal.
  </behavior>
  <action>
    Modify `scripts/cleanup-checkpoint-test-data.ts`:

    1. **Add `findManualFolderId` helper** (after `findSupplierFolderId`, before `listAllDescendantFiles`):

       ```
       async function findManualFolderId(
         drive: ReturnType<typeof createDriveClient>,
       ): Promise<string | null> {
         const q = `'${ROOT_FOLDER_ID}' in parents and name = 'MANUAL' and mimeType = 'application/vnd.google-apps.folder' and trashed = false`;
         const r = await drive.files.list({
           q,
           fields: 'files(id,name)',
           pageSize: 1,
           supportsAllDrives: true,
           includeItemsFromAllDrives: true,
         });
         return r.data.files?.[0]?.id ?? null;
       }
       ```

    2. **Add `trashCheckpointArtifactsInManualFolder` helper** (after `trashAllCheckpointFiles`):

       ```
       async function trashCheckpointArtifactsInManualFolder(
         drive: ReturnType<typeof createDriveClient>,
       ): Promise<number> {
         const manualId = await findManualFolderId(drive);
         if (!manualId) {
           console.log('[cleanup] no MANUAL/ Drive folder found — nothing to trash');
           return 0;
         }
         let trashed = 0;
         for (const pid of TEST_PIDS) {
           // T-17-23: only enumerate the per-pid subfolders matching the checkpoint pids exactly.
           // No recursion into MANUAL/<real-pid>/ — those are operator force-replace files we must not touch.
           const r = await drive.files.list({
             q: `'${manualId}' in parents and name = '${pid}' and mimeType = 'application/vnd.google-apps.folder' and trashed = false`,
             fields: 'files(id,name)',
             pageSize: 1,
             supportsAllDrives: true,
             includeItemsFromAllDrives: true,
           });
           const pidFolder = r.data.files?.[0]?.id;
           if (!pidFolder) continue;
           // Trash every descendant
           const descendants = await listAllDescendantFiles(drive, pidFolder);
           for (const f of descendants) {
             try {
               await trashDriveFile(drive, f.id);
               console.log(`[cleanup]   MANUAL/ trashed ${f.name} (${f.id})`);
               trashed++;
             } catch (err) {
               console.warn(`[cleanup]   FAILED to trash ${f.name} (${f.id}): ${err}`);
             }
           }
           // T-17-24: trash ONLY the per-pid subfolder, NEVER the MANUAL/ root.
           try {
             await trashDriveFile(drive, pidFolder);
             console.log(`[cleanup]   MANUAL/ trashed folder ${pid} (${pidFolder})`);
           } catch (err) {
             console.warn(`[cleanup]   could not trash MANUAL/${pid}: ${err}`);
           }
         }
         return trashed;
       }
       ```

    3. **Refactor `main()` to expose `runCleanup(deps)` for testing**:

       Extract the body of `main()` into a new exported function:

       ```
       export interface CleanupDeps {
         driveClient: ReturnType<typeof createDriveClient>;
         sheetsClient: ReturnType<typeof createSheetsClient>;
         trashDriveFileFn: typeof trashDriveFile;
         findSupplierFolderIdFn: (drive: ReturnType<typeof createDriveClient>) => Promise<string | null>;
         findManualFolderIdFn: (drive: ReturnType<typeof createDriveClient>) => Promise<string | null>;
         listAllDescendantFilesFn: (drive: ReturnType<typeof createDriveClient>, folderId: string) => Promise<{id: string; name: string}[]>;
         deleteBrRowsFn: (sheets: ReturnType<typeof createSheetsClient>) => Promise<number>;
       }

       export async function runCleanup(deps: CleanupDeps): Promise<{ ctTrashed: number; manualTrashed: number; brRowsDeleted: number }> {
         console.log('[cleanup] scanning CHECKPOINT-TEST/ ...');
         const ctTrashed = await trashAllCheckpointFiles(deps.driveClient);  // existing logic
         console.log(`  CHECKPOINT-TEST/ files trashed: ${ctTrashed}`);

         console.log('[cleanup] scanning MANUAL/<pid>/ for checkpoint leftovers...');
         const manualTrashed = await trashCheckpointArtifactsInManualFolder(deps.driveClient);
         console.log(`  MANUAL/ leftover files trashed: ${manualTrashed}`);

         const brRowsDeleted = await deps.deleteBrRowsFn(deps.sheetsClient);
         return { ctTrashed, manualTrashed, brRowsDeleted };
       }
       ```

       Then `main()` becomes:

       ```
       async function main(): Promise<void> {
         const drive = createDriveClient();
         const sheets = createSheetsClient();
         const result = await runCleanup({
           driveClient: drive,
           sheetsClient: sheets,
           trashDriveFileFn: trashDriveFile,
           findSupplierFolderIdFn: findSupplierFolderId,
           findManualFolderIdFn: findManualFolderId,
           listAllDescendantFilesFn: listAllDescendantFiles,
           deleteBrRowsFn: deleteBrRows,
         });
         console.log('[cleanup] done:', result);
       }
       ```

    4. **Export `TEST_PIDS`** (was previously a file-local const). Add `export` before its declaration for testing. Other exports as listed above.

    5. **Create `tests/scripts/cleanup-checkpoint-test-data.test.ts`** (new file). Cover the 7 tests above. Use `vi.fn()` for every mockable callback. Construct synthetic `CleanupDeps`. Pattern from `tests/scripts/fix-image-pollution.test.ts`.

    6. **Do NOT** modify the BR row deletion logic (`deleteBrRows`) — out of scope for B-3. That function is well-tested via the prior live checkpoint runs.
  </action>
  <verify>
    <automated>npx vitest run tests/scripts/cleanup-checkpoint-test-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^async function findManualFolderId\\(" scripts/cleanup-checkpoint-test-data.ts` matches one line.
    - `grep -nE "^async function trashCheckpointArtifactsInManualFolder\\(" scripts/cleanup-checkpoint-test-data.ts` matches one line.
    - `grep -nE "^export async function runCleanup\\(" scripts/cleanup-checkpoint-test-data.ts` matches one line.
    - `grep -nE "^export interface CleanupDeps " scripts/cleanup-checkpoint-test-data.ts` matches one line.
    - `grep -nE "name = '\\\${pid}'" scripts/cleanup-checkpoint-test-data.ts` matches at least one line (exact-match per-pid lookup — T-17-23 mitigation).
    - `test -f tests/scripts/cleanup-checkpoint-test-data.test.ts` succeeds.
    - `npx vitest run tests/scripts/cleanup-checkpoint-test-data.test.ts` reports 7 tests passing.
    - `npx vitest run` (full suite) reports no regressions.
  </acceptance_criteria>
  <done>
    The cleanup script scans `MANUAL/<CHECKPOINT-TEST-pid>/` in addition to `CHECKPOINT-TEST/`. Exact-name-match for per-pid subfolders prevents accidentally trashing real operator-uploaded MANUAL/ data. `runCleanup(deps)` is exported for DI testing. 7 new tests passing.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/scripts/cleanup-checkpoint-test-data.test.ts` reports 7 tests passing.
- `grep` acceptance criteria all pass.
- Manual smoke test (optional — operator may not have CHECKPOINT-TEST orphan files anymore since Phase 16's manual cleanup; if they do, run `npx tsx scripts/cleanup-checkpoint-test-data.ts` and verify both `CHECKPOINT-TEST/` and `MANUAL/CHECKPOINT-TEST-*/` are emptied).
</verification>

<success_criteria>
- B-3 blind spot closed: cleanup now scans both CHECKPOINT-TEST/ AND MANUAL/CHECKPOINT-TEST-*/.
- T-17-23 mitigation: real operator MANUAL/<production-pid>/ folders are NEVER touched (exact-name match on TEST_PIDS).
- T-17-24 mitigation: MANUAL/ root folder itself is NEVER trashed.
- Idempotent re-run: zero false-positive trashes on a second invocation.
- 7 new tests; no Phase 16 regression.
</success_criteria>

<output>
After completion, create `.planning/phases/17-catalog-image-pollution-fix/17-07-SUMMARY.md` recording:
- The 3 new helpers added (findManualFolderId, trashCheckpointArtifactsInManualFolder, runCleanup).
- The 7 test cases.
- Smoke-test outcome if run.
- Note on whether the script needed any additional refactoring (e.g., the deleteBrRows function may have been an inline closure — surface if it was extracted as part of the DI refactor).
</output>
