---
phase: 17-catalog-image-pollution-fix
plan: 04
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - src/lib/supplier-canonical.ts
  - tests/lib/supplier-canonical.test.ts
autonomous: true
requirements: [R17-04]

must_haves:
  truths:
    - "`resolveSupplierCanonical('A702', colorName)` routes through the S&S branch (per RESEARCH Finding 1: adidas is exclusive to S&S in the promo channel). Returns a non-null result when S&S's `/styles/?search=A702` resolves and a matching color variant has `colorFrontImage`."
    - "`resolveSupplierCanonical('CE520L', colorName)` routes through the S&S branch (adidas CE-prefix variant)."
    - "`resolveSupplierCanonical('6110', colorName)`, `resolveSupplierCanonical('3001', ...)`, `resolveSupplierCanonical('8882', ...)`, `resolveSupplierCanonical('1510', ...)`, `resolveSupplierCanonical('5200', ...)`, `resolveSupplierCanonical('1304', ...)` all route through the S&S branch (RESEARCH Finding 2: Bella+Canvas / Gildan / Next Level / American Apparel are carried by S&S Canada)."
    - "Headwear pids (anything matching `/^H08/i`) STILL return null FIRST (D-22 invariant — out of scope). The prefix dispatcher fires AFTER the H08 check."
    - "Existing S* pid behavior unchanged (S05610 still resolves via S&S; CSW L00660 still resolves via CSW)."
    - "Unknown pids that match neither `/^S/i`, `/^L/i`, nor any KNOWN_SUPPLIER_PREFIXES entry return null (true M786, QTB6000, NE220 cases — handled by Plan 17-05 manual triage)."
    - "After 17-04 ships, re-running the 2026-05-14 audit logs ≥ 100 fewer 'no canonical for tier 1' trail rows than the original 714 (researcher's disciplined estimate: 714 → ~50-150 after 17-02 + 17-04)."
  artifacts:
    - path: "src/lib/supplier-canonical.ts"
      provides: "Extended prefix dispatcher routing adidas/Bella/Gildan/Next Level/Comfort Colors/American Apparel pids through S&S"
      exports: ["resolveSupplierCanonical"]
      contains: "routesViaSS"
    - path: "tests/lib/supplier-canonical.test.ts"
      provides: "Test coverage for new brand-prefix routing"
      contains: "17-04 prefix routing"
  key_links:
    - from: "src/lib/supplier-canonical.ts"
      to: "S&S /styles/?search= endpoint"
      via: "resolveSSStyleId works for ANY pid, not just S*-prefixed — verified pattern in scripts/fetch-ss-rest-sizes.ts:80-94"
      pattern: "resolveSSStyleId"
    - from: "src/lib/supplier-canonical.ts"
      to: "KNOWN_SUPPLIER_PREFIXES map (existing — 19 entries)"
      via: "Object.keys(KNOWN_SUPPLIER_PREFIXES) feeds a Set<string> for O(1) dispatch lookup"
      pattern: "KNOWN_SUPPLIER_PREFIXES"
---

<objective>
Widen the prefix dispatcher in `src/lib/supplier-canonical.ts` to route adidas (A*, CE*) pids plus the existing `KNOWN_SUPPLIER_PREFIXES` brand-allowlist pids (Bella+Canvas, Gildan, Next Level, Comfort Colors, American Apparel) through the existing S&S branch. This is the surprise finding from RESEARCH Findings 1 + 2: **all these brands are already carried by S&S Canada's wholesale catalog**, so NO new scrapers are needed — the existing S&S `/styles/?search=<pid>` resolver works for ANY pid, not just `S*`-prefixed ones.

Purpose: unlock Tier 1 fixes for ~145 pids in BR that previously logged "no canonical for tier 1" because the dispatcher only fired on `/^S/i.test(pid)` or `/^L/i.test(pid)`. After 17-04 ships, those 714 "no canonical" rows from 2026-05-14 collapse to ≤ 100 (only the true unsupported brands remain: New Era proprietary NE2xx codes, anvil 9xx, M786, QTB6000).

This plan WAVE 3 (depends on 17-01 + 17-02; runs AFTER 17-02 per the locked execution order in 17-CONTEXT.md). Plan 17-02 already adds the per-color filter to the S&S branch; 17-04 just expands WHICH pids hit that branch. Both touch `src/lib/supplier-canonical.ts`, so they run sequentially (17-02 wave 2 → 17-04 wave 3) to prevent file-conflict in the wave system.

Output:
- Modified `src/lib/supplier-canonical.ts` with a `routesViaSS(pid)` helper that returns true for any of: `/^S/i`, `/^A\d/i` (adidas A2009 etc.), `/^CE\d/i` (adidas CE520L etc.), or `KNOWN_SUPPLIER_PREFIXES[pid] !== undefined`.
- Extended `tests/lib/supplier-canonical.test.ts` with new "17-04 prefix routing" test group covering adidas + each brand family in KNOWN_SUPPLIER_PREFIXES.
</objective>

<execution_context>
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-catalog-image-pollution-fix/17-CONTEXT.md
@.planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md
@.planning/research/phase17-prep/PRODUCTION-AUDIT-FINDINGS.md
@.planning/phases/17-catalog-image-pollution-fix/17-01-PLAN.md
@.planning/phases/17-catalog-image-pollution-fix/17-02-PLAN.md
@src/lib/supplier-canonical.ts
@scripts/fetch-ss-rest-sizes.ts
@tests/lib/supplier-canonical.test.ts

<interfaces>
<!-- Post-17-02 signature (this plan extends the same function 17-02 just modified). -->
export async function resolveSupplierCanonical(
  pid: string,
  colorName?: string,
): Promise<CanonicalResult | null>;

<!-- Existing KNOWN_SUPPLIER_PREFIXES (src/lib/supplier-canonical.ts:55-81) — DO NOT add or remove
     entries. Just consume the keys for dispatch.
     Entries are: ['168', '112', '1010', '3001', '3010', '3480', '4610', '6003',
     '6008', '6110', '8882', '1510', '3900', '3911', '9002', '1466', '1467',
     '5200', '1304']  (19 entries). -->

<!-- Existing resolveSSStyleId helper (line ~146-158 in supplier-canonical.ts).
     The current implementation works for ANY pid via /styles/?search=<pid>:
       const styles = await ssGet(`/styles/?search=${pid}&fields=styleID,styleName`, auth)
       Find exact-match by styleName.toUpperCase(); if 1-of-1 match, return styleID;
       else null.
     VERIFIED in scripts/fetch-ss-rest-sizes.ts:80-94 — this generic resolver
     accepts non-S* pids (e.g. it's already used for Gildan styles). -->

<!-- D-22 / H08 invariant (existing — preserve). The H08 early-return at the top
     of resolveSupplierCanonical MUST fire BEFORE the new dispatcher. Richardson
     caps (pid '168', '112') are in KNOWN_SUPPLIER_PREFIXES but are caps; they're
     NOT H08-prefixed and per RESEARCH Open Question 4 they're allowed to route
     via S&S (S&S carries Richardson). The H08 check covers a separate class
     (custom in-house headwear like H08355). -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pid string → dispatcher | BR-row-provided; lowercase / uppercase / leading-zero variation possible. |
| S&S /styles/?search= → styleID resolution for non-S* pids | Vendor query; the resolver already handles ambiguous matches by returning null. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-15 | T (Tampering) — wrong brand routed to S&S | routesViaSS helper | mitigate | `routesViaSS` checks (a) `/^S/i` (native S&S), (b) `/^A\d/i` and `/^CE\d/i` (adidas — RESEARCH Finding 1 exclusive distribution), (c) `KNOWN_SUPPLIER_PREFIXES[pid]` exact-key match. Pids that fall through (M786, QTB6000, NE220, NE7xx, 9xx anvil) return null. resolveSSStyleId already returns null on no-match, so a wrong-brand routing gets a clean null response and cascades to manual. No false positive can write a wrong product. |
| T-17-16 | T — H08 headwear leaks past the early-return | H08 invariant | mitigate | The H08 check is at the TOP of the function (before routesViaSS). Test guard: any H08 pid still returns null, even if it accidentally appears in KNOWN_SUPPLIER_PREFIXES (currently none do — Richardson 168/112 are not H08-prefixed). |
| T-17-17 | I (Information Disclosure) — extra S&S API calls leak product graph to vendor | new pids hitting S&S | accept | All these pids would ALREADY hit S&S if their prefix had been S*. The vendor sees the same call shape. No new disclosure. |
| T-17-18 | D — extra S&S throttled calls slow down audit | new dispatch routing | accept | Throttle is 1100ms between S&S calls. Adding ~145 new pids to route through S&S adds ~145 × 1100ms = 160s extra audit runtime worst case. Acceptable; the audit already runs ~30-60min. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen routesViaSS to cover adidas + KNOWN_SUPPLIER_PREFIXES brand pids</name>
  <files>
    src/lib/supplier-canonical.ts,
    tests/lib/supplier-canonical.test.ts
  </files>
  <read_first>
    - src/lib/supplier-canonical.ts (entire file — focus on lines 55-81 for KNOWN_SUPPLIER_PREFIXES, the H08 early-return at the top of resolveSupplierCanonical, and the dispatcher itself around line 200 — note that 17-02 already added the per-color filter inside the S&S branch; 17-04 just adds more pids to that branch)
    - tests/lib/supplier-canonical.test.ts (post-17-02 state — extend with new "17-04 prefix routing" describe block)
    - scripts/fetch-ss-rest-sizes.ts lines 75-100 (verified pattern of generic resolveStyleId — works for any pid, not just S*-prefixed)
    - .planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md "Finding 1: adidas is exclusive to S&S in the promo channel" lines 131-141
    - .planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md "Finding 2: Bella+Canvas, Gildan, Next Level distributed via multiple wholesalers including S&S" lines 143-149
    - .planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md "Pattern B" lines 769-854 (the routesViaSS code excerpt — use directly)
    - .planning/phases/17-catalog-image-pollution-fix/17-RESEARCH.md "Open Question 4" lines 1049-1052 (Richardson 168/112 routing decision — let them route via S&S; they're not H08-prefixed)
  </read_first>
  <behavior>
    All tests assume the post-17-02 codebase. New tests in `describe('17-04 prefix routing', () => { ... })`.

    routesViaSS direct unit tests (testable if the function is exported file-locally OR if we don't export it, we test the effect by calling resolveSupplierCanonical with mocked fetch and asserting S&S branch was hit):
    - Test 1 (adidas A702 routes via S&S): mock /styles/?search=A702 returning `[{styleID: 12345, styleName: 'A702'}]`; mock /products/?style=12345 returning a multi-color variants array. Call `resolveSupplierCanonical('A702', 'BLACK')`. Assert: the S&S branch fires (fetch called with `/styles/?search=A702` and `/products/?style=12345`); returns a non-null CanonicalResult with source === 'ss'.
    - Test 2 (adidas CE520L routes via S&S): mock /styles/?search=CE520L and /products/?style=. Call `resolveSupplierCanonical('CE520L', 'WHITE')`. Assert non-null result with source === 'ss'.
    - Test 3 (Bella 6110 routes via S&S): same pattern, pid '6110'. Assert non-null S&S result.
    - Test 4 (Bella 3001 routes via S&S): same pattern, pid '3001'. Assert non-null S&S result.
    - Test 5 (Gildan 5200 routes via S&S): same pattern, pid '5200'. Assert non-null S&S result.
    - Test 6 (Next Level 1510 routes via S&S): same pattern, pid '1510'. Assert non-null S&S result.
    - Test 7 (Comfort Colors 1466 routes via S&S): same pattern, pid '1466'. Assert non-null S&S result.
    - Test 8 (American Apparel 1304 routes via S&S): same pattern, pid '1304'. Assert non-null S&S result.
    - Test 9 (Richardson 168 routes via S&S): same pattern, pid '168'. Per Open Question 4, Richardson caps are NOT H08-prefixed so they route via S&S like any other KNOWN_SUPPLIER_PREFIXES entry.
    - Test 10 (H08 still returns null, even if accidentally in KNOWN_SUPPLIER_PREFIXES): call `resolveSupplierCanonical('H08355', 'BLACK')`. Assert: returns null; NO fetch made (H08 early-return fires before dispatcher).
    - Test 11 (M786 returns null — true unsupported): mock /styles/?search=M786 returning empty array. Call `resolveSupplierCanonical('M786', 'BLACK')`. Assert: returns null. (Test that the function ATTEMPTED S&S — because M786 doesn't match S*/A*/CE*/KNOWN_PREFIXES, it must NOT have routed via S&S at all. So fetch called 0 times for /styles/?search=M786. M786 = manual triage cascade.)
    - Test 12 (NE220 returns null — true unsupported): pid 'NE220' doesn't match S*/A*/CE*/KNOWN_PREFIXES. Call `resolveSupplierCanonical('NE220', 'BLACK')`. Assert: returns null; fetch NOT called. (New Era proprietary codes need manual triage per RESEARCH Finding 3.)
    - Test 13 (existing S* behavior unchanged): pid 'S05610'. Standard S&S resolution. Returns non-null. Same as Phase 16's pre-17-04 baseline.
    - Test 14 (existing L* CSW behavior unchanged): pid 'L00660'. Standard CSW resolution. Returns non-null with source === 'csw'.

    routesViaSS prefix-matching edge cases:
    - Test 15 (`/^A\d/` doesn't match standalone 'A'): pid 'A' (single letter, no digit). Should NOT route via S&S (`/^A\d/i` requires a digit after A, preventing accidental match on weird BR rows). routesViaSS('A') === false.
    - Test 16 (`/^A\d/` matches 'A2009'): routesViaSS('A2009') === true.
    - Test 17 (`/^CE\d/` matches 'CE520L'): routesViaSS('CE520L') === true.
    - Test 18 (CSW 'L*' still routes via CSW, not S&S — even though no /^L/ prefix override): pid 'L00660' is /^L/i.test → routes via CSW branch. The dispatcher checks S&S FIRST? Or CSW first? See action below — order matters.
  </behavior>
  <action>
    Modify `src/lib/supplier-canonical.ts`:

    1. **Add `SS_ROUTABLE_PIDS` set** (top of file, after KNOWN_SUPPLIER_PREFIXES declaration):

       ```
       /**
        * Phase 17 17-04: pids dispatched to the S&S branch via brand-prefix routing.
        * Per RESEARCH Findings 1 + 2:
        *   - adidas is EXCLUSIVE to S&S in the promo channel (`/^A\d/i`, `/^CE\d/i`)
        *   - Bella+Canvas, Gildan, Next Level, Comfort Colors, American Apparel
        *     pids (the KNOWN_SUPPLIER_PREFIXES allowlist) are ALSO carried by S&S
        * Routing them through the existing S&S branch unlocks Tier 1 fixes without
        * building separate scrapers per brand.
        */
       const SS_ROUTABLE_PIDS = new Set<string>(Object.keys(KNOWN_SUPPLIER_PREFIXES));
       ```

    2. **Add `routesViaSS(pid)` helper** (file-local; doesn't need to be exported but DO export it as a named export for testability — preceded by the small comment):

       ```
       /**
        * Returns true if `pid` should be dispatched through the S&S branch of
        * resolveSupplierCanonical. Phase 17 17-04.
        *
        * Trigger conditions (any match):
        *   - /^S/i  (native S&S S* prefix — Phase 16 default)
        *   - /^A\d/i (adidas A2009, A702 etc.)
        *   - /^CE\d/i (adidas CE520L etc.)
        *   - SS_ROUTABLE_PIDS.has(pid) (Bella/Gildan/Next Level/Comfort/American/Richardson)
        */
       export function routesViaSS(pid: string): boolean {
         if (/^S/i.test(pid)) return true;
         if (/^A\d/i.test(pid)) return true;
         if (/^CE\d/i.test(pid)) return true;
         if (SS_ROUTABLE_PIDS.has(pid)) return true;
         return false;
       }
       ```

    3. **Modify `resolveSupplierCanonical` dispatcher** (in the function body, around line 200 region after the H08 early-return and the per-color logic 17-02 added):

       The CURRENT dispatcher structure (post-17-02) likely is:
       ```
       if (/^H08/i.test(pid)) return null;
       if (/^S/i.test(pid)) { /* S&S branch */ }
       if (/^L/i.test(pid)) { /* CSW branch */ }
       return null;
       ```

       Replace with:
       ```
       if (/^H08/i.test(pid)) return null;

       // Phase 17 17-04: brand-prefix dispatch routes adidas + KNOWN_SUPPLIER_PREFIXES
       // through the existing S&S branch. No new scrapers — S&S carries these brands.
       if (routesViaSS(pid)) {
         // [S&S branch body — unchanged from 17-02; the per-color filter still applies.]
       }

       if (/^L/i.test(pid)) {
         // [CSW branch body — unchanged from 17-02.]
       }

       return null;
       ```

       Notes:
       - **Order matters**: S&S branch fires BEFORE CSW. The `routesViaSS` check includes `/^S/i` so S* pids still go S&S (same behavior). CSW pids (L*) don't match `routesViaSS` so they fall through to the L* branch. No regression.
       - **Do NOT duplicate the S&S branch body** — wrap it in a single block guarded by `if (routesViaSS(pid))`. The existing body (S&S styleID resolve → /products/ fetch → 17-02 per-color filter) runs unchanged.
       - The log line at the entry of the S&S branch should be updated to surface which routing rule fired (`S* native`, `A* adidas`, `CE* adidas`, or `KNOWN_SUPPLIER_PREFIXES`):

         ```
         logger.info(
           `[supplier-canonical] routing ${pid} via S&S (${
             /^S/i.test(pid) ? 'S*' :
             /^A\d/i.test(pid) ? 'A*' :
             /^CE\d/i.test(pid) ? 'CE*' :
             'KNOWN_PREFIX'
           })${colorName ? ` colorName=${colorName}` : ''}`,
         );
         ```

         This is operator observability — when an audit run produces unexpected results, the logger reveals which dispatch rule matched.

    4. **Do NOT modify** the existing per-color filter logic (17-02's work), the H08 early-return, the CSW branch, the throttle helpers, or any auth helpers.

    5. **Do NOT add new entries to KNOWN_SUPPLIER_PREFIXES.** The 19 existing entries are the authoritative allowlist. If post-17-04 audit shows additional brand pids that should be routed, that's a follow-up patch (or a 17-04 v2 — out of scope for this plan).

    6. **Extend `tests/lib/supplier-canonical.test.ts`** with the 18 new tests above under `describe('17-04 prefix routing', () => { ... })`. Some tests target `routesViaSS` directly (Tests 15-18); others mock fetch and call `resolveSupplierCanonical` to assert the routing effect (Tests 1-14). Use the same mock-fetch pattern from 17-02's tests.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/supplier-canonical.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^const SS_ROUTABLE_PIDS = new Set" src/lib/supplier-canonical.ts` matches one line.
    - `grep -nE "^export function routesViaSS\\(pid: string\\)" src/lib/supplier-canonical.ts` matches one line.
    - `grep -nE "/\\^A\\\\d/i\\.test" src/lib/supplier-canonical.ts` matches at least one line (adidas A-prefix regex).
    - `grep -nE "/\\^CE\\\\d/i\\.test" src/lib/supplier-canonical.ts` matches at least one line (adidas CE-prefix regex).
    - `grep -nE "if \\(routesViaSS\\(pid\\)\\)" src/lib/supplier-canonical.ts` matches exactly one line (single dispatcher call — no duplication).
    - `grep -cE "describe\\(.17-04 prefix routing" tests/lib/supplier-canonical.test.ts` returns 1.
    - `npx vitest run tests/lib/supplier-canonical.test.ts` reports all 17-04 tests passing (≥ 18 new tests).
    - `npx vitest run` (full suite) reports no regressions. Specifically: 17-02 tests still pass (the S&S branch body is unchanged); Phase 16 tests still pass (S* native routing unchanged).
    - H08 invariant holds: `npx vitest run tests/lib/supplier-canonical.test.ts -t "H08"` reports the existing Phase 16 H08-early-return test passing, AND the new Test 10 passing.
  </acceptance_criteria>
  <done>
    `routesViaSS` is exported and covers adidas A*/CE* + all 19 KNOWN_SUPPLIER_PREFIXES entries. The dispatcher calls it exactly once. H08 early-return still fires first. CSW L* routing is unchanged. 18 new tests passing. Full suite green.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` (full suite) is green.
- `grep` acceptance criteria above all pass.
- Manual operator smoke test (in SUMMARY runbook): re-run the audit on the FIRST 50 polluted pids that previously logged "no canonical for tier 1" in `tmp/image-pollution-fix-trail-2026-05-14.tsv`. Expected: ≥ 30 of those 50 now produce a SUPPLIER_FETCH row with a non-null URL. Real-yield TBD — the actual count depends on (a) whether S&S's `/styles/?search=` recognizes the brand pid string verbatim (per A17-3, A17-4 assumptions) and (b) whether the BR colorName matches an S&S color variant for that pid.
</verification>

<success_criteria>
- adidas A* and CE* pids route via S&S (Finding 1 honored).
- All 19 KNOWN_SUPPLIER_PREFIXES pids route via S&S (Finding 2 honored).
- H08 invariant (D-22) preserved — headwear out of scope.
- CSW L* routing unchanged.
- M786 / NE220 / 9xx / QTB6000 still return null — true unsupported brands handled by Plan 17-05 manual triage.
- Order: `routesViaSS` check is exactly ONE call in the dispatcher (no body duplication).
- Log lines surface which routing rule fired (operator observability).
- Researcher-disciplined estimate post-17-04: 714 "no canonical" trail rows → ≤ 100. Real-yield TBD; document actuals in SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/17-catalog-image-pollution-fix/17-04-SUMMARY.md` recording:
- Number of new tests added.
- Operator runbook entry: re-run audit on the 449-pid corpus, count "no canonical for tier 1" trail rows (`awk -F'\t' '$3 == "SUPPLIER_FETCH" && $9 ~ /no canonical/ {print}' tmp/image-pollution-fix-trail-{date}.tsv | wc -l`). Expected: drops from 714 to ≤ 100.
- Per-brand breakdown of new Tier 1 unlocks (e.g., "Bella+Canvas 3001: 12 pids now resolve; adidas A702: 1 pid resolves; Gildan 5200: 8 pids resolve").
- Any S&S `/styles/?search=` surprises (e.g., a brand pid that doesn't resolve because S&S uses a different styleName — A17-3 / A17-4 risk realized).
- Confirmation that the H08 invariant still holds (no headwear leaked into the dispatcher).
</output>
