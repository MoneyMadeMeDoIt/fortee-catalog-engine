---
phase: 16-catalog-image-pollution-audit-fix
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - scripts/fix-image-pollution.ts
  - tests/scripts/fix-image-pollution.test.ts
autonomous: true
requirements: [R3, R4, R6, R7, R8, R9, R11]

must_haves:
  truths:
    - "Running `npx tsx scripts/fix-image-pollution.ts` reads the latest tmp/image-pollution-audit-{date}.tsv, executes Tier 1 then Tier 2 sequentially (D-21), and produces tmp/image-pollution-manual-queue-{date}.tsv for any pids that cascade."
    - "Tier 1 (supplier fetch) writes to BR and Drive ONLY when verifier-after-fix passes. On verifier fail, no mutation occurs and the pid cascades to Tier 2 (D-18)."
    - "Tier 2 invokes Phase 10 `generateGarmentView` (which already runs Phase 15 verifier internally — no second verifier call needed per RESEARCH lines 513-514)."
    - "After Tier 2 completes, if manual queue size > 20 (R6), the script writes a JSON summary with `manual_queue_size`, prints `BLOCKED-QUEUE-OVERFLOW`, and exits with code 2."
    - "Drive update-in-place gotcha (T-16-01) is mitigated: every Drive write captures origFileId AND newFileId; trashDriveFile is called ONLY when origFileId !== newFileId."
    - "Resume from trail: on startup, loadProcessedPids returns the set of {BR_WRITE, MANUAL_SKIP, MANUAL_ACCEPT} pids from today's trail; the orchestrator skips them silently (D-15)."
    - "Front-pollution-first ordering: within a pid that has BOTH front + back/side pollution, Tier 1 fixes Front BEFORE Back/Side (so back/side verifier-after-fix uses the newly-fixed front as source-of-truth per D-17)."
  artifacts:
    - path: "scripts/fix-image-pollution.ts"
      provides: "Tier 1 + Tier 2 fix orchestrator + R6 manual queue gate"
      exports: ["runImagePollutionFix", "RunImagePollutionFixDeps", "tier1Fix", "tier2Fix"]
    - path: "tmp/image-pollution-manual-queue-{YYYY-MM-DD}.tsv"
      provides: "Tier 3 input queue for manual CLI (Plan 04)"
      contains: "pid\tpollution_class\taffected_columns\tcurrent_drive_urls\tfront_image_screenshot_link\tsuggested_action"
    - path: "tmp/image-pollution-fix-summary-{YYYY-MM-DD}.json"
      provides: "Run summary: tier1_fixed, tier2_fixed, manual_queue_size, total_cost_usd"
      contains: "manual_queue_size"
  key_links:
    - from: "scripts/fix-image-pollution.ts"
      to: "src/sheets/writer.ts (writeUpdates)"
      via: "atomic per-cell BR write via project helper"
      pattern: "writeUpdates\\("
    - from: "scripts/fix-image-pollution.ts"
      to: "src/sheets/drive.ts (uploadToDrive + trashDriveFile + extractFileId)"
      via: "update-in-place + compare-before-trash"
      pattern: "origFileId !== newFileId"
    - from: "scripts/fix-image-pollution.ts"
      to: "src/lib/ai-image-generator.ts (generateGarmentView)"
      via: "Tier 2 AI regen with built-in Phase 15 verifier"
      pattern: "generateGarmentView"
    - from: "scripts/fix-image-pollution.ts"
      to: "src/lib/image-pollution-trail.ts (appendTrailRow + loadProcessedPids)"
      via: "every Drive/BR mutation logged with fsync"
      pattern: "appendTrailRow"
---

<objective>
Build the Tier 1 + Tier 2 fix orchestrator `scripts/fix-image-pollution.ts`. Consumes the audit TSV from Plan 02 and the trail from Plan 01 (for resume). Executes Tier 1 (supplier fetch) and Tier 2 (AI regen) sequentially per D-21. After Tier 2, computes manual queue size; if > 20 (R6) writes BLOCKED-QUEUE-OVERFLOW summary and exits 2. Otherwise writes the manual queue TSV for Plan 04 to consume.

Purpose: enforce the R6 hard cap algorithmically (no operator workload spike); enforce R8/R9 safety rails (verifier-after-fix mandatory on every write; Drive delete only after explicit pollution confirmation); mitigate T-16-01 (Drive update-in-place data destruction) at every Drive write site.

Output:
- 1 new orchestrator script
- 1 new test file with R3/R4/R6/R7/R8/R9 coverage
</objective>

<execution_context>
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ugofo/Downloads/Claude code project/Auto add product/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-catalog-image-pollution-audit-fix/16-SPEC.md
@.planning/phases/16-catalog-image-pollution-audit-fix/16-CONTEXT.md
@.planning/phases/16-catalog-image-pollution-audit-fix/16-RESEARCH.md
@.planning/phases/16-catalog-image-pollution-audit-fix/16-PATTERNS.md
@.planning/phases/16-catalog-image-pollution-audit-fix/16-01-PLAN.md
@scripts/audit-garment-types.ts
@scripts/fetch-ss-images-fixed.ts
@src/sheets/writer.ts
@src/sheets/drive.ts
@src/sheets/column-map.ts
@src/lib/ai-image-generator.ts
@src/shopify/image-standardizer.ts

<interfaces>
<!-- From Plan 01 — primitives this orchestrator consumes -->
import {
  appendTrailRow,
  getOrCreateTrailRunId,
  loadProcessedPids,
  TrailRow,
} from '../src/lib/image-pollution-trail.js';
import { verifySameProduct } from '../src/lib/verify-same-product.js';
import { resolveSupplierCanonical, CanonicalResult } from '../src/lib/supplier-canonical.js';
import {
  extractFileId,
  downloadFromDrive,
  trashDriveFile,
  uploadToDrive,
  createDriveClient,
} from '../src/sheets/drive.js';

<!-- Phase 10 Tier 2 entry point — src/lib/ai-image-generator.ts:341-479 -->
// generateGarmentView ALREADY runs verifyGarmentTypeMatch internally (line 392).
// No second verifier call needed for Tier 2 outputs.
export async function generateGarmentView(
  frontBuffer: Buffer,
  view: 'back' | 'side',
  options?: { pid?: string; costTracker?: CostTracker },
): Promise<Buffer | null>;

<!-- Project Sheet writer — src/sheets/writer.ts:18-48 (DO NOT reimplement chunking) -->
import { writeUpdates } from '../src/sheets/writer.js';
// writeUpdates(sheets, spreadsheetId, updates): Promise<number>
// updates: Array<{ range: string; values: string[][] }>
// Chunks at 50_000 cells; uses RAW valueInputOption.

<!-- Project column helper — src/sheets/column-map.ts:11-19 -->
import { columnToLetter } from '../src/sheets/column-map.js';
// columnToLetter(idx: number): string  — converts 0-indexed column → A..Z, AA..AZ, ...

<!-- BR column header positions (verified via fetch-ss-images-fixed.ts:83-86 raw read) -->
// FrontImage, BackImage, DirectSideImage, ModelFrontImage, ModelSideImage, ModelBackImage
// productId column also present; index determined at runtime via header row.

<!-- Image download helper (project standard) -->
import { downloadImage } from '../src/shopify/image-standardizer.js';
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit TSV → orchestrator | Audit-generated row data with `pollution_class` enum + `affected_columns` list. |
| Supplier API → Buffer | Bytes from S&S / CSW could be malformed or wrong product. |
| OpenAI → verifier-after-fix decision | match/no-match result drives BR + Drive mutations. |
| Drive update-in-place | uploadToDrive returns SAME fileId on filename collision. |
| Trail TSV → resume | Trail rows from previous run determine which pids to skip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | T (Tampering) — DATA DESTRUCTION | Tier 1 Drive write, manual CLI Drive write | mitigate | At every Drive write site: capture `origFileId = extractFileId(currentUrl)` BEFORE upload; call `uploadToDrive(...)` to get `newUrl`; capture `newFileId = extractFileId(newUrl)`; call `trashDriveFile(driveClient, origFileId)` ONLY when `origFileId && newFileId && origFileId !== newFileId`. NEVER trash without that comparison. Add unit test that mocks `uploadToDrive` to return the same fileId and asserts `trashDriveFile` is NOT called. |
| T-16-02 | T — verifier false-pass | Tier 1 verifier-after-fix | mitigate | Verifier-after-fix uses `verifySameProduct` strict same-product prompt on Back/Side/Model writes. For FrontImage replacements, the verifier is SKIPPED per D-17 Exception (supplier canonical IS the source-of-truth — tautological). Trail logs both input fileIds (old + new) in `column_or_path` + `notes`. Post-mortem can compare via the trail. |
| T-16-03 | R — verifier false-fail blocks valid fix | Tier 1 + Tier 2 | mitigate | VERIFIER_FAIL row in trail with `notes=<reason>`. Pid cascades to Tier 2 (Tier 1 fail) or Tier 3 (Tier 2 fail). Manual CLI can `[f]orce` past with `FORCE` literal. |
| T-16-04 | T — delete good image | All Drive deletes | mitigate | Drive delete ONLY happens when (a) the audit TSV row for this pid has `pollution_class != null` AND (b) the verifier-after-fix returns `match: true` for the replacement (so we know we have a real fix in hand). NEVER blank a BR cell or trash a Drive file based solely on audit suspicion. Plan 03 does NOT support `[d]elete` choice — that's only in Plan 04 (manual CLI) with `DELETE` literal confirmation. |
| T-16-05 | D (DoS) — supplier rate limit | Tier 1 batched calls | mitigate | resolveSupplierCanonical (Plan 01) already enforces RATE_LIMIT_MS=1100 (S&S) / 1000 (CSW). Per-pid sequential within Tier 1 (D-20). Trail logs SUPPLIER_FETCH so cost is observable in real-time. |
| T-16-07 | T — concurrency race on trail | All trail writes | mitigate | D-20 default: sequential per-pid within each tier. Plan 03 uses single-threaded `for...of` loops. If a future contributor adds Promise.all, they MUST wrap appendTrailRow in a mutex (documented in Plan 01 JSDoc). |
| T-16-08 (NEW) | I (Information Disclosure) — cost/runtime overrun | Tier 2 AI regen | accept | D-24: no fix cost cap. The orchestrator prints running cost (sum of openai responses) every 10 pids so operator can ctrl-C. JSON summary records final cost. |

**Block on:** T-16-01 (Drive update-in-place compare-before-trash) and T-16-04 (no implicit deletes) MUST be tested and pass before merge.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orchestrator skeleton + Tier 1 (supplier fetch)</name>
  <files>
    scripts/fix-image-pollution.ts,
    tests/scripts/fix-image-pollution.test.ts
  </files>
  <read_first>
    - scripts/audit-garment-types.ts (entire file — CLI skeleton analog for parseArgs + env validation + isDirectRun guard)
    - scripts/fetch-ss-images-fixed.ts (lines 147-180 — read-update-write flow with `updates` array)
    - src/sheets/writer.ts (lines 18-48 — writeUpdates signature)
    - src/sheets/column-map.ts (lines 11-19 — columnToLetter)
    - src/sheets/drive.ts (lines 94-155 — uploadToDrive update-in-place semantics)
    - src/lib/image-pollution-trail.ts (Plan 01 — appendTrailRow, loadProcessedPids signatures)
    - .planning/phases/16-catalog-image-pollution-audit-fix/16-PATTERNS.md ("scripts/fix-image-pollution.ts" + "Pattern 6 — Drive update-in-place compare-before-trash (CRITICAL)")
    - .planning/phases/16-catalog-image-pollution-audit-fix/16-RESEARCH.md ("Tier-1 happy path skeleton")
  </read_first>
  <behavior>
    - Test 1 (DI seam): `runImagePollutionFix(mockDeps)` accepts injected deps + returns `{tier1Fixed, tier2Fixed, cascadedToTier3, manualQueueSize, manualQueuePath}`.
    - Test 2 (audit TSV parser): given a tmp/image-pollution-audit-2026-05-12.tsv with 3 polluted rows, the orchestrator parses them into PollutionRow objects with the right `affected_columns` list and `recommended_fix_tier`.
    - Test 3 (resume from trail): mocked `loadProcessedPids` returns `Set(['pidA'])`. Audit TSV has rows for pidA + pidB. The orchestrator processes ONLY pidB (pidA silently skipped). Asserted via verifier call counts.
    - Test 4 (R3 happy path — Tier 1): pid 'S05610' with content_mismatch on FrontImage. Mocked resolveSupplierCanonical returns `{url: 'https://www.ssactivewear.com/x_fl.jpg', source: 'ss'}`. Mocked downloadImage returns Buffer. Per D-17 Exception (CONTEXT.md, revision iteration 1), the verifier-after-fix call is SKIPPED for FrontImage replacement because supplier-canonical IS the source-of-truth (tautological comparison). Mocked verifySameProduct is NOT called for FrontImage. Mocked uploadToDrive returns NEW url with different fileId. Assert: deps.verifySameProductFn.mock.calls.length === 0 for this pid; trashDriveFile called with origFileId; writeUpdates called with the new URL; trail has SUPPLIER_FETCH (with `notes='verifier_skipped_tautology'`, tier=1) + DRIVE_UPLOAD + DRIVE_DELETE + BR_WRITE rows. NO VERIFIER_PASS row for FrontImage.
    - Test 5 (R3 Tier 1 — back/side replacement): pid with content_mismatch on BackImage (not Front). The verifier-after-fix MUST run (per D-17 + RESEARCH line 516-518) comparing new candidate Back against current FrontImage. Mocked verifySameProduct returns `{match: true}` → write proceeds. Trail has VERIFIER_PASS + DRIVE_UPLOAD + BR_WRITE.
    - Test 6 (R9 safety rail — verifier-after-fix fail): pid with content_mismatch on BackImage. Mocked verifySameProduct returns `{match: false, reason: 'shape drift'}`. Assert: writeUpdates NOT called; trashDriveFile NOT called; trail has VERIFIER_FAIL with the reason; pid added to `cascadedToTier3` for now (Plan 03 doesn't have Tier 2 yet — comes Task 2).
    - Test 7 (T-16-01 — Drive update-in-place compare-before-trash): pid where mocked uploadToDrive returns SAME fileId as origFileId. Assert: trashDriveFile NOT called even though write succeeded. Trail has DRIVE_UPLOAD but NO DRIVE_DELETE.
    - Test 8 (Front-first ordering): pid with content_mismatch on BOTH FrontImage AND BackImage. Tier 1 processes FrontImage FIRST (so the back-side fix uses the newly-fixed front as source-of-truth). Asserted via the order of writeUpdates calls.
    - Test 9 (no_canonical_available → cascade): pid where supplier-canonical returns null. Tier 1 logs `notes='no canonical for tier 1'` to trail and cascades the pid to Tier 2 (or Tier 3 if shape-not-applicable). No writes.
  </behavior>
  <action>
    Create `scripts/fix-image-pollution.ts`. Pattern: CLI skeleton from `scripts/audit-garment-types.ts` + read-update-write flow from `scripts/fetch-ss-images-fixed.ts`. Concrete structure:

    1. Safety-comment disclaimer at top (informational, not enforced — this script DOES write):
       ```
       // Tier 1 + Tier 2 image pollution fix orchestrator for Phase 16 (SPEC R3, R4, R6, R8, R9).
       // Reads tmp/image-pollution-audit-{date}.tsv; writes BR + Drive on verifier-passing fixes.
       // R6 GATE: if manual queue > 20 after Tier 2, exits with code 2 (BLOCKED-QUEUE-OVERFLOW).
       // T-16-01 mitigation: every Drive write compares origFileId vs newFileId BEFORE trashing.
       ```

    2. Types:
       ```
       export interface PollutionRow {
         pid: string;
         pollution_class: 'shared_url' | 'invalid_image_format' | 'content_mismatch' | 'model_pollution' | 'shape_drift';
         affected_columns: string[];           // ['FrontImage', 'BackImage', ...]
         affected_drive_urls: string[];        // parallel array to affected_columns
         expected_supplier_url: string | null; // canonical url or null
         recommended_fix_tier: 1 | 2 | 3;
         notes: string;
       }
       export interface TierResult {
         tier: 1 | 2 | 3;
         status: 'fixed' | 'verifier_rejected' | 'no_canonical' | 'cascade' | 'skipped';
         cascade: boolean;
       }
       export interface RunImagePollutionFixDeps {
         args: RunImagePollutionFixArgs;
         sheetsClient: sheets_v4.Sheets;
         driveClient: drive_v3.Drive;
         openai: OpenAI;
         readRawRowsFn: (sheets, spreadsheetId, tab) => Promise<string[][]>;
         downloadImageFn: typeof downloadImage;
         downloadFromDriveFn: typeof downloadFromDrive;
         supplierCanonicalFn: typeof resolveSupplierCanonical;
         verifySameProductFn: typeof verifySameProduct;
         generateGarmentViewFn: typeof generateGarmentView;
         uploadToDriveFn: typeof uploadToDrive;
         trashDriveFileFn: typeof trashDriveFile;
         writeUpdatesFn: typeof writeUpdates;
         appendTrailRowFn: typeof appendTrailRow;
         loadProcessedPidsFn: typeof loadProcessedPids;
         spreadsheetId: string;
         sheetName: string;            // 'Bestsellers-Ready'
       }
       ```

    3. `parseAuditTsv(path: string): Promise<PollutionRow[]>` — reads the TSV, skips `#` summary lines + header, splits each row, parses `affected_columns` and `affected_drive_urls` from comma-separated strings. Wrap in try/catch — empty TSV returns `[]`.

    4. `tier1Fix(pid, polluted, brRowIndex, brHeaderMap, deps): Promise<TierResult>`:
       - Resolve `canonical = await deps.supplierCanonicalFn(pid)`. If null:
         - `await deps.appendTrailRowFn({operation: 'SUPPLIER_FETCH', notes: 'no canonical for tier 1', tier: 1, ...})`
         - Return `{tier: 1, status: 'no_canonical', cascade: true}`.
       - **Front-first ordering**: sort polluted.affected_columns so 'FrontImage' is first if present.
       - For each `col` in sorted affected_columns:
         - Determine `sourceOfTruthBuf` per D-17:
           - If `col === 'FrontImage'`: source = supplier canonical (downloaded fresh).
           - Else: source = current BR FrontImage (download via `downloadFromDriveFn(driveClient, extractFileId(brFront)!)` — use the latest front, which may have been updated earlier in this loop iteration).
         - Download new candidate via `deps.downloadImageFn(canonical.url)` — log SUPPLIER_FETCH.
         - For `col === 'FrontImage'`: skip the verifySameProduct call (RESEARCH lines 510-512 — tautology) and trust the supplier API response. Log a single trail row with `operation='SUPPLIER_FETCH'`, `tier=1`, and `notes='verifier_skipped_tautology'` per D-17 Exception (CONTEXT.md amendment, revision iteration 1). Do NOT emit a fake VERIFIER_PASS row.
         - Else: call `verifyResult = await deps.verifySameProductFn(deps.openai, newBuf, sourceOfTruthBuf)`. Log VERIFIER_PASS or VERIFIER_FAIL.
         - If `verifyResult?.match === false`:
           - Log no further mutation; return `{tier: 1, status: 'verifier_rejected', cascade: true}`.
         - **Drive write with compare-before-trash (T-16-01 mitigation)**:
           ```
           const origFileId = extractFileId(currentBrUrl);
           const filename = `${supplierPrefix}_${col}.png`;
           const newUrl = await deps.uploadToDriveFn(deps.driveClient, newBuf, filename, supplierCode, styleId);
           const newFileId = extractFileId(newUrl);
           await deps.appendTrailRowFn({operation: 'DRIVE_UPLOAD', column_or_path: newFileId, old_value: origFileId ?? '', new_value: newFileId ?? '', tier: 1, ...});
           if (origFileId && newFileId && origFileId !== newFileId) {
             await deps.trashDriveFileFn(deps.driveClient, origFileId);
             await deps.appendTrailRowFn({operation: 'DRIVE_DELETE', column_or_path: origFileId, tier: 1, notes: 'replaced by ' + newFileId, ...});
           }
           ```
         - **BR write**:
           ```
           const colIdx = brHeaderMap[col];
           const range = `'${deps.sheetName}'!${columnToLetter(colIdx)}${brRowIndex + 1}`;
           await deps.writeUpdatesFn(deps.sheetsClient, deps.spreadsheetId, [{range, values: [[newUrl]]}]);
           await deps.appendTrailRowFn({operation: 'BR_WRITE', column_or_path: col, old_value: currentBrUrl, new_value: newUrl, tier: 1, ...});
           ```
       - Return `{tier: 1, status: 'fixed', cascade: false}` if all columns processed without verifier-fail.

    5. `runImagePollutionFix(deps)` (Task 1 only implements Tier 1 — Task 2 adds Tier 2):
       - Find latest `tmp/image-pollution-audit-${date}.tsv` (or accept `--audit-file <path>` arg).
       - Call `parseAuditTsv` → `pollutedRows: PollutionRow[]`.
       - Call `deps.loadProcessedPidsFn()` → `processedPids: Set<string>`. Skip rows whose pid is in this set.
       - Read raw BR rows for row-index mapping (`pid → BR row index`).
       - For each polluted row (sequential per D-20):
         - Try `tier1Fix(...)` in try/catch.
         - Collect results into `tier1Results: TierResult[]`.
       - Compute `tier1Fixed = count where status='fixed'`, `cascadedToTier2 = pids where cascade=true`.
       - (Task 1 stops here; Task 2 implements Tier 2 + R6 gate.)
       - Return preliminary summary.

    6. CLI entry `main()`:
       - parseArgs: `--audit-file <path>`, `--limit N`, `--dry-run`, `--pid <pid>`, `--help/-h`.
       - Env validation: OPENAI, GOOGLE_*, SS_*.
       - Construct concrete deps; call `runImagePollutionFix(deps)`; print summary.
       - isDirectRun guard.

    Create `tests/scripts/fix-image-pollution.test.ts`. Mock every dep via vi.fn. Cover Tests 1–9 above. Use synthetic BR rows: header + 3 data rows, with productId, FrontImage, BackImage, DirectSideImage, ModelFrontImage, ModelSideImage, ModelBackImage columns.

    Critical scope note for Task 1: only Tier 1 is implemented. Task 2 will fill Tier 2 + R6 gate + manual queue emission.
  </action>
  <verify>
    <automated>npx vitest run tests/scripts/fix-image-pollution.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export " scripts/fix-image-pollution.ts` >= 5 (runImagePollutionFix, PollutionRow, TierResult, RunImagePollutionFixDeps, tier1Fix).
    - `grep -nE "origFileId !== newFileId" scripts/fix-image-pollution.ts` matches at least one line (T-16-01 mitigation).
    - `grep -nE "FrontImage" scripts/fix-image-pollution.ts | grep -i "first\|sort\|priority"` matches at least one line (front-first ordering).
    - `grep -nE "no_canonical|no canonical" scripts/fix-image-pollution.ts` matches at least one line.
    - `grep -nE "loadProcessedPids" scripts/fix-image-pollution.ts` matches at least one line.
    - `grep -nE "VERIFIER_FAIL|VERIFIER_PASS" scripts/fix-image-pollution.ts` matches at least 2 lines (Back/Side/Model verifier calls; FrontImage replacement does NOT emit VERIFIER_PASS).
    - `grep -nE "verifier_skipped_tautology" scripts/fix-image-pollution.ts` matches at least one line (D-17 Exception trail row).
    - `grep -nE "DRIVE_UPLOAD|DRIVE_DELETE|BR_WRITE|SUPPLIER_FETCH" scripts/fix-image-pollution.ts` matches at least 4 lines.
    - `grep -nE "columnToLetter\\(" scripts/fix-image-pollution.ts` matches at least one line (project-standard column helper).
    - `grep -nE "writeUpdates\\(" scripts/fix-image-pollution.ts` matches at least one line.
    - `npx vitest run tests/scripts/fix-image-pollution.test.ts` reports >= 9 tests passing.
  </acceptance_criteria>
  <done>
    Tier 1 fix path is implemented, tested against R3 happy path + R9 verifier-fail safety rail + T-16-01 update-in-place gotcha + D-17 source-of-truth ordering + D-15 resume-from-trail. CLI parseArgs + env validation in place. Task 2 will extend.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tier 2 (AI regen) + R6 gate + manual queue emission</name>
  <files>
    scripts/fix-image-pollution.ts,
    tests/scripts/fix-image-pollution.test.ts
  </files>
  <read_first>
    - scripts/fix-image-pollution.ts (current state after Task 1)
    - tests/scripts/fix-image-pollution.test.ts (current state after Task 1)
    - src/lib/ai-image-generator.ts (lines 341-479 — generateGarmentView with strict AND filter at line 392)
    - .planning/phases/16-catalog-image-pollution-audit-fix/16-RESEARCH.md ("Tier 2 invocation" lines 661-664 + R6 gate snippet lines 664-672)
    - .planning/phases/16-catalog-image-pollution-audit-fix/16-CONTEXT.md (D-11, D-21)
  </read_first>
  <behavior>
    Tier 2:
    - Test 10 (R4 happy path): pid with shape_drift on BackImage cascaded from Tier 1 (or original recommended_fix_tier=2). Mocked `generateGarmentView` returns a non-null Buffer. Assert: uploadToDrive called with new buffer; writeUpdates called for BackImage column; trail has AI_REGEN + DRIVE_UPLOAD + BR_WRITE. NO second verifySameProduct call (RESEARCH line 514: generateGarmentView already runs verifier internally).
    - Test 11 (R4 Tier 2 retry-fail cascade): pid where mocked `generateGarmentView` returns `null` (Phase 15 strict-AND filter rejected all candidates). Assert: no writeUpdates; pid added to manual queue; trail has AI_REGEN row with `notes='Tier 2 returned null — all candidates verifier-rejected'`.
    - Test 12 (T-16-01 in Tier 2): Tier 2 upload-and-trash logic mirrors Tier 1: compare origFileId vs newFileId before trashing. Same assertion as Test 7.
    - Test 13 (D-21 — no concurrency between tiers): Tier 1 must complete FULLY before Tier 2 starts. Asserted by the order of mocked calls — all Tier 1 supplier-canonical calls happen before any Tier 2 generateGarmentView call.

    R6 gate:
    - Test 14 (R6 hard cap — pass): feed 19 pids that cascade to Tier 3. Assert: script exits with code 0; writes manual queue TSV with 19 rows; writes JSON summary with `manual_queue_size: 19`.
    - Test 15 (R6 hard cap — BLOCK): feed 21 pids that cascade to Tier 3. Assert: script exits with code 2; prints `BLOCKED-QUEUE-OVERFLOW`; writes JSON summary with `manual_queue_size: 21, status: 'BLOCKED-QUEUE-OVERFLOW'`. The manual queue TSV is still written (so operator can inspect the overflow before re-planning).

    Manual queue TSV emission:
    - Test 16 (queue TSV format): for each pid cascading to Tier 3, the queue row has columns `pid\tpollution_class\taffected_columns\tcurrent_drive_urls\tfront_image_screenshot_link\tsuggested_action`. `front_image_screenshot_link` = `https://drive.google.com/uc?id=<frontFileId>` for terminal-clickable convenience. `suggested_action` = derived from `pollution_class`: 'replace with operator-picked URL' for content_mismatch/model_pollution, 'delete + leave blank for next push regen' for shape_drift after Tier 2 failure.

    JSON summary emission:
    - Test 17 (summary JSON shape): file `tmp/image-pollution-fix-summary-${date}.json` is written with keys `audit_run_id`, `tier1_fixed`, `tier2_fixed`, `cascaded_to_tier3`, `manual_queue_size`, `manual_queue_path`, `total_cost_usd_estimate`, `status` (one of 'OK' or 'BLOCKED-QUEUE-OVERFLOW'), `processed_pid_count`.
  </behavior>
  <action>
    Extend `scripts/fix-image-pollution.ts` from Task 1.

    1. `tier2Fix(pid, polluted, brRowIndex, brHeaderMap, deps): Promise<TierResult>`:
       - Download BR FrontImage via `downloadFromDriveFn`.
       - For each `col` in `polluted.affected_columns` that is `BackImage` or `DirectSideImage`:
         - `const view = col === 'BackImage' ? 'back' : 'side'`.
         - `const regenBuf = await deps.generateGarmentViewFn(frontBuf, view, { pid });`
         - `await deps.appendTrailRowFn({operation: 'AI_REGEN', column_or_path: col, old_value: currentUrl, new_value: regenBuf === null ? '' : '<buffer>', tier: 2, notes: regenBuf === null ? 'Tier 2 returned null — all candidates verifier-rejected' : 'Tier 2 success', ...})`.
         - If `regenBuf === null`: return `{tier: 2, status: 'verifier_rejected', cascade: true}` (cascade to Tier 3).
         - Else: write to Drive + BR with the SAME T-16-01 compare-before-trash logic as Tier 1. Trail rows: DRIVE_UPLOAD + DRIVE_DELETE (if applicable) + BR_WRITE.
       - Return `{tier: 2, status: 'fixed', cascade: false}` if all columns succeed.

    2. Wire Tier 2 into `runImagePollutionFix`:
       ```
       const tier1Results = ...; // from Task 1
       const cascadedToTier2 = tier1Results
         .filter(r => r.cascade)
         .map(r => r.pid);
       // D-21: Tier 1 fully complete before Tier 2 starts
       const tier2Results: TierResult[] = [];
       for (const pid of cascadedToTier2) {
         // only attempt Tier 2 if affected_columns includes back/side
         const polluted = pollutedRows.find(p => p.pid === pid)!;
         const hasBackOrSide = polluted.affected_columns.some(c => c === 'BackImage' || c === 'DirectSideImage');
         if (!hasBackOrSide) {
           tier2Results.push({tier: 2, status: 'skipped', cascade: true});  // → Tier 3
           continue;
         }
         try {
           tier2Results.push(await tier2Fix(pid, polluted, ...));
         } catch (err) {
           logger.warn(`[fix-image-pollution] Tier 2 failed for ${pid}: ${err}`);
           tier2Results.push({tier: 2, status: 'verifier_rejected', cascade: true});
         }
       }
       ```

    3. Manual queue emission (Tier 3 input for Plan 04):
       ```
       const tier3Pids: string[] = [
         ...tier1Results.filter(r => r.status === 'fixed' ? false : !cascadedToTier2.includes(r.pid)).map(r => r.pid),
         ...tier2Results.filter(r => r.cascade).map((_, i) => cascadedToTier2[i]),
       ];
       const manualQueuePath = `tmp/image-pollution-manual-queue-${date}.tsv`;
       writeManualQueueTsv(manualQueuePath, tier3Pids, pollutedRows, brRowIndex, brHeaderMap);
       ```
       Helper `writeManualQueueTsv` writes the header row + one row per pid with the 6-column schema in Test 16. `suggested_action` derivation:
       - `content_mismatch | model_pollution | invalid_image_format | shared_url` → 'replace with operator-picked URL'
       - `shape_drift` (post-Tier-2 failure) → 'delete + leave blank for next push regen'

    4. R6 gate:
       ```
       const manualQueueSize = tier3Pids.length;
       const status = manualQueueSize > 20 ? 'BLOCKED-QUEUE-OVERFLOW' : 'OK';
       const summary = {
         audit_run_id: getOrCreateTrailRunId(),
         tier1_fixed: tier1Results.filter(r => r.status === 'fixed').length,
         tier2_fixed: tier2Results.filter(r => r.status === 'fixed').length,
         cascaded_to_tier3: manualQueueSize,
         manual_queue_size: manualQueueSize,
         manual_queue_path: manualQueuePath,
         total_cost_usd_estimate: <computed>,
         status,
         processed_pid_count: pollutedRows.length - processedPids.size,
       };
       writeFileSync(`tmp/image-pollution-fix-summary-${date}.json`, JSON.stringify(summary, null, 2));
       console.log(summary);
       if (status === 'BLOCKED-QUEUE-OVERFLOW') {
         console.error(`BLOCKED-QUEUE-OVERFLOW: manual queue size ${manualQueueSize} exceeds R6 cap (20). Run --post-mortem for analysis. Plan D-12 scraper expansion.`);
         process.exit(2);
       }
       ```

    5. Cost tracking: maintain `costUsd = 0` over the run. Each VERIFIER call (Pass 1 doesn't have any — they're free), each AI_REGEN. Use rough estimate `0.0003` per Vision call, `0.04` per generateGarmentView. Append to summary.

    Extend the test file with Tests 10–17 above.
  </action>
  <verify>
    <automated>npx vitest run tests/scripts/fix-image-pollution.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "(async function tier2Fix|^async function tier2Fix)" scripts/fix-image-pollution.ts` matches one line.
    - `grep -nE "generateGarmentView" scripts/fix-image-pollution.ts` matches at least one line.
    - `grep -nE "AI_REGEN" scripts/fix-image-pollution.ts` matches at least 2 lines.
    - `grep -nE "BLOCKED-QUEUE-OVERFLOW" scripts/fix-image-pollution.ts` matches at least 2 lines.
    - `grep -nE "process\\.exit\\(2\\)" scripts/fix-image-pollution.ts` matches one line.
    - `grep -nE "image-pollution-manual-queue-" scripts/fix-image-pollution.ts` matches at least one line.
    - `grep -nE "image-pollution-fix-summary-" scripts/fix-image-pollution.ts` matches at least one line.
    - `grep -nE "manual_queue_size" scripts/fix-image-pollution.ts` matches at least 2 lines.
    - Tier 2 does NOT call verifySameProduct (Phase 15 verifier is internal to generateGarmentView): `grep -A 20 "function tier2Fix" scripts/fix-image-pollution.ts | grep -c "verifySameProductFn"` returns 0.
    - `npx vitest run tests/scripts/fix-image-pollution.test.ts` reports >= 17 tests passing total (Task 1's 9 + Task 2's 8).
  </acceptance_criteria>
  <done>
    Tier 2 implemented with no double-verifier (uses Phase 10's built-in Phase 15 check). R6 hard cap algorithmically enforced — exits 2 + writes JSON summary + still writes manual queue TSV when blocked. D-21 (no cross-tier concurrency) and front-first ordering preserved.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `npx vitest run tests/scripts/fix-image-pollution.test.ts` reports >= 17 tests passing.
- `npx vitest run` (full suite) shows no regressions in Phase 15 + Phase 16 Plan 01/02 tests.
- Manual smoke test (operator-only): create a synthetic 1-pid audit TSV at `tmp/image-pollution-audit-2026-05-12.tsv` for a known-good pid; run `npx tsx scripts/fix-image-pollution.ts --dry-run --pid <known-good>`; expect 0 mutations and "OK" status in summary.
</verification>

<success_criteria>
- Tier 1 happy path + verifier-after-fail cascade + T-16-01 update-in-place mitigation all verified.
- Tier 2 invokes generateGarmentView with NO second verifier call (delegated to Phase 15 internal).
- D-15 resume-from-trail honored (loadProcessedPids skip).
- D-17 front-first ordering enforced and tested.
- D-21 no cross-tier concurrency.
- R6 hard cap: ≤ 20 → status OK, > 20 → BLOCKED + exit code 2.
- JSON summary + manual queue TSV both written.
- T-16-04 invariant: no Drive deletes without verifier-confirmed pollution + verifier-confirmed fix (Tier 1/2 only delete the OLD file after the NEW file passes verification AND has a different fileId).
</success_criteria>

<output>
After completion, create `.planning/phases/16-catalog-image-pollution-audit-fix/16-03-SUMMARY.md` capturing: total lines of scripts/fix-image-pollution.ts, test counts (Tier 1 vs Tier 2 vs R6 gate vs queue emission), any edge cases encountered (e.g., pids with both Front and Back pollution and whether front-first ordering held), and a sample run summary JSON if test fixtures permit.
</output>
